# Changelog

All notable Rakka changes should be recorded here before a release candidate is cut.

The format follows Keep a Changelog style sections, and versioning is expected to follow SemVer once v1 release candidates begin.

## Unreleased

### Added

- V1 hardening foundations for TCP remoting, cluster runtime integration, compatibility matrix tests, generated HTTP/gRPC contract examples, observability exporters, Kubernetes local-cluster scenarios, security defaults, and repository validation scripts.
- `scripts/validate.sh` as the required local validation entry point.
- `scripts/package-check.sh` as the offline package metadata and publishability check entry point.
- V1 release-candidate review docs for reliability boundaries, N/N+1 rolling updates, known limitations, post-v1 roadmap, and final review checklist.
- Phase 5 agent workflow autonomy APIs: effect target catalog and policy validation, A2A peer adapter contracts, dispatcher registry routing, A2A peer/webhook/push dispatch classes, and deterministic dispatcher cancellation marking.
- `rakka-agent`: the crate that will own the durable agent domain — goal, typed task, run, evaluation, handoff, delegation, team, moderation, and workflow-tool contracts, the durable loop runtime, the provider-neutral model adapter trait, the continuous wake controller, the escrow budget ledger, autonomy admission, guardrails, checkpoints, tool authority, memory traits, structured telemetry, bounded operational queries, and deterministic test support. The first release landed the crate scaffolding: the documented module map, the `rig` (default) and `otel` feature gates, and the `agent`, `agent-rig`, and `agent-otel` facade features. The crate builds and tests with `--no-default-features`, which `scripts/validate.sh` now enforces.
- `rakka-agent` identity, definition, and agent-entity contracts:
  - Tenant-scoped identities (`AgentId`, `AgentGoalId`, `AgentTaskId`, `AgentRunId`, `AgentDelegationId`, `AgentWakeId`, `AgentEnvironmentRef`, `KnowledgeSpaceId`) that stay distinct types even where their values coincide, plus the composite scope keys (`AgentScope`, `AgentTaskScope`, `AgentRunScope`) that address the sharded entities. Identifier values are validated and fail closed on deserialization so that flattening a composite scope into an entity or persistence id stays injective and cannot alias two tenants onto one durable record. `AgentRunBinding` fixes a run's task at construction; there is no setter to re-target it.
  - `AgentOperationId` and `AgentOperationKind`: derived — not generated — stable operation and deduplication identifiers, convertible to the substrate's `AgentDeduplicationKey` and `AgentCommandId` so the durable inbox and outbox deduplicate on the same value the agent domain reasons about.
  - `AgentDefinitionRevision`, `SettingsRevision` with the turn-bound / immediate-safety / run-pinned timing classes, and `AgentSetupRevision`. `AgentAuthorityEnvelope` carries the definition's authority in one place, and a setup or settings revision may only narrow it: introducing an undeclared tool, weakening a mandatory guardrail, choosing an unapproved model, widening credential/knowledge/environment access, adding an unauthorized peer, downgrading a tool's effect safety, rerouting a tool through a different execution policy (opaque policies cannot be ranked, so the declared routing is pinned), or raising a budget ceiling is rejected with a stable reason code. Definition content is validated at construction, on deserialization, and again at the entity's accept path, so an out-of-bounds description can neither cross the wire nor be persisted, and a settings revision's change list is bounded on the load path as well. `effective_settings_for_turn` resolves a run's pinned revision against the agent's current one.
  - Sharded `AgentEntity`, keyed `(TenantId, AgentId)`, owning the durable definition, lifecycle status (`Active`/`Suspended`/`Terminated`), current settings revision, policy and logical credential-binding references, and the agent-private memory namespace. Its `AgentEntityCommand`/`AgentEntityReply` protocol is serializable from the first commit and routable over `rakka-remote` via `init_agent_entity_remote_sharding`. Administrative commands deduplicate on their operation id and return the original outcome on replay; settings updates are fenced on the settings revision they expect to succeed, and suspend/resume/terminate on the monotonic lifecycle revision they expect to advance, so a replay that has aged out of the deduplication window cannot reorder over a decision its initiator never saw. Instantiation takes the definition content and publishes the initial revision itself, so a foreign revision number or schema version cannot enter the first durable record. `load_agent_entity_state` is the durable read path that keeps run creation out of the entity's mailbox.
  - Schema versions on every persisted record, with a default N/N+1 compatibility policy (`AgentSchemaPolicy`). A record written by a newer binary, or one older than the supported window, fails closed on load rather than being interpreted with guessed semantics.
  - No resolved credential material appears in durable state, snapshots, or replies; credential bindings are logical references only.
- `rakka-agent` inter-entity choreography substrate:
  - `AgentExchangeJournal`, the durable saga record every choreography participant embeds in its own state. It is the entity's outbox and inbox at once: the exchanges it owes, the operations it has settled, and the operations it has applied together with the logical result each returned. Because it lives inside the participant's state record, an exchange is persisted in the *same* compare-and-set as the domain transition that owed it, and a receiver's decision is persisted in the same compare-and-set as the transition that produced it — so there is no window in which an entity has transitioned but forgotten what it owes, and none in which it has decided but not recorded the decision.
  - `AgentExchangeHost`, the durable host that owns recovery, the fail-closed schema check, deduplication, and the compare-and-set write of every transition. Participants supply only bounded in-memory transitions through `AgentExchangeParticipant`, and an accepted exchange may owe the next one atomically, which is what makes the canonical creation → assignment → run-acceptance chain of specification 9.8 unbreakable. A participant transition is stamped with the commit-time clock the host passes it, never with the envelope's initiator-side creation time, so a receiver's durable timestamps cannot regress under a skewed or replayed initiator. `drive_pending_exchanges` re-drives everything an entity owes from durable state under the operation ids its transitions first minted, so calling it after a transition, after recovery, or on a timer are the same operation.
  - `AgentEntityAddress` (agent, task, and run classes) as the routing key and durable record locator of an exchange, `AgentExchangeKind` for the six exchanges of M1 (creation, assignment, result proposal, budget allocation, settlement, and return), and the bounded, typed `AgentExchangePayload`. An exchange may not cross a tenant boundary, an entity may not apply an exchange addressed to another, and one operation id may not name two exchanges.
  - `AgentExchangeTransport`, `ShardedExchangeRoute`, `AgentExchangeRouter`, and `register_agent_exchange_codecs`: an exchange resolves its target's shard owner and is then either asked of the local entity or asked of the owning node over `rakka-remote`. Both paths reach the same durable accept path, so colocation changes the transport and never the durable record — and an exchange stays correct after its entities move to different nodes. Delivery stays at-most-once; a delivery failure is never read as evidence that the receiver did not apply the exchange.
  - The per-exchange failure-window table (initiator loss before send, receiver loss after acceptance, reply loss, duplicate delivery) is a doc section of `rakka_agent::choreography`, with the test that proves each row.
  - `rakka_agent::testkit` gained `ChoreographyProbe`, a fenced reference participant, and `InProcessExchangeTransport`, which injects those failure windows.
- `rakka-agent` typed-task entity:
  - Sharded `AgentTaskEntity`, keyed `(TenantId, AgentTaskId)`, owning the public work lifecycle (`Created`/`Blocked`/`Assigned`/`InProgress`/`WaitingForInput`/`Completed`/`Failed`/`Cancelled`) independently of any one execution. Its `AgentTaskEntityCommand`/`AgentTaskEntityReply` protocol is serializable from the first commit, and `init_agent_task_entity_remote_sharding` makes the entity reachable over `rakka-remote`, where the exchange envelope is the cross-entity command surface. Commands deduplicate on their operation id and return the original outcome on replay; the entity is a choreography participant, so an exchange it owes is persisted in the same compare-and-set as the transition that owed it.
  - Versioned `AgentTaskDefinition` with input/result schema references, deterministic result rules, rejection and assignment limits, dependency-failure policy, per-task budget ceilings, ownership policy, and required skills. It is validated at construction, on deserialization, and again at the entity's creation path; rule content is itself bounded (`AGENT_TASK_RULE_POINTER_MAX_LENGTH`, `AGENT_TASK_RULE_ONE_OF_MAX_VALUES`, `AGENT_TASK_RULE_VALUE_MAX_LENGTH`), so a definition cannot grow the materialized record without limit. `TypedTask<I, R>` is the only place generics appear: it binds Rust types to the definition's schema references as compile-time ergonomics, while durable state and exchange payloads stay non-generic, bounded, and versioned. Decoding an accepted result under a mismatched definition or schema revision fails closed.
  - The creation → assignment → run-acceptance flow of specification 9.8. The assignment decision reads the agent's durable definition and admission state through `load_agent_entity_state` — never a synchronous round trip through `AgentEntity`, so a popular agent is not a bottleneck for its own runs — and then commits the decision, the assignment generation, and the run-creation command it owes in one transition. The run identity is *derived* from the task and the generation (`run_id_for_assignment`), so replaying an assignment resolves to the same run rather than creating a second one. An agent that is suspended, absent, or does not declare the task definition in its authority envelope is refused, and the task stays assignable rather than failing; an unchanged refusal is recorded in history exactly once, however often the task settles. Because every run id must remain a valid identity for every reachable generation, an agent-owned task id longer than `AGENT_TASK_ASSIGNABLE_ID_MAX_LENGTH` is refused at creation (`task-id-too-long`) rather than discovered at decision time. A delegated creation's accepted reply carries its `AgentTaskOutcome` under `AGENT_TASK_CREATION_OUTCOME_PAYLOAD_TYPE`, so `AGENT_TASK_DECISION_PAYLOAD_TYPE` names exactly one wire shape.
  - Deterministic in-entity result validation. A proposal is fenced on the current assignment generation, checked against the definition and schema revision it claims, and then run through every `AgentTaskResultRule`; a rejection is a durable decision carrying the rule, its revision, the proposal's digest, the rejection count, and its causation, and it is returned unchanged on replay. Exhausting the rejection budget fails the task; it never silently accepts the result it just refused. A superseded run's proposal consumes none of the live run's budget. Model-assisted evaluation stays a durable effect, never I/O inside a transition.
  - Bounded materialized state (specification 9.6): the record holds the current identity, status, revisions, dependency summary, assignment, pending references, accepted result, and terminal reason, and nothing that accumulates. Superseded assignments, proposals, and rejection decisions become `AgentTaskHistoryEntry` rows in an `AgentTaskHistoryStore`, readable only through a bounded, tenant-scoped cursor; content beyond the inline bound must arrive as an artifact reference. Admission reserves `AGENT_TASK_STATE_GROWTH_RESERVE_BYTES` of growth headroom below the materialized limit — the same reservation the id bound makes for derived run ids — so a task the entity admits is never later refused its own assignment, rejection, or terminal reason for want of room; a creation or late dependency declaration that would spend the reserve is refused instead, and a proposal whose causation id exceeds the identity bound is refused without a validation decision (`proposal-causation-too-long`). All durable task timestamps are stamped by the owner at commit time, never by the initiator's clock. History is written through a bounded outbox in the entity's own state and flushed idempotently, so no transition can commit while forgetting the history it owes.
  - Durable, bounded, acyclic dependencies. An edge is rejected if it would close a cycle against the dependency's own declared ancestry, and the default failed-dependency policy cancels dependents; `ContinueWithEvidence` must be chosen explicitly. Duplicate declarations inside one creation follow the post-creation rule: a repeat is idempotent, a conflicting policy is refused rather than last-wins, and the dependency limit counts edges, not declarations.
  - `rakka_agent::testkit` gained `RunAcceptanceProbe`, a minimal run that holds the far side of the assignment exchange still, and `InProcessTaskEntityTransport`, which drives exchanges into a real task entity that is re-materialized from durable state on every delivery.
- `rakka-agent` run entity and durable loop:
  - Sharded `AgentRunEntity`, keyed `(TenantId, AgentId, AgentRunId)`, owning one agent's contribution to one task: its loop state and status, its effects and accepted result, its own budget ledger, and its participation in the result-proposal exchange. `AgentRunBinding` fixes the task at construction, so a run serves exactly one task and one assignment generation for its whole life; handoff and reassignment create a new run. The `AgentRunEntityCommand`/`AgentRunEntityReply` protocol is serializable from the first commit, and `init_agent_run_entity_remote_sharding` makes the entity reachable over `rakka-remote`, where the exchange envelope is the cross-entity command surface.
  - The full run status set of specification 9.3. The substrate's single `WaitingForHuman` is split at this first commit into `WaitingForApproval`, `WaitingForAuthorization`, and `WaitingForReconciliation` — they resolve through three different durable decisions, and answering one is not answering another — so no agent-domain record was ever written under an unsplit status and no migration is owed. `AgentRunStatus::is_waiting_for_human` names the set the substrate's variant corresponds to, and is what slice 1.12 projects onto A2A. `Accepted`, `Running`, and `Cancelling` are not residency claims: the entity passivates whenever no bounded transition is immediately executable.
  - `AgentLoopPhase` and the versioned durable `AgentLoopState`: goal, task, turn, phase, the pinned agent definition and settings revisions, the context-snapshot reference a model effect was prepared against, pending effect references, the accepted result, sanitized rejection feedback, remaining budgets, pending checkpoint/timer references, and both the loop-state schema version and the adapter version — which version separately, so an adapter upgrade is an explicit migration rather than a silent reinterpretation of the turns the previous adapter wrote.
  - The execution rule of specification 9.5, enforced rather than asserted. Every loop arrow is one bounded transition and one compare-and-set, and every rest position is a durable wait: `PreparingContext` persists the model effect *and* the wait together, so there is no instant at which a run waits for an effect that was never recorded. `AgentRunEntityStore::settle_side_effects` cranks the loop from durable state alone — advance, dispatch, drive — so calling it after a transition, after recovery, or from a sweep are the same operation; `AGENT_RUN_MAX_LOOP_STEPS_PER_PASS` and `AGENT_RUN_MAX_SETTLE_ROUNDS` fence the handler's work.
  - The result proposal/decision exchange. The run persists its proposal before the exchange that carries it, under an id derived from the run and the turn, and it never makes the public task terminal by mutating its own state: the task's persisted validation decision is the source of truth for the outcome, and the run's state is the source of truth only for the run's consequence of it. A rejection returns sanitized feedback and a remaining-iteration count, and the run takes another bounded turn; an exhausted rejection budget fails the run rather than treating a refused result as accepted. A *refusal* — the task fenced the run by a newer generation, or has moved on — is not a validation decision, so the run stops rather than iterate, mapping the task's status onto `Superseded`, `Cancelled`, or `Failed`.
  - The interim durable effect: `AgentRunEffect` is a field of the run's own state, committed by the transition that decided it, and `AgentRunEffectSink` hands it to the existing agent-workflow `AgentEffect` outbox afterwards. The two cannot be one write, and that is the point — a run that committed `AwaitingModel` and then lost its node before writing the outbox row would wait forever for an effect nobody will dispatch. Dispatch is idempotent on the derived `effect_id`, so a re-driven flush names the same effect and shard movement cannot make one dispatchable twice. A result for an effect the run does not hold, or has already resolved, is refused by the run's own fence as well as by its deduplication log. Slice 1.7 retrofits the safety classes, the durable `Started` with its lease and fence, and the `Indeterminate` → reconciliation path onto this record.
  - `AgentModelTurn`: the Rakka-owned, versioned, bounded record of one model turn, and the only durable format for what a model produced — no provider type and no adapter's internal runner state ever reaches durable state. It carries assistant text, tool-call *requests* (a request, never an authorization: model output cannot widen what a run may do), an optional typed result proposal, and usage. It arrives the way every effect result arrives, as a durable command the dispatcher returns through the inbox, which is why the loop can be driven end to end before slice 1.6's adapter exists to produce one.
  - `AgentRunBudget`: the run's own durable ledger — remaining loop iterations, model calls, tool calls, tokens, cost, and deadline — allocated from the task definition's ceilings at acceptance and charged by the run's own single-entity transitions, never by reading a parent scope at dispatch time. A model effect is charged when it is persisted, not when its result returns, because an effect that reaches durable dispatch has been paid for whether or not its outcome is ever known. Exhaustion is a structured stop (`AgentBudgetExhaustion` carries the dimension, the limit, and the consumed value), not a bare failure code, so slice 1.9's top-up exchange is a pure addition.
  - Bounded state however long a run iterates: a turn's content leaves the record when the turn is recorded (slice 1.11 appends it to session memory at exactly that point), resolved effects leave with it, and acceptance reserves `AGENT_RUN_STATE_GROWTH_RESERVE_BYTES` of growth headroom so a run the entity admits can never be unable to record the turn it was created to take.
  - Terminal fencing and quiesce, enforced on every path (specification 8.7). A failed effect winds the run down the way a requested cancellation does: it fences further dispatch but stays non-terminal while sibling effects are outstanding — their results are recorded, never refused as `run-terminal` — and it never overwrites the cancellation reason an operator already recorded. A result proposal awaiting the task's decision counts as outstanding work too, so a cancelled run quiesces until the decision settles instead of being resurrected by a late rejection or left holding an accepted result under a cancelled status; acceptance wins that race, because the task durably completed on the result. A decision this binary cannot decode is refused *before* the exchange settles (`AgentExchangeParticipant::check_settle`) and stays outstanding for re-drive — a rolling upgrade no longer terminally fails runs whose tasks hold valid decisions. A replayed command whose transition already committed re-drives the settle pass it still owes before answering `Duplicate`, so the documented retry contract can unstick a run lost between committing and dispatching. An incoming model turn's schema version is checked against the run's policy where it enters, never after it is committed where recovery would fail closed forever. Wall-clock budget exhaustion reports the configured ceiling and the elapsed time, like every other dimension, and bounded run details truncate at a char boundary rather than panicking on multi-byte UTF-8. The stale-generation refusal code both entities share is the exported `AGENT_TASK_REFUSAL_STALE_GENERATION` constant.
  - `rakka_agent::testkit` gained `ScriptedDispatcher`, which plays the dispatcher's role rather than shortcutting it — it reads the effects the run committed, does the scripted work, and returns each outcome as a durable result command, memoized on the effect id so re-invoking an effect whose result was lost returns the same answer; `InProcessRunEntityTransport`; `CrashingStateStore` and `CrashPoint`, which kill an entity's owner at an exact durable write on either side of the compare-and-set; and `DeferredExchangeRouter`, the in-process late binding for two entity classes that exchange with each other.
- `rakka-agent` model adapter, deterministic test adapter, and Rig feature:
  - `AgentModelAdapter`: the Rakka-owned, provider-neutral model contract. It converts an `AgentModelRequest` — an immutable context-snapshot reference, the model profile and sampling a settings revision selected, and the turn number — into a bounded `AgentModelTurn`, and turns a provider response into that same bounded turn. The durable loop, the effect model, and the testkit depend on this trait and nothing else; a provider client, stream, open request, or credential value is never durable state, and the versioned turn is the only durable format for what a model produced. The context snapshot stays an opaque versioned reference until slice 1.11 gives it `MemoryContextSnapshot` content.
  - `AgentModelRetryPolicy`: the explicit, deployment-chosen policy that governs whether a model call is retried after an ambiguous worker loss — there is no implicit retry, because every attempt is billed. It carries the effect safety class (default `ReadOnly`) and a bounded attempt count, is declared by the adapter, and is validated where a policy enters — the adapters' fallible `with_retry_policy` builders, `read_only`, and deserialization all refuse a policy that would auto-retry a non-idempotent call after ambiguity or permit zero attempts (specification 11.4); slice 1.7's effect machine additionally enforces it at dispatch.
  - `DeterministicModelAdapter`: the deterministic test adapter of specification 10.4, implementing the trait without the `rig` feature. It scripts turns in order or conditioned on the turn number, produces them synchronously at the future's first poll — a call built and dropped unpolled consumes nothing, exactly like a provider call never made — and exercises the *same* durable effect path a provider does rather than invoking the loop around persistence. `ScriptedDispatcher` now produces its model turns through an `AgentModelAdapter` and is generic over it, so a scripted turn travels the exact path a real one travels; an adapter error — a provider failure, or a turn that cannot be bounded — surfaces as a failed effect, and answers are memoized on the effect id *and its dispatch generation*, so a re-invocation replays the recorded answer while a later generation (a genuine retry) reaches the adapter again.
  - The `rig` feature: `RigModelAdapter` wraps any Rig `CompletionModel` and implements the Rakka-owned trait, mapping a provider `CompletionResponse` onto a bounded turn — assistant text to text (separate blocks newline-separated), a call to the result tool (`submit_result` by default) to the typed result proposal, and any other tool call to a tool-call request. The result tool is declared on every completion request the adapter builds, so a real provider can actually call it; resolved `top_p` sampling is forwarded through Rig's provider parameters; billed tokens are recovered from the provider's reported total so cached and reasoning tokens are charged exactly once; a response proposing two results is refused as ambiguous rather than silently keeping the last; colliding provider call ids (rig's Gemini conversion reuses the function name) are disambiguated so every tool result stays correlated to its request; and a response carrying only content the durable turn does not record (reasoning, images) fails the mapping instead of committing an empty turn that burns billed iterations. `rig-core` is pinned exactly (`=0.37.0`) with default features off, and the pin and its compatibility review are properties of the feature: no Rig type appears in the non-`rig` public API or in persisted state, and a Rig upgrade never changes the adapter trait, the core domain types, or the persisted turn. `ScriptedCompletionModel` is a deterministic stub provider so the Rig adapter can be driven without a network, credentials, or a provider account; `returning_result_as` scripts a renamed result tool.
  - One scripted model turn runs end to end through the durable effect path under `--no-default-features`, and the same test passes under the `rig` feature against the stub provider; the adapter is the only thing that differs, and both converge on the same completed run.
- `rakka-agent` effect model and dispatcher integration:
  - The full durable effect intent on `AgentRunEffect` (specification 11.1): identity and generation, the full `AgentEffectSafety` record — `ReadOnly`, `Idempotent { external_key }`, `Reconcileable { protocol }`, `NonIdempotent` — the canonical argument digest, the settings revision, the per-attempt timeout, and the logical credential *binding*, never a resolved value. `AgentEffectSpec` is the class-level declaration a registration supplies, validated wherever it enters: a spec that would auto-retry a non-idempotent effect, permit zero attempts, or claim reconcilability without naming its protocol can neither be constructed nor loaded. `AgentEffectPolicies` is the interim registration surface on the run entity (deployment configuration keyed by tool; slice 1.8's registry replaces the map without changing the record), and its defaults fail safe: a model call is one read-only attempt, and an unclassified tool is non-idempotent. The external idempotency key derives from the effect identity *and generation*, so every retry of one generation hands the target the same key and a newly authorized invocation presents a fresh one.
  - The effect status machine with generations (specification 11.3). The run-side record owns `Pending`, `Ready`, and the five terminal outcomes (`Succeeded`, `Failed`, `Exhausted`, `Indeterminate`, `Cancelled`); the dispatch layer's durable records own `Started` (outbox `Dispatching` plus the fleet lease and fencing token) and `RetryScheduled`. `Pending` is load-bearing: an effect is handed to the sink only after the transition that marked it `Ready` committed, so a `Pending` effect has provably never reached the outbox and a cancellation may fence it in place — in the same compare-and-set as the cancellation's acceptance — with nothing abandoned. `Indeterminate` is terminal for one generation but blocks the run's settlement: the run parks in `WaitingForReconciliation`, automatic dispatch eligibility for the effect is revoked at both durable layers, and only the explicit `AgentEffectResolution` decision — `ConfirmedExecuted` with the established outcome, or `ConfirmedNotExecuted`, never a generic retry — resolves it. `ConfirmedNotExecuted` mints a *new* effect generation: a fresh dispatch ticket, attempt budget, and external key under the same identity, so the ambiguous original is never mutated back into a routine attempt, and a late result from a superseded generation is refused by the run's own fence (`run-stale-effect-generation`).
  - `AgentRunEffectDispatcher` in `rakka_agent::dispatch`: the durable dispatch pipeline over the run's agent-workflow outbox and the dispatcher fleet. Each generation's dispatch ticket is an outbox row deduplicated on the generation-qualified ticket id with its retry budget aligned to the intent's attempt bound; a worker claims the fleet lease and writes the outbox's durable `Started` before any external call; credentials are resolved only inside the bounded attempt through `AgentEffectCredentialResolver` (reusing the substrate's redacted `AgentEphemeralCredential`) and dropped with it; and every result command carries effect id, generation, attempt, and the claim's fencing token (specification 11.4). Crash and timeout recovery reads the outbox row as the ambiguity marker and applies the safety class (specification 11.5): a row never marked `Dispatching` redispatches safely for every class; an ambiguous read-only or idempotent attempt retries under the intent's bounded budget (the idempotent retry reusing its external key); an ambiguous reconcileable attempt queries its `AgentEffectReconciler` protocol before any retry and retries only when proven absent; and an ambiguous non-idempotent attempt produces exactly one durable `Indeterminate` outcome and is never automatically re-invoked. A stale ticket — one whose run-side intent was resolved, superseded, or fenced — settles as cancelled without invoking anything.
  - Cancellation fencing at the effect layer (specification 8.7). Accepting a cancellation fences provably-unsent effects in the same transition and stops the flush; the pipeline cancels tickets that provably never started (planting a tombstone row where a laggard flush might still write one), lets attempts already at the dispatch layer settle truthfully, and treats an ambiguous consequential attempt under cancellation exactly as above — so a cancelled run's ambiguous non-idempotent effect keeps the run nonterminal in `WaitingForReconciliation`, and terminal cancellation is projected only after the outcome is explicitly resolved (scenario 57's effect half). Failed-effect wind-downs fence their unsent siblings the same way.
  - Recovery scenarios 5 through 9 are proven over the real pipeline with dispatcher loss injected at every durable boundary — before `Started`, after `Started`, after the external commit, and after result delivery — via `rakka_agent::testkit`'s new `KillSwitchProbe`, `RecordingToolExecutor` (whose invocation log is the external system: commit counts and the idempotency keys the target saw), `ScriptedReconciler`, `ScriptedCredentialResolver`, `InProcessRunResultDelivery`, and `SharedAtomicWorkflowClock` for deliberate lease expiry. A first rough per-turn measurement over the integrated path (one clean model turn: 8 run-store compare-and-sets, 3 workflow-store writes, ~6ms wall on in-memory stores in a debug build) is recorded by `per_turn_durable_write_count_and_latency_measurement`; slice 1.14 formalizes the budget.
- `rakka-agent` tool registry, tool authority, and guardrail baseline:
  - `AgentToolRegistry` in `rakka_agent::tools` (specification 11.7): the deployment's registry of dispatchable tools, each an `AgentToolBinding` over a bounded, model-visible `AgentToolDescriptor` — stable name, monotonic descriptor version, kind (`Function`, `Workflow`, `Process`, `RemoteMcp`, `Retrieval`, `Environment`, `AgentCoordination`), bounded description, input/output schema references, optional inline parameter schema, and declared result behavior. The binding carries the trusted deployment authority: safety class, scoped capabilities, credential binding class, execution-policy routing, attempt bound, reconciliation protocol, per-attempt timeout, required guardrail stages, and checkpoint requirement. A tool registered without a declaration is *unclassified* and fails safe as one non-idempotent attempt. `AgentToolRegistry::effect_policies` projects the bindings onto the run's commit-time `AgentEffectPolicies`, so the registry is the single source the loop stamps intents from and the dispatcher revalidates against; `model_visible` computes the descriptors a model may be shown (registered, declared by the envelope, not revoked) — visibility that carries no dispatch authority.
  - The four authority layers of specification 11.8, each only able to narrow the one above it: descriptor (model-visible), binding (deployment-authorized), `AgentRunEffect` (the exact durable intent, which now also persists its application-owned `AgentExecutionPolicyRef` and echoes it on the dispatch ticket), and `AgentDispatchGrant` — the current authorization to execute one exact intent, binding tenant/goal/task/agent/run, effect id and generation, descriptor version and schema digest, target and canonical argument digest, safety class, the definition/settings/setup/guardrail revisions it validated, capabilities, credential binding, execution policy, expiry, and an allowed use count aligned to the intent's attempt bound. Model output structurally cannot widen anything: a tool-call request carries only a call id, a tool name, and arguments, and everything else resolves from durable definition, setup, and deployment data.
  - `AgentDispatchAuthority` is a *required* collaborator of `AgentRunEffectDispatcher`: every attempt is authorized before durable `Started`, and the grant is revalidated against the exact intent — there is no permissive default, because a pipeline that could skip the check would be the universally privileged worker specification 16 forbids. `AgentEntityAuthority` backs the gate with the agent entity's durable state, so a suspension or an immediate-safety settings revocation (a revoked tool or credential binding) accepted by the entity is honored by the very next dispatch attempt with the agent fully passivated, and the slice 1.2 setup/settings envelope is enforced at dispatch: an unregistered tool, an undeclared tool, a tool the run's setup narrowed away, a downgraded safety class, a mismatched credential or execution policy, a missing checkpoint grant, and an unroutable execution policy all leave the call undispatchable with a stable reason code (scenarios 44 and 54). The intent-layer revalidation also holds the reconciliation protocol and per-attempt timeout to the binding's declaration, and the settings' guardrail-policy selection — the third immediate-safety field — refuses dispatch (`guardrail-policy-mismatch`) unless the deployed chain provably implements it. A transient refusal — a suspension — spends nothing: the ticket is deferred at the fleet with the outbox row untouched, so no attempt burns, no `Failed` row can be misread by recovery as a possibly-executed reconcileable attempt, and a resumed agent's next attempt rechecks and proceeds even under the fail-safe single-attempt defaults. A definitive refusal settles the generation as failed with nothing invoked — unless a prior attempt of the generation may already have executed (the recovery retry of an ambiguous idempotent loss), in which case the generation parks `Indeterminate` under the refusal's code, preserving the ambiguity an operator must resolve. Because runs do not yet carry their setup reference, the gate resolves setups per claimed run through `AgentRunSetupResolver` — never a fixed per-worker setup a fleet-wide claim batch would misapply to other runs. The authority also resolves the turn-bound settings of specification 7.2 at dispatch: the granted model call carries the profile and sampling the *current* settings revision selects, validated against the definition and setup envelopes. Grants are minted and revalidated per attempt, valid through their expiry instant; the TTL bounds a grant a holder retains, which no shipped path does before slice 1.10's durable checkpoint grants.
  - The guardrail baseline in `rakka_agent::guardrails` (specification 16): versioned, ordered `AgentGuardrailChain` stages with the bounded outcome set — `Allow`, `Block`, `Transform`, `ReportOnly`, `RequireCheckpoint` — stable reason codes, and protected evidence references. Stages declare the boundaries they run at, and presence in the chain is never coverage: a boundary-less stage is refused at registration (`guardrail-stage-unbound`), and a required stage bound only to boundaries the caller does not evaluate refuses dispatch (`guardrail-stage-unevaluated`) rather than satisfying an envelope's mandatory set while never running. This slice evaluates the tool-request and model-request boundaries before every attempt's durable `Started` (`AGENT_EVALUATED_GUARDRAIL_BOUNDARIES`), and the remaining declared boundaries — model/tool response, A2A ingress/egress, memory ingress — gain their evaluation points, and with them their coverage, from the slices that own those flows. A stage is evaluated against an `AgentGuardrailContext` naming the boundary, the run scope, and — at the tool boundaries — the tool being called, so per-tool and per-tenant policy is expressible; only the content a transform may rewrite is passed as content, so identity is readable but structurally unrewritable. Evaluation is a pure synchronous fold, so a stage physically cannot introduce I/O into a durable transition: a block short-circuits and wins, carrying its evidence reference into the refusal detail; transforms compose deterministically and are recorded with their reasons (the tool boundary bounds transform content to what a call can actually carry, so an oversized transform is one deterministic block wherever it is caught); report-only findings accumulate and, like applied transforms, surface on the dispatcher's trace; and a checkpoint requirement fails closed until the slice 1.10 checkpoint runtime can satisfy it (a transform at the still-synthesized model-request boundary also fails closed, `guardrail-transform-unsupported`, rather than being silently ignored). A deployment-mandatory stage cannot be removed by a definition, setup, or settings update — `narrowed` refuses the removal, and a narrowed chain must carry its own revision, because a different stage set is a different evaluation — and an envelope that requires a stage the deployment's chain cannot run refuses dispatch (`guardrail-stage-missing`) rather than running without the guardrail the definition promised. A guardrail-transformed tool call executes the transformed arguments while the intent's argument digest stays bound to what the run committed, and the transform is provably identical on every attempt of the generation: each committed intent pins the chain revision of the policies it was stamped from (`AgentToolAuthority::effect_policies`), and the pipeline refuses an attempt (`guardrail-revision-mismatch`) whenever a transform would decide the payload — or any retry follows a possibly-delivered attempt — under a chain that no longer matches the pin, so one external idempotency key can never carry two different payloads.
- `rakka-agent` hierarchical escrow budget ledger (specification 9.7): `AgentEscrowLedger` is a component of the owning entity's own durable state, so an allocation, a settlement, and a return commit in the same compare-and-set as the transition that decided them — one writer, no distributed transaction, oversubscription structurally impossible. The dimensions split in two: *conserved* quantities (iterations, model calls, tool calls, effects, effect attempts, tokens, cost) are escrowed — debited from a parent, held by a child, returned unused — while wall-clock and concurrency are inherited limits rather than quantities and are narrowed down the hierarchy rather than debited. A task holds the escrow for its runs (`AgentTask::escrow`) and debits every run it assigns inside the assignment transition, carrying the `AgentBudgetGrant` on the assignment command; a run whose escrow the task cannot afford is refused (`task-budget-unavailable`) and stays assignable, so an earlier generation's settlement can restore the headroom. Every effect a run commits is reserved from its own ledger before dispatch, folded into the same all-or-nothing per-turn charge (`reserve_model_turn`, `reserve_tool_turn`): one durable effect and its whole attempt bound, plus a concurrency-ceiling check, so a run cannot start work it could not afford to finish retrying, and a fan-out wider than the concurrency ceiling never commits. When a generation resolves, `settle_effect` consumes the attempts that actually reached `Started` — a `Started` attempt that becomes `Indeterminate` still consumes its attempt — and releases the retries the generation did not use (scenario 52). A terminal run hands its escrow back in two ordered exchanges — settle what it consumed, then return what it did not — sequenced by its durable `AgentRunSettlementStatus` so a loss between them re-drives the same operation. Settlement precedes return, and the ledger refuses a return for a child that has not settled: releasing the whole allocation before knowing what was spent would hand back budget already burned. Each command is deduplicated on the child's escrow record rather than the exchange journal's bounded ring, so a replay that outlived the window still credits the parent exactly once, and over-consumption (a final turn billing past its allocation) is recorded and saturates the parent's headroom at zero rather than being rejected — the next charge is where a spent budget refuses (scenario 61, `tests/escrow_ledger.rs`). A run that exhausts a ceiling does not fail on the spot: it parks with the structured exhaustion (`AgentPendingTopUp`, status stays `Running` because a pending exchange is the run's own outbox, not a residency wait) and asks its parent for more through a deduplicated `BudgetAllocation` exchange, committed to the run's journal by the transition that hit the ceiling and drained by the same courier. The parent's grant is an ordinary parent-local decision under its own ceilings; a grant that adds room in the exhausted dimension resumes the run exactly where it parked — the failing charge is all-or-nothing (`reserve_model_turn`, `reserve_tool_turn`), so re-attempting double-counts nothing — while a grant of nothing (the honest answer when the parent has nothing left) stops the run with its *original* exhaustion rather than parking forever, and because each grant strictly reduces the parent's headroom the asking always terminates. Only conserved dimensions park to ask: a wall-clock deadline or a concurrency ceiling is not a quantity a parent can grant, so a run that crosses one stops with the structured reason rather than re-parking on a ceiling that would never move. Enforcement is proven end to end — a real dispatched effect debits `effects` and `effect_attempts`, an unaffordable turn is denied, an over-deadline run stops, concurrent allocations cannot oversubscribe a parent, the whole round trip (allocation, reservation, settlement, return) converges under owner-crash injection at every durable write on both sides of the compare-and-set without double-debiting or double-crediting, and an unattended (`BoundedAsync`) task is refused without an admission and runs once admitted (`tests/escrow_ledger.rs`, `tests/autonomy_admission.rs`; scenarios 52, 53, 61).
- `rakka-agent` inter-entity choreography: an entity's `accept` of a delivered exchange now makes *local* progress only — it cranks its own loop and dispatches effects, or decides an assignment freed budget permits and flushes owed history — and commits any onward cross-entity exchange to its durable journal without delivering it. Draining that journal is the courier's job (a command's settle pass, a recovery sweep, a fault-injection pump), never a synchronous step inside a delivery. This is the durable-outbox discipline made uniform, and it is what keeps the choreography acyclic: the initiator of the exchange being accepted is mid-delivery to this entity, so driving an owed exchange back to it from inside `accept` would re-enter a transition whose reply has not settled — a terminal run owing its task a settlement, and the task re-driving that run's still-outstanding assignment, would otherwise recurse without bound. Every cross-entity outcome still converges through the bounded-iterative courier exactly as before.
- `rakka-agent` autonomy admission (specification 7.4): `AutonomyAdmissionDecision` is the immutable, fail-closed record that decides whether an unattended class (`BoundedAsync`, `Continuous`) may run at all — it carries its operation classes, the definition/settings/setup revisions it evaluated, the evaluator principal or service, the requirements it verified, bounded constraints, and an optional expiry, and never a resolved credential. An authorized evaluator authors it outside Rakka and submits it as an ordinary deduplicated `Admit` command; the entity is not a rubber stamp — it verifies every requirement the definition itself answers (`verify`) and refuses a decision naming revisions the agent is no longer on (`stale-admission-revision`). An unattended class that does not name every requirement is not a partial admission, it is not an admission; `Retract` returns the agent to the fail-closed default (`admission-missing`), indistinguishable from never-admitted at every enforcement point. Widening is *derived, never tracked*: assignment re-derives the decision against the definition now in force (`admits_definition`) rather than trusting a recorded answer, so a definition published since the admission that widened its authority envelope stops assignment (`admission-widened`) without anything having had to notice the update. The derivation is *both* the envelope narrowing **and** the structural requirements `verify` checks: a republish that dropped a required approval, authorization, or escalation policy — a policy reference is not part of the authority envelope, so it is not an envelope widening — is refused (`admission-requirement-regressed`) even though the envelope did not widen, closing a fail-open where a stale admission could otherwise admit unattended work whose verified guarantees the current definition no longer provides. Because enforcement derives from the current definition, publishing a new definition deliberately does not retract the admission: fail-closed is a property of the derivation, not of a hook every future call site must remember to fire.
- `rakka-agent` durable checkpoints and HITL — the checkpoint substrate and its digest-bound grant (specification 12; scenarios 3, 11, 12, and the reconciliation half of 57):
  - `AgentCheckpoint` in `rakka_agent::checkpoints` is the durable, schema-versioned checkpoint record of specification 12.2, in the three kinds `Approval`, `SecurityAuthorization`, and `IndeterminateEffectReconciliation`. It binds the exact effect intent (id, generation, target, argument digest, safety class, logical credential binding) and carries the tenant/goal/task/agent/run/delegation identity, bounded non-secret prompt/decision summary, allowed decisions, required roles/capabilities and policy reference, settings/definition revisions, creation/SLA/expiration/escalation timestamps and target, immutable context/evidence and audit references, and creator/resolver principals — never resolved credential material. It is plain durable state that round-trips through serialization, so a run passivates behind an open checkpoint and resolves it on the next command with no live execution task.
  - Grant binding is cryptographic and exact (specification 12.3). `AgentContentDigest` gained a `Sha256` algorithm — a pure-safe-Rust FIPS 180-4 implementation, no new dependency, pinned to the standard vectors — because the FNV fingerprint the effect carries is explicitly not a security boundary. An `Approve`/authorization resolution issues an `AgentCheckpointGrant` bound to the SHA-256 digest of the effect's canonical arguments, plus scope, effect id and generation, target, safety class, settings revision, logical credential binding, resolver, expiry, and allowed use count. `AgentCheckpointGrant::validate_for` recomputes the cryptographic digest from the intent and fails closed on a changed argument (`checkpoint-argument-digest-mismatch`), a superseded generation or target (`checkpoint-grant-intent-mismatch`), a changed credential binding, a spent use count, an elapsed expiry, or a non-cryptographic digest — so a changed effect digest invalidates a stale approval (scenario 12).
  - The two authority checkpoint gates that previously failed closed unconditionally — the `checkpoint_required` binding and a guardrail `CheckpointRequired` disposition — now accept a valid digest-bound grant carried on `AgentAuthorityContext::with_checkpoint_grant`, revalidated against the exact intent and the actual 1-based attempt number before dispatch (`AgentDispatchAuthority::authorize` and `AgentToolAuthority::authorize` receive the attempt the pipeline is about to make, so a grant's allowed use count bounds retries: a spent grant does not cover the next attempt), and still fail closed with `checkpoint-required` (or the specific grant-invalidation reason) when no grant binds. Because revocation is an immediate-safety settings check that runs before the checkpoint gate, an immediate tool or credential revocation refuses the next dispatch even under a still-valid approval (scenario 13). The `AgentEntityAuthority` gate sources the grant the run holds and passes it through the same revalidation (see the run-side wave below).
  - Run-side park/resume and durable SLA timers. A tool the deployment marks checkpoint-required carries that requirement on its projected `AgentEffectSpec` and durable `AgentRunEffect`, so when the loop would schedule it the run opens an `Approval` `AgentCheckpoint` bound to the exact effect and parks in `WaitingForApproval` — the checkpoint and the effect commit in one transition, and `dispatch_effects` never hands a gated effect to the sink without a grant. The run passivates behind the wait with no live task and resumes only on a durable command (scenario 3). A new `AgentRunEntityCommand::ResolveCheckpoint`, deduplicated on its operation id, applies an authenticated decision: an `Approve`/`Grant` records the digest-bound `AgentCheckpointGrant` on the run's loop state and resumes it, so the next dispatch pass finds the grant, the real `AgentEntityAuthority` revalidates it against the exact intent, and the tool executes; a duplicate submission returns the original outcome and never resumes the run twice (scenario 11); a `Deny` fails the gated effect's generation (`checkpoint-denied`). `WaitingForApproval`/`WaitingForAuthorization` now fence the loop crank in `permits_progress`, and a resolved grant leaves with its effect. `AgentRunEntityCommand::FireCheckpointTimers` applies the durable SLA and expiration deadlines a deployment configures through `AgentCheckpointSla`: an overdue checkpoint escalates and stays open, an expired one is denied (`checkpoint-expired`) and the run winds down — a timeout never auto-approves sensitive or non-idempotent work ([specification 12.6](docs/plans/rakka-agent/spec.md)). Covered by `tests/checkpoint_run.rs` and the end-to-end grant test in `tests/tool_authority.rs`. A cryptographic argument digest is added to `AgentDigestAlgorithm::Sha256` for the grant binding (see above); the guardrail-driven checkpoint requirement discovered dynamically at dispatch remains a dispatch-time concern rather than a commit-time park.
  - The reconciliation decision set is exactly specification 12.5 — `ConfirmedCompleted`, `ConfirmedNotExecuted`, `Compensate`, `Escalate`, `AbandonAndFail` — with no plain `Retry`: `ConfirmedCompleted`/`ConfirmedNotExecuted` map to the effect layer's `AgentEffectResolution` (only `ConfirmedNotExecuted` authorizes a new generation), `Compensate` schedules an explicitly defined compensation (`AgentCompensationRef`), `AbandonAndFail` fails the effect's generation terminally, and `Escalate` keeps the checkpoint nonterminal so a later decision converges it — the reconciliation half of scenario 57.
  - Every resolution is deduplicated on its decision key: a replayed decision returns the original outcome and makes no second transition, and a different decision against a resolved checkpoint is refused (`checkpoint-already-resolved`), so a duplicate human or authorization decision never resumes a run twice (scenario 11). `AgentCheckpoint::on_timer` drives SLA escalation and hard expiration from durable timestamps and can only escalate or expire a waiting checkpoint — it can never grant or approve, so a timeout on sensitive or non-idempotent work fails closed (specification 12.6). Covered by `tests/checkpoints.rs` and the checkpoint-gate tests in `rakka_agent::tools`.
  - The `SecurityAuthorization` runtime path (specification 12.4). A tool binding may declare `with_authorization_required`, projected through `AgentEffectSpec` onto the durable `AgentRunEffect`, so the run opens a `SecurityAuthorization` checkpoint bound to the exact effect and parks in `WaitingForAuthorization` — the same durable wait, dedup, timer, and passivation substrate as an approval, with the resolution supplying the capability or logical credential binding the effect needs (a reference only, never secret material). The dispatch authority enforces the *kind*: an authorization-required tool refuses without a grant (`authorization-required`) and refuses an approval-kind grant — a human approval can never stand in for a security authorization — so the authorization sub-case of scenario 3 and the authorization flavor of scenario 11 are now demonstrable (`tests/checkpoint_run.rs`, the kind-gate test in `rakka_agent::tools`).
  - Reconciliation waits now run through the checkpoint record. An `Indeterminate` outcome opens an `IndeterminateEffectReconciliation` checkpoint in the same transition that recorded the ambiguity, so the wait carries the full specification 12.2 surface — decision set, dedup key, SLA escalation, roles — and the whole 12.5 decision set reaches the run through `ResolveCheckpoint`: the confirming decisions flow through the shared effect-resolution core (with `ConfirmedNotExecuted` still minting exactly one new generation under a fresh attempt reservation — and re-parking a checkpoint- or authorization-gated effect's new generation behind a fresh checkpoint of the matching kind, since the grant that authorized the ambiguous generation binds that generation exactly), `Escalate` keeps the wait, and `AbandonAndFail` fails the generation (`reconciliation-abandoned`) and winds the run down under whatever wind-down reason was already on record. A reconciliation checkpoint never hard-expires — expiry would fabricate a definitive failure for an effect whose outcome is unknown — and a run cancellation cancels only approval-family checkpoints, so the parked ambiguity stays resolvable and terminal cancellation still projects only after its decision (scenario 57). The effect-layer `ResolveIndeterminateEffect` command remains valid and retires the checkpoint it bypasses.
  - `Compensate` now schedules a real durable compensation effect. The ambiguous generation settles as `AgentRunEffectStatus::Compensated`, the run winds down under a new `AgentRunTerminalReason::EffectCompensated`, and a new `AgentRunEffectRequest::Compensation` effect — carrying the `AgentCompensationRef` and the compensated generation, reserved against the run's budget like any re-authorized attempt — is committed *after* the wind-down fence and is the one kind of effect a winding-down run still flushes to the sink; the run reaches its terminal status only after the compensation's own outcome settles. Rakka routes the reference; the application owns the execution behind it through the new `AgentCompensationExecutor` collaborator on `AgentRunEffectDispatcher` (`with_compensation_executor`; absent, a compensation dispatch fails closed with `compensation-executor-missing`), authorized at the gate without a tool binding — the operator's decision is its authorization — but still subject to credential-revocation and execution-policy checks. Covered end to end by `tests/checkpoint_reconciliation.rs`.
- `rakka-agent` short-term session memory and immutable context snapshots (specification 13.1, 13.2, 13.5, and the short-term clauses of 13.6; scenarios 14, 16, and 17):
  - `SessionMemoryStore` in `rakka_agent::memory`, scoped `(TenantId, AgentId, AgentRunId)`: a bounded, ordered short-term memory whose append is idempotent on a derived `MemoryOperationId`, so a replay returns the original logical result without a second entry. A `SessionMemoryEntry` carries its stable `MemoryEntryId` and operation id, a monotonic `MemorySequence`, its `MemoryEntryRole`, bounded inline content or an immutable artifact reference, a content digest that survives redaction, its turn and source provenance, and `MemoryClassification`/redaction metadata — never resolved credential or secret material. Reads and writes under one run are never visible through another run's session API, including another run of the same agent (scenario 14). `InMemorySessionMemoryStore` is the in-process implementation.
  - `MemoryContextSnapshot` and `ContextSnapshotStore` (specification 13.5): the immutable, content-addressed context a model effect is prepared against — the exact bounded session entries used, retrieval queries and retriever versions, prompt/context budget accounting, the policy revision in force, and a content digest, with the private and communal selections fixed in shape but empty until phase 2. All assembled context is `MemoryTrust::Untrusted`: it is contextual model input that a guardrail chain evaluates, never something that can replace a system instruction or widen a tool capability. `ContextSnapshotStore::persist` is first-writer-wins, so a re-assembly of the same reference returns the original snapshot; `assemble_session_context` reads a bounded recent window shaped by a `SessionWindowPolicy` — the Rakka-owned write path a Rig memory policy shapes *behind*, never bypassing the durable, deduplicated append boundary (specification 10.3). `InMemoryContextSnapshotStore` is the in-process implementation.
  - The `AgentPrivateMemoryStore` trait scoped `(TenantId, AgentId)` is *declared* (with its `AgentPrivateMemory` record and `AgentPrivateMemoryKind`) but not implemented, so the session and snapshot identities cannot bake in an incompatible scope before phase 2 delivers the private and communal stores.
  - Run-entity wiring, through the optional `AgentRunEntityStore::with_memory(AgentRunMemory)` collaborator: a wired run assembles and persists the immutable snapshot for a model effect before the effect is dispatched, and reuses it on every re-driven settle — a dispatcher retry or a recovery finds the snapshot already stored and never re-assembles it from newer memory, so a concurrent memory write cannot silently change a retried model input (scenario 17). The first turn's preparation records the task's bounded input as the session's opening `User` entry — in the same compare-and-set that commits the first model effect, and always within bounds, since task content and session entries share one inline limit — so the first model turn's snapshot carries what the run was created to serve. At `AgentLoopPhase::RecordingTurn` the run records the turn and its tool results into the same bounded session-memory outbox in its own loop state, and the settle pass flushes them to the store idempotently on their operation id — the same durable-outbox discipline the task history keeps, so an interrupted flush after a restart re-drives without duplicating an entry (scenario 16) and the loop keeps no turn content of its own. An unwired run keeps only the opaque context reference and retains no session memory — the pre-slice-1.11 behavior — so existing runs are unchanged. `AgentRunProgress` gained `session_entries_flushed` and `snapshots_persisted`. Covered by `crates/rakka-agent/tests/session_memory.rs` and the `memory` unit tests.
- `rakka-agent-postgres`: the PostgreSQL binding for agent short-term session memory (`PostgresSessionMemoryStore`) and immutable context snapshots (`PostgresContextSnapshotStore`), scoped and idempotent exactly as the in-memory stores. Uniqueness constraints make replay harmless — a session append deduplicates on `(tenant, agent, run, operation_id)` and fails closed when a different operation claims a taken sequence; a snapshot persist is first-writer-wins on `(tenant, agent, run, snapshot_id)`. Schema is applied by an idempotent, advisory-locked `migrate`, loads fail closed on an unsupported schema version, and the deploying application supplies its own `tokio_postgres::Client` with its own TLS and credentials. Gated tests run only when `RAKKA_POSTGRES_TEST_DSN` is set (`cargo test -p rakka-agent-postgres`), proving scenarios 14, 16, and 17 against a live database.
- `rakka-a2a`: a reusable A2A protocol adapter crate for durable Rakka agent-workflow runs. It provides A2A-to-Rakka command mapping, an async task projection store with in-memory and PostgreSQL implementations, durable public task-event streaming replay with a bounded-interval PostgreSQL event watcher, a builder-based durable A2A SDK `RequestHandler` with tenant-resolver/authorizer/workflow-catalog hooks, a sharded run owner host and cluster owner router, a push notification config store with credential redaction plus a push dispatch boundary, a dynamic agent-card builder, axum route composition, and bounded observability snapshots. Exposed through the gated `rakka` facade features `a2a`, `a2a-server`, `a2a-sharding`, `a2a-postgres`, `a2a-http`, `a2a-k8s`, `a2a-otel`, and `a2a-testkit`. The `clustered-sharded-entity-a2a-agents` example is now a thin product composition over this crate.
- `rakka-a2a` `agents` feature — the typed Rakka Agent A2A surface and typed client (specification 14.1-14.3, 14.5, and the external settings entry of 7.2; scenario 1):
  - `rakka_a2a::agents`: a sibling of the workflow-substrate handler, driving the real `rakka-agent` entities over durable state. Public A2A `Task.id` equals `AgentTaskId` verbatim (resolved open decision 17); a send without a task id derives its identity deterministically from the tenant and the request's deduplication discriminator (the explicit `io.rakka.command.deduplication_key`, or the A2A `message_id`), so a retried send reaches the same entity with the same operation id and the entity's deduplicated inbox answers with the original outcome — one task, one run, one turn, proven end to end over the real task/agent/run entities with the deterministic model adapter (`tests/agents_surface.rs`). The tenant always comes from the authenticated context, never the id; `contextId` is opaque and defaults to the task's own id, with a client-supplied value retained in the projection read model.
  - The full specification 14.3 state projection as a pure function over the authoritative task status and current run condition, tested row-for-row plus a full-cartesian sweep proving `UNSPECIFIED` is never produced: the domain's three-way human-wait split drives `INPUT_REQUIRED`/`AUTH_REQUIRED`, a `Suspended` run projects `WORKING` with bounded suspension metadata, `HandedOff`/`Superseded` never close the public task, and a cancellation request alone never projects `CANCELED` — a parked indeterminate effect holds `INPUT_REQUIRED` with the stable `indeterminate-effect` reason until its reconciliation decision. Accepted commands and lagging reads feed the existing `A2ATaskProjectionStore` event stream, so replayable subscription with cursors, bounded retention, and explicit resync is the existing machinery reused.
  - The versioned agent-management extension (resolved open decision 10): `urn:rakka:a2a-extension:agent-management:v1` carries the version in its URI, commands (`update-settings`, `suspend`, `resume`, `terminate`, `describe`) travel as a tagged data-part envelope into the owning `AgentEntity`'s durable deduplicated inbox, an unsupported version or unauthenticated write fails closed, and the immediate response message — never an A2A task — carries the applied revisions, a duplicate's original outcome, or the structured stale-revision conflict. Authorization gates on the new `A2AOperation::AgentManagementWrite`/`AgentManagementRead`, the authenticated principal becomes the `SettingsRevision` provenance, and the agent-card builder gained an explicit `extensions(...)` declaration setter.
  - `RakkaAgentClient` in `rakka-agent` over the `AgentClientTransport` durable-command port (the crate's boxed-future idiom), with `A2AAgentClientTransport` implementing the port over the agents-surface service — a client call takes the identical normalize → authorize → durable-inbox → settle → project path as a network caller, and `run_task` polls to a terminal state with no retained server-side residency. Input delivery to an existing task and binary/URL message parts refuse with stable reasons until their owning slices land.
- `rakka-agent` trace-context schema retrofit (specification 17.1, 17.5; the slice 1.13 resolutions): the durable boundary records now persist W3C trace context — additively, defaulted, and never fail-closed, because telemetry is observability and never a correctness input. `AgentExchangeEnvelope` (stamped by the initiator, riding every re-drive), `AgentRunEffect` (the scheduling segment's context, forwarded onto the dispatch ticket the substrate outbox already carried a context field for; a reconciled next generation starts parent-less with a `superseded-generation` span link to the attempt an operator proved never executed), `AgentCheckpoint` (the parked span a resolution links back to), and `AgentLoopState` (the last committing segment, stamped onto every effect the loop commits, first-commit-only under replay) each gained one `#[serde(default)]` `AgentTelemetryContext` field with **no schema-version bump**: a pre-retrofit record decodes to the empty context inside the unchanged v1 window and projects a byte-identical dispatch ticket (`tests/telemetry_context.rs`, the schema half of scenario 23). The write side is strict where the read side is permissive: `sanitize_agent_telemetry_context` drops a malformed `traceparent`/`tracestate` pair whole, filters invalid span links individually, caps links at `AGENT_TELEMETRY_MAX_SPAN_LINKS` keeping the newest, and clears baggage unconditionally — M1 persists no baggage, and externally received baggage is untrusted.

- `rakka-agent` structured decision events (specification 17.7, 17.13): a run wired with an `AgentDecisionEventSink` records why its loop did what it did — `AgentDecisionEvent` carries the decision kind (`continue`, `call-tools`, `submit-result`, and the M1 park/terminal kinds), the source (`model`, `deterministic-policy`, `human`, `authorization-service`), the turn, loop phase, revisions, context-snapshot reference, selected tool classes, safety class, a bounded reason code, and the deciding segment's trace context — never model text, tool payloads, or credentials. The event is recorded in the same compare-and-set as the deciding transition into a bounded loop-state outbox and flushed to the sink in the settle pass, so an event exists only for a transition that durably committed, exactly once per derived operation id (a re-driven transition and a re-driven flush both converge). Telemetry is never a correctness input: where the session-memory outbox fails closed at its bound, the decision outbox is a ring that drops its oldest owed event and counts the drop (`decision_drops`), and an unavailable sink leaves events owed for the next pass instead of failing the run. The in-memory sink keeps the durable contract — dedup on operation id, bounded per-run retention, and an explicit `ReplayWindowExpired` answer for a cursor that predates the window — and the decision record is the twentieth `AgentRecordKind`, versioned and fail-closed like every other. An unwired run records nothing (`tests/decision_events.rs`).

- `rakka-agent` bounded `rakka.agent.*` metrics (specification 17.12; the slice 1.13 metric-vocabulary resolution): the run store records its own durable transitions through a wired `MetricsRecorder` — `rakka.agent.run.transitions` (by advancing phase), `rakka.agent.effect.outcomes` (by effect kind, safety class, and terminal outcome, `indeterminate` included), `rakka.agent.decisions` (by kind and source, counted on first durable sink acceptance so a re-driven flush counts nothing), `rakka.agent.recovery.events`, the `rakka.agent.decision.drops` gauge over the durable ring-drop counter, and `rakka.agent.telemetry.flush.failures` — while the substrate keeps measuring the substrate under `rakka.agent_workflow.*`, so one physical dispatch is never the same concern in both vocabularies. The substrate's forbidden-field guard is reused unchanged and composed with the closed agent label set (`AGENT_METRIC_FIELDS`); every recorded value comes from an `as_label()` vocabulary, a label set that fails validation records nothing, and metrics never fail the run. An unwired store records nothing (`tests/agent_metrics.rs`, which also walks every emitted observation through the bounded guard).

- `rakka-agent` authoritative operational queries (specification 17.18; scenarios 21 and 56): `agent_operational_snapshot` is the point query answered from the durable run record in one read — no metric, trace, sink, or Collector consulted — returning the durable state revision it derived from, so the answer stays correct while telemetry is sampled, delayed, dropped, or entirely unavailable and while the entity is passivated (status is logical lifecycle, never a residency claim). `AgentOperationalSnapshot` reports the run projection, the bounded wait reason and earliest checkpoint deadline, every effect that blocks settlement (generation, attempts, safety class, gate requirements, grant state — `indeterminate` included), the open checkpoints with their bounded resolver requirements, the durable decision cursor/owed/drops, and `AgentCancellationProgress`: the full six-state vocabulary from M1 (`Propagating` becomes derivable with Phase 4 delegation), derived from what the record proves — an accepted cancellation request with an ambiguous consequential effect reports `WaitingForReconciliation`, never terminal (scenario 57's projection half). `assemble_agent_session_view` is the projection over it: it joins the sink's retained decision events and the trace segments the durable records carry (loop, effect-scheduling, checkpoint-open), and is explicit about its own freshness — `decisions_available` and `decision_lag` against the durable cursor — so an unavailable sink degrades only the projection half, byte-for-byte never the snapshot inside it (`tests/operational_query.rs`).

- `rakka-agent` pinned GenAI convention mapping behind the `otel` feature (specification 17.2, 17.6, 17.17, 17.20), composed additively over the `rakka-agent-workflow` OTLP bridge — which gained the fields a convention-compliant adapter must not silently drop: span kind (`AgentOtelSpanKind`, the schedule/dispatch pair reads as `PRODUCER`/`CONSUMER`), span status, bounded span events, and the batch-level `AgentOtelInstrumentationScope`, all `#[serde(default)]` so a pre-extension bridge record decodes with the pre-extension semantics. `rakka_agent::otel` pins the reviewed semantic-convention revision (`1.36.0`) in a schema URL stamped into every batch's instrumentation scope; an upgrade is an explicit 17.20 compatibility review, never a side effect, and never a durable-state migration. `AgentGenAiOperation` is the M1 span-model table — `invoke_agent {name}`, `{operation} {model}` as `CLIENT`, `execute_tool {tool}`, and the stable `rakka.agent.*` names for decide/schedule/dispatch/authorize/checkpoint-open/resume/recover/admit/reserve/settle — every embedded value a bounded registry class, never an id or argument text. `AgentGenAiIdentity` maps the 17.3 identity table (`gen_ai.agent.id/name/version`, `gen_ai.conversation.id`, restricted `rakka.agent.goal/task/delegation.id`) with values the caller supplies under its tenant's telemetry access policy — pseudonyms where required, the reversible mapping never entering telemetry. `decision_span_event` maps a structured loop decision to a bounded span event (kind, source, turn, phase, revisions, safety class, reason code — no identifier, no content), and `usage_attributes` maps only provider-reported token counts. The crate still builds and tests with `--no-default-features`.

- `rakka-agent` and `rakka-a2a` end-to-end trace-context flow and the M1 telemetry scenario proofs (specification 17.5; scenarios 22-26): the agents surface extracts W3C `traceparent`/`tracestate` from A2A request metadata case-insensitively *before* durable acceptance — malformed context is dropped whole, never refusing the send — and the typed client injects the caller's context under the same keys on egress. The extracted context rides `AgentTaskCreation` into the creating transition, the task's materialized state holds it so the assignment decided in a later transition carries it onward, the choreography host propagates the causing exchange's context onto owed envelopes that carry none (accept and settle both), and the run participant records the accepted exchange's context into its loop state — so one ingress `traceparent` reaches every effect, dispatch ticket, and checkpoint the run commits, with no participant doing per-exchange work. Proven (`tests/trace_scenarios.rs`): the chain survives owner loss with the traced run's durable outcome byte-equal to an untraced run's (scenario 23); a parked checkpoint holds the segment durably and a resume links both the parked span and its trigger (scenario 22); the sampled flag changes no metric, decision event, or durable outcome (scenario 24); no telemetry surface carries model text, tool arguments/results, proposal content, or credential material — the sentinel sweep that proved it also forced a content redaction of the operational snapshot, which now reports `has_pending_proposal` instead of embedding the proposal (scenario 25); and an unavailable sink blocks nothing while its loss stays visible through the bounded flush-failure counter and the snapshot's owed count (scenario 26).

- `rakka-agent` sharded run entities carry the observability wiring (specification 17.7, 17.12): `AgentRunEntityShardingSettings` gained `with_metrics` and `with_decision_events`, and the sharded factory installs both onto every hosted run through one `build_hosted_run_entity` path — so a run materialized on its owner measures its durable transitions and records its decisions exactly as a directly-driven one does, closing the gap where the signals were reachable only through the entity builder in tests. Both default to their no-op wiring (a `NoopMetricsRecorder` and no sink), so an unwired deployment records nothing and opts in by passing its system recorder and sink. Proven end to end over the real sharded actor (`tests/run_sharding_wiring.rs`: a hosted run records `rakka.agent.recovery.events` through the settings recorder, and an unwired one records nothing) and at the factory's per-entity builder (`src/run.rs` unit test).

- `rakka-agent` telemetry hardening (slice 1.13 review follow-up): the agent-domain metric guard now reuses the substrate's *value* bound (length and single-line), not only its key vocabulary, so a bounded key can never smuggle an unbounded or multi-line value — factored into the reusable `rakka_agent_workflow::validate_agent_metric_attribute_value`. The OTLP bridge gained `AgentOtlpBridgeExport::validate`, which the receive path now runs so a batch's optional instrumentation scope is validated (blank name/version rejected) alongside its spans, exporter, and resource. The pinned GenAI convention revision is single-sourced through one literal so the bare revision and its schema URL cannot drift. The authoritative operational snapshot reports only checkpoints the run is genuinely waiting on (a resolved-but-not-dropped one no longer surfaces a stale `next_wake`) and derives its content-redacted run projection from a single durable read.

- `rakka-agent` recovery suite, fault injection, and M1 acceptance (specification 15, 18, and 22; scenarios 4, 19, 35, 46 plus the full M1 regression): every store class the M1 suite drives — run, task, agent, workflow outbox, and dispatcher fleet — is now a crash-armable `CrashingStateStore`, and the new `testkit::sweep_crash_points` harness kills the owner at every durable write on both sides of the compare-and-set, re-proving every previously landed M1 scenario under fault injection (A2A dedup, checkpoint park/decide/resume, reconciliation, session memory, task lifecycle, bounded state, tool authority, admission, decision events, operational queries, and the traced flow). The four owed scenarios landed: a stale owner's write after shard movement is rejected by the revision fence and answered from the authoritative record (`tests/stale_owner_fencing.rs`); a terminal run's recovery is provably writeless and dispatchless, enforced by a permanently armed crash point (`tests/terminal_run_recovery.rs`); goal-active work over all three real sharded entity types passivates to zero resident entities and one durable trigger advances it exactly once (`tests/goal_passivation.rs`, with the M1 goal reading pinned in the test doc); and an idle agent with a blocked future task auto-passivates under the real idle timer with no lifecycle command and reactivates through one deduplicated dependency-outcome trigger (`tests/idle_agent_reactivation.rs`, open decision 20's proof). `testkit::LocalShardedExchangeRoute` is the production sharded route's local arm, so these gates run ungated in sandboxes. `AgentRunEntityShardingSettings` gained `with_memory`, closing the gap where a sharded run — the production driver — could not persist the session context specification 22 requires. The per-turn durable-write budget is pinned as exact assertions (10 run-store, 8 task-store, 3 workflow-outbox compare-and-sets per clean accepted turn) in `tests/effect_dispatch.rs`, and `examples/durable-agent-acceptance` demonstrates all eighteen bullets of the initial acceptance statement end to end — real `ClusterSharding`, the in-process A2A service core, the production dispatcher fleet, one stable stdout line per bullet, with the transcript asserted verbatim by the example's own test and quoted in its README alongside the recorded budget.

- `rakka-agent` agent-private long-term memory (specification 13.1, 13.3; slice 2.1, the first M2 slice — the private halves of scenarios 15, 16, and 18): the interface-only `AgentPrivateMemoryStore` declared by slice 1.11 is now a real subsystem. The record carries the full 13.3 shape — schema version with fail-closed load, the compare-and-set `revision`, structured `AgentPrivateMemorySource` provenance (run/effect/entry, never authority), content-free `MemoryEmbeddingRef` metadata (slice 2.2 fills the vectors), `MemoryRetention` (persistent or expiring), the auditable `MemoryTombstone` stub state, and policy/audit references — reshaped in place at schema version 1 under the unreleased-branch rule, since no released writer ever persisted the earlier shape. Writes resolve open decision 1's M2 half: a create is idempotent on its operation id and a replay answers the *original* result from the store's operation ledger even after later updates, an update is a compare-and-set on an explicit `PrivateMemoryExpectation` (a blind overwrite is unrepresentable), and a stale writer is refused (`memory-revision-conflict`) rather than overwriting a concurrent run's memory (scenario 15). Deletion is final in both directions: tombstone and delete are their own idempotent operations that erase the ledger's earlier payloads for the memory, so a replayed old write fails closed (`memory-operation-erased`) instead of resurrecting withdrawn content, while the tombstone's content-free stub stays visible in scope — digest and provenance intact — because the withdrawal itself is auditable. Reads reveal nothing across scopes: an out-of-scope `get` or `list` is byte-identical to reading a memory that never existed (scenario 18), and an expired memory is invisible from its expiry instant whether or not the bounded, deployment-invoked `purge_expired` sweep has run. Promotion from session memory is an idempotent durable effect, as 13.3 requires: a deduplicated `AgentRunEntityCommand::PromoteMemory` selects a bounded contiguous range of the run's own durably assigned session sequences and commits one `MemoryPromotion` effect in a bounded transition (budget-reserved, fenced by wind-down, validated with stable `run-memory-*` refusal codes); the dispatcher-side `SessionMemoryPromotionExecutor` reads the selection and upserts under purely derived identities — the memory id from (agent scope, source entry, kind), the operation id from (run scope, effect, generation, entry) — so any replay converges on one logical write per entry, a re-promotion of the same entry converges on the existing record instead of duplicating it, and two runs of one agent derive disjoint memories. Consolidation is the same effect naming an `AgentMemoryConsolidationTarget`: a compare-and-set update of exactly one memory from exactly one entry, whose stale expectation settles the generation `Failed` without a write. The run keeps a bounded receipt ring (`AgentLoopState::memory_promotions`, identities and revisions only) — and, deliberately, a failed promotion generation does **not** wind the run down: memory is never the correctness source (13.1), so a memory-store outage records its failure on the effect and the live run continues. The guardrail `MemoryIngress` evaluation point is explicitly deferred to the slice 2.2 retrieval flow the boundary describes — adding it on the promotion path alone would let a required MemoryIngress-only stage satisfy coverage while not running where retrieval happens — which is safe because promoted content is verbatim session content and cannot reach a model context without passing the retrieval-side evaluation when it lands. Open decision 7 is resolved: terminal-run session retention is a per-tenant `SessionRetentionPolicy` (30-day bounded default, legal hold) enforced *inside* the new idempotent `purge_run` on both `SessionMemoryStore` and `ContextSnapshotStore` — snapshots embed session content, so discharging retention purges both — with held and not-yet-due outcomes reported as values so a fleet sweep never aborts, and export remaining the ordinary bounded cursor read. Proven at the store level (`memory` unit tests), end to end through the real run entity including the owner-kill crash sweep over the command → effect → upsert → settle chain (`tests/private_memory_promotion.rs`), and through the production dispatch pipeline with the authority's promotion arm and dispatcher loss mid-pass (`tests/effect_dispatch.rs`).
- `rakka-agent-postgres`: the PostgreSQL binding for agent-private long-term memory (`PostgresAgentPrivateMemoryStore`) and the terminal-run retention purges. Every private-memory write is one data-modifying-CTE statement — a single statement is a single implicit transaction on the shared pipelined client, so the operation-ledger row and the memory-row mutation commit or fail together, with no crash window where an applied operation lacks its ledger row and no raw `BEGIN`/`COMMIT` on a shared connection — and the `WHERE revision = $n` update is the genuinely concurrent compare-and-set: of two racing connections, exactly one wins and the loser surfaces `memory-revision-conflict` (scenario 15, proven over two live connections). The table denormalizes revision/expiry/tombstone flags purely for SQL-side filtering; the authoritative record stays the BYTEA-encoded `AgentPrivateMemory`, decoded through the same fail-closed schema policy, and a tombstoned row stores the content-free stub itself. The crate's advisory migration lock moved to its own id (`982_451_881`) — the previous value collided with `rakka-sharding-postgres`, needlessly serializing the two subsystems' migrations in a shared database. `purge_run` lands on both existing stores as plain scoped deletes with the hold and due-time enforced before any row is touched. The DSN-gated suite proves create/replay/CAS, the concurrent CAS race, cross-scope invisibility, tombstone/delete erasure semantics, the bounded expiry sweep, and the legal-hold purge against a live PostgreSQL 16.
- `rakka-agent` private-memory retrieval (specification 13.3, 13.5, 13.6, and the retrieval clauses of 16; slice 2.2 — retrieval isolation extending scenario 18, and snapshot-reuse-under-drift extending scenario 17): the new `rakka_agent::retrieval` module owns the vendor-neutral seam a vector backend implements. `AgentPrivateMemoryRetriever` ranks an agent's private memories against a bounded `MemoryRetrievalQuery` whose scope, classification, confidence, tombstone, and expiry filters are all *pre-ranking* predicates — a wrong-scope retrieval answers byte-identically to an empty agent's — and `AgentMemoryEmbedder` is the deployment-supplied embedding seam (Rakka ships no production embedder; the deterministic testkit one and the in-memory reference retriever prove the contract without a backend). Retrieval feeds a model only through the slice 1.11 snapshot path: `assemble_context` extends the settle-pass assembly to derive a deterministic query from the just-assembled session window, retrieve, re-check every returned record fail-closed (the assembly trusts no adapter), evaluate each record at the now-live `MemoryIngress` guardrail boundary, and embed the selections *content-first* into the immutable `MemoryContextSnapshot` — `private_memory` was retyped from bare entry ids to `SnapshotPrivateMemory` (id, revision, kind, exact content used, digest, classification, confidence, relevance, embedding metadata, and recorded ingress transforms/reports) under the unreleased-branch rule, because spec 13.5 requires the snapshot to pin content and an id alone cannot make a retried input immune to index drift. First-writer-wins persistence then makes the proof: a re-driven settle after a concurrent CAS update, a tombstone, even a retriever upgrade reloads the original snapshot byte-identical. The ingress boundary joins `AGENT_EVALUATED_GUARDRAIL_BOUNDARIES` (the flip slice 2.1 deferred), evaluated per record with the memory's identity on the guardrail context: block drops that record only, a transform's output is what the snapshot embeds (recorded with its stage revision; a retry reuses it structurally), report-only records the finding on the selection, and require-checkpoint is a fail-closed drop — no checkpoint plumbing exists at snapshot assembly, and memory must never gate liveness. A retriever outage degrades the turn to an empty selection with the attempted retrieval still recorded (13.1 over 13.5; the degraded turn keeps its empty selection forever, by design), counted under the new bounded `rakka.agent.memory.retrievals` / `rakka.agent.memory.ingress.outcomes` metrics — raw queries, records, and embeddings are never labels. Nothing stamps `AgentPrivateMemory.embedding` back through the store: the derived rows carry the metadata, and retrieval output surfaces it per item. Proven by the `retrieval` unit suite and end to end through the real run entity (`tests/private_memory_retrieval.rs`, `tests/memory_ingress_guardrails.rs`).
- `rakka-agent-postgres`: the pgvector retrieval adapter (`PgvectorPrivateMemoryRetriever`). Derived vectors live in their own `rakka_agent_private_memory_embedding` table — a typmod-less `vector` column with a per-row dimension check so one shared migration serves any embedder, keyed `(tenant, agent, memory_id)` with the scope columns leading the primary key, holding the vector plus content-free metadata (model, dimensions, version, `source_revision`, classification, confidence, digest) and never content — and the vector migration is separate (`CREATE EXTENSION IF NOT EXISTS vector` + DDL under the same advisory lock), so the three existing stores and `MIGRATION_SQL` stay green on databases without pgvector. Retrieval is one statement whose tenant, agent, embedder-compatibility, `source_revision = revision` currency, tombstone, expiry, classification, and confidence-floor predicates all sit ahead of the `ORDER BY` distance — every filter the query carries, so an inadmissible record can never consume a result slot and leave a retrieval answering short of what the corpus holds; the two policy columns are denormalized onto the derived row precisely so their predicates can precede the ranking, and the revision fence keeps them from going stale — joined to the authoritative table so content only ever comes from the live record: a leftover vector row is unretrievable in any scope even mid-crash between a delete and its deindex, and eventual consistency manifests as absence, never as ranking current content by stale geometry. Vectors bind as text literals (`$n::text::vector`) — no new dependency; Rust's shortest-round-trip `f32` formatting restores identical values through pgvector's parser, proven server-side by exact zero self-distance — and non-finite or wrong-length embedder output fails closed before any SQL through `embed_memory_vector`, the one check `rakka-agent` now exports so every backend refuses a misdescribed vector identically. The shipped configuration is an exact scan (recall 1.0, cost linear in one agent's corpus; pgvector's approximate indexes post-filter and lose recall under the mandatory scope predicate), with the HNSW/iterative-scan opt-in recipe documented on the module. Maintenance is deployment-invoked, mirroring `purge_expired`: `index_memory` derives one vector (skipping absent/tombstoned/expired/redacted/artifact-backed records visibly), paged `reindex` is the rebuild path — drop every derived row and reindex restores identical retrieval, and it pages *past* a record it cannot decode, counting it into `ReindexPage::failed` rather than propagating, so one unreadable record cannot wedge a rebuild on the same page forever — and `deindex_memory`/`purge_orphaned` remove residual rows after withdrawal, as hygiene rather than correctness. The DSN-gated suite (which probes for the extension and skips with a message on plain PostgreSQL) proves migration idempotence beside the base schema, per-tenant/per-agent scoping with empty-scope indistinguishability, exclusion of withdrawn records with no deindex run, rebuild-identical reindexing, stale-revision and model/version non-candidacy, fail-closed embedder mismatches, exact text-path round-trips, two-connection concurrent index convergence, the classification filter and the confidence floor each beating a nearer hit, and a rebuild paging past a deliberately corrupted record; the CI postgres job now runs on `pgvector/pgvector:pg16` and tests this crate.

- `rakka-agent-knowledge-graph`: the communal knowledge graph crate
  (specification 13.4, 13.6; slice 2.3 — the graph halves of scenarios 16 and
  18). Agents append provenance-bearing claims scoped
  `(TenantId, KnowledgeSpaceId)` — never overwrite communal truth — and every
  claim is born `Proposed` structurally: `Claim::new` takes no trust
  parameter, the only path to a non-`Proposed` claim is restoring a persisted
  record whose `Proposed ⇔ zero-transitions` coherence invariant is
  re-validated on every load, and the store's append door refuses anything
  else (`claim-append-not-proposed`), which resolves open decision 3. Every
  derived field on a claim is likewise re-derived on load rather than trusted:
  `Claim::validate` recomputes the `content_digest` statement fingerprint from
  the record's own subject/predicate/object and refuses a mismatch
  (`claim-statement-digest-mismatch`), so a forged fingerprint — or a statement
  edited under a stale one, as a hand-repaired row or a partially rewritten
  adapter row would leave it — reaches neither the wire nor a store. Open
  decision 2 is resolved by `KnowledgeSpaceScope`: the tenant is part of the
  injective scope key, so a cross-tenant graph is unrepresentable rather than
  merely disallowed. Claim identity derives from the append operation id
  (conflicting claims must coexist, so the statement cannot be the identity),
  which is what makes a replayed append converge on the same claim. Every
  identity derivation (append key, transition key, claim id) digests with
  sha2-256 rather than the default FNV fingerprint — salted domains stop a
  discriminator from being spelled as another domain's input, but only a
  collision-resistant digest stops one from being searched for a collision
  within a domain, and identity here decides whose write a replay answers;
  because the append door re-derives, the algorithm is part of the durable
  contract rather than an implementation detail. The derivation is likewise an
  invariant rather than a convention: `Claim::new` takes no
  claim id and derives one from the scope and operation id, while the append
  door re-derives and refuses a mismatch
  (`claim-append-id-not-derived`), since a restored record can carry any id
  and a squatted id would otherwise deny its rightful writer's append forever.
  Trust moves only through append-only `ClaimTrustTransition` records under the
  documented lattice (`Retracted` terminal, nothing transitions to
  `Proposed`), with a bounded history that refuses explicitly at its cap. The
  `KnowledgeGraphStore` SPI is vendor-neutral — no client, SQL, Cypher,
  SPARQL, or vendor identifier in any public type — with idempotent append
  and transition by operation id (a replay answers the *original* result,
  even after later operations moved the record, and never re-evaluates a
  decided promotion gate), bounded provenance/trust-filtered queries, bounded
  deterministic breadth-first traversal with explicit truncation, and
  optional-capability reporting (`semantic-search` is declared;
  no search method ships until the communal-retrieval slice). Scope isolation
  is scenario 18's shape: every wrong-scope read answers exactly as an empty
  space answers, and a wrong-scope write fails with the same
  `claim-not-found` an absent claim produces. Promotion to `Verified` for
  consequential claims reuses the slice 1.10 checkpoint machinery verbatim:
  the new additive `AgentCheckpointGrant::validate_for_binding` seam in
  `rakka-agent` (which the existing effect-path `validate_for` now delegates
  to, gaining a `target` comparison — a pure strengthening) validates a grant
  against a binding recomputed from the authoritative claim, sha256 over the
  scope key, full statement, and history position, with the effect generation
  pinned to the transition ordinal — so a grant authorizes exactly one
  promotion at exactly one history position and a pre-dispute grant can never
  be replayed into a re-promotion. The default `ClaimPromotionPolicy` gates
  every promotion (fail closed); `ungated` is an explicit deployment
  statement. The crate ships the workspace's first reusable backend
  conformance harness (`conformance::check_knowledge_graph_contract` and its
  per-clause functions), which slice 2.4 runs unchanged against a second
  structurally different backend (scenario 20). It injects fresh *scopes*
  rather than fresh stores — a live-database backend cannot cheaply build
  stores, but every backend can serve one more tenant — and those scopes are
  unique per clause, per process, and per run: `ConformanceScopes::unique`
  prefixes a per-run namespace digested from the process id and the wall
  clock, which `RAKKA_KNOWLEDGE_GRAPH_CONFORMANCE_RUN` pins for a suite that
  wants to find its own rows afterwards and `unique_in` names explicitly for
  one that manages its own namespacing. Its bounded-query and
  bounded-traversal clauses assert against the *effective* limit — the smaller
  of the request and the backend's declared bound — so a backend declaring a
  page or depth bound tighter than the crate cap passes the suite by honouring
  it, while one serving more than it declares still fails; every other clause
  drains the cursor rather than reading a single page. The crate is
  deliberately not
  in the `rakka` facade yet — the agent-adapter precedent — pending the
  specification 19 API review. Proven in
  `crates/rakka-agent-knowledge-graph/tests/knowledge_graph_conformance.rs`
  (scenarios 16 and 18 by name),
  `crates/rakka-agent-knowledge-graph/tests/claim_promotion_gate.rs` (the
  nine-case gate matrix, generation pinning included), the inline module
  tests, and the binding/effect parity test in
  `crates/rakka-agent/tests/checkpoints.rs`.
- `rakka-agent-knowledge-graph-postgres`: the PostgreSQL binding of the
  communal knowledge-graph store SPI, and the second structurally different
  `KnowledgeGraphStore` implementation slice 2.4 owes (specification 13.6,
  scenario 20) — relational tables instead of ordered in-process maps, proven
  by running the conformance suite unchanged, in a commit that touches
  nothing under the agent-domain crates. This also discharges open decision 8
  with no reference backend named: the representative claim, traversal,
  tenancy, and bounded-query families are captured as the conformance
  clauses themselves (tabled in the conformance-module docs), and migration
  stays backend-owned because the portable SPI deliberately has no migration
  surface. The `record` BYTEA is authoritative and rebuilt through the
  domain restore doors on every load, so the schema window, statement-digest
  re-derivation, and trust coherence fail closed against live rows; the
  denormalized columns serve only the traversal predicates and the
  `transition_count` compare-and-set fence, and a column that disagrees with
  its own record is refused as drift. Every write is one
  data-modifying-CTE statement on the shared pipelined client — claim
  mutation, transition row, and scope-wide operation-ledger row commit or
  fail together — and a replay answers the ledger's original bytes without
  re-litigating a decided promotion gate, even under a grant that has since
  expired. Queries are keyset scans in `COLLATE "C"` claim-id order with the
  shared `admits()` rule applied in Rust (`ClaimFilter` deliberately exposes
  no accessors, and resuming by claim-id position loses nothing); traversal
  is the reference breadth-first expansion over bounded per-node queries
  whose predicates all sit ahead of the `LIMIT`; the transition path is a
  bounded compare-and-set retry loop that re-reads the ledger first on every
  attempt, so contention converges on the winner's replay or a typed
  legality refusal. The migration advisory lock takes the fresh id
  `982_451_927`, and the CI postgres job tests the crate on every pull
  request. Proven in
  `crates/rakka-agent-knowledge-graph-postgres/tests/postgres_conformance.rs`
  (all twelve clauses plus scenarios 16, 18, and 20 by name) and
  `tests/postgres_backend_proofs.rs` (reconnect replay, two-connection
  distinct- and same-operation races, gated-promotion replay after grant
  expiry, migration idempotence and the four-migrator private-schema race,
  doctored rows failing closed).
- `rakka-agent` continuous-goal wake identity and versioned wake policy (slice 3.1):
  - `AgentWakeId` construction is *derived*, never generated: `wake_id_for_occurrence` is a pure function of `(tenant, goal, ScheduleRevision, logical occurrence)` — a `wake-`-prefixed SHA-256 digest over a length-prefixed canonical encoding — so the same occurrence reached from any trigger path (timer scan, duplicate scan after scanner restart, redelivered event, authenticated A2A command, callback) yields one identity, and wake deduplication is a construction property rather than runtime coordination. The encoding is injective whatever the goal or event identity contains, the output is fixed-length and always a valid identity segment, and the format is pinned by golden vectors in `tests/wake_identity.rs` as a persisted compatibility surface. `AgentWakeOccurrence` carries the identity half of a wake (scheduled due time, external event, A2A command operation, or callback identity); `AgentWakeBinding` carries everything specification 6.9 requires a wake to bind — trigger kind/source, due/accepted times, policy revision — and deserialization re-derives the identity and fails closed on a record whose stored id its own components do not derive. `wake_admission_operation_id` is the `wake-admission` operation the slice 3.2 controller's durable inbox will deduplicate on. The dedup construction is property-tested with `proptest` (a new dev-only workspace dependency): trigger-path invariance, injectivity, per-component sensitivity, and fixed-length validity.
  - `AgentWakePolicy` with the full specification 8.2 field set — allowed trigger classes, admission window and maximum lateness, overlap, missed-occurrence, per-epoch budget and deadline, rolling/calendar goal window ceiling, failure backoff, and suspension/renewal/expiry/retirement — versioned by `AgentWakePolicyRevision` under its own fail-closed schema version, exactly as settings revisions are. The constructor produces the resolved continuous defaults (specification 21.1 items 1-3): overlap forbidden with durable coalescing, at most one coalesced occurrence after downtime, no catch-up; parallel epochs and bounded catch-up must be declared explicitly with their concurrency and result policy. An epoch must be bounded from construction — a deadline or at least one bounded budget dimension — and a malformed policy fails closed on deserialization.
  - Continuous-mode fields on the goal/root-task contract, scoped to what the controller needs (the full `AgentGoalSpec` lands with M4): `AgentGoalMode` distinguishes finite from continuous goals, and `AgentContinuousGoalSpec` carries the current `ScheduleRevision`, the versioned wake policy, and the explicit health condition specification 8.1 requires. `AgentTaskCreation` and the materialized task record carry the mode (records persisted before the field load as finite); a continuous creation without a goal binding is refused closed (`task-continuous-without-goal`).
  - Review hardening on the slice: the task-state load gate version-checks the wake-policy revision a continuous task embeds, so a task record carrying a revision written by a newer binary fails closed (`schema-version-ahead`) exactly as the embedded task definition does; a maximum lateness that undercuts the admission window is refused (`wake-lateness-below-admission-window`) — an occurrence between the two would be both admittable and missed; and a zero `ScheduleRevision` is unrepresentable — construction clamps it to the initial revision and a persisted zero fails closed on load.
- `rakka-agent` continuous-goal wake controller and shared scanners (slice 3.2):
  - `AgentWakeControllerState`, the durable admission state machine the continuous root control task embeds: every delivery is dispositioned deterministically — fenced for an obsolete `ScheduleRevision`, answered as a duplicate, admitted into a bounded active slot, durably coalesced behind it, or skipped under the missed-occurrence policy — and every disposition, a fence and a skip included, is a recorded transition under the wake's derived admission operation id, so the wake counters are exact and a replayed delivery answers from the record instead of transitioning twice. Deduplication beyond the task operation log is a state property: the active and parked slots, a bounded ring of recently dispositioned wake identities, and a monotone scheduled-due-time watermark. The resolved defaults of specification 21.1 are enforced at runtime: a trigger arriving while an occurrence owns execution coalesces into a single latest-wins slot, downtime admits at most one coalesced representative, `Skip` counts and drops, and `BoundedCatchUp` queues to `min(bound, AGENT_WAKE_PENDING_CAPACITY)` and skips the overflow rather than keeping it silently. The band between the admission window and the maximum lateness coalesces exactly as slice 3.1 documented, and a persisted controller state beyond its bounded capacities fails closed on load.
  - Wake transitions on `AgentTaskEntity`: `AdmitWake` fails closed unless its operation id is the one its own binding derives (`task-wake-operation-mismatch`) and the task continuously coordinates the wake's goal (`task-wake-not-continuous`, `task-wake-goal-mismatch`); `CompleteWakeOccurrence` releases the active occurrence and promotes the oldest parked one in the same durable transition — the transition slice 3.3's epoch-result path will drive; `UpdateContinuousSchedule` is strictly monotonic on the schedule revision and any riding wake-policy revision (`task-schedule-not-monotonic`, `task-wake-policy-not-newer`) and fences every parked occurrence the old schedule created. A binding whose revision is *ahead* of the controller fails closed (`wake-revision-ahead`): no accepted schedule issued it. The task outcome records the wake disposition, release, or schedule update it answered, so a replay carries it; `AgentTaskSnapshot` exposes the continuous goal's `AgentWakeStatusView` — schedule and policy revisions, active and parked wakes, last admission, and the monotone counters.
  - `AgentWakeTimerStore`, the shared scanner's durable index of parked occurrences under the new fail-closed `AgentRecordKind::WakeTimerState` schema version: one compare-and-set record keyed by the derived `AgentWakeId`, so parking the same occurrence twice is a construction-level duplicate and a colliding entry with a different binding or target task is refused (`wake-timer-mismatch`); entries carry the full `AgentWakeBinding` — whose load re-derives and verifies the identity — plus the root control task to deliver to, and mark-fired, mark-fenced, and cancel are idempotent terminal transitions, so a fenced entry is never rescanned. Losing a compare-and-set race drops the cached record, so the loser's next operation recovers the authoritative revision and converges instead of failing forever against a revision that no longer exists.
  - `AgentWakeScanner`, a caller-driven bounded pass in the courier idiom: it recovers due entries, injects each as the `AdmitWake` command its binding derives (`wake_admission_command`, the one constructor every trigger path shares), marks the terminal status the controller's disposition earned, and leaves refused or undelivered entries pending for the next pass. Scanner and pod uptime never create an occurrence; a crash between delivery and mark-fired redelivers into the controller's deduplication. A pass preserves per-task due order: after a failed or refused delivery, later-due entries of the same task are held back (`AgentWakeScanOutcome::HeldBack`) instead of delivered around the failure, so the controller's scheduled-due-time watermark — which deduplicates on occurrences arriving in due order — can never be advanced over an occurrence that was never applied and then swallow its redelivery as a false duplicate. `ShardedWakeDelivery` is the production delivery over the sharded task-entity registration: it asks the locally owned entity and reports a stable `wake-remote-owner` failure otherwise, so delivery follows shard ownership and a wake needs no cross-node command surface. Deliveries count on the bounded `rakka.agent.wakes` metric, and `rakka_agent::testkit` gained `InProcessWakeDelivery`, which injects the wake failure windows through the same `ExchangeFault` queue the exchange transports use.
  - Scenarios 47-50 are proven end to end: `tests/wake_scanner.rs` (restart, activation, and empty scans create nothing; the lost-reply, double-delivery, and mark-fired crash windows converge; an exhaustive owner-loss sweep over every task-store write of the create-then-admit flow), `tests/wake_coalescing.rs` (one occurrence from every trigger path is one wake and one admission; overlap coalescing with latest-wins supersession and release-time promotion; downtime admits at most one coalesced occurrence; `Skip` skips), `tests/wake_fencing.rs` (an obsolete-revision injection is fenced and its entry goes terminal; a restart resets neither revision, policy, nor counters; a stale owner is fenced by the compare-and-set and converges after recovery), and `tests/wake_sharding.rs` (the controller passivates between wakes under the real sharded actors, and only a durable due occurrence — delivered through the production `ShardedWakeDelivery` — reactivates it).
- `rakka-agent` continuous-goal epoch admission and budget windows (slice 3.3):
  - An admitted wake now creates its finite child epoch, atomically. The epoch's identities are *derived* from the wake — the child task is `epoch-` plus the wake's own digest (`epoch_task_id_for_wake`, a constant 70 bytes independent of the root task id, pinned by a golden vector), the run is `run_id_for_assignment` at generation one — and one compare-and-set on the root controller carries the disposition, the goal-window charge, the escrow debit from the root's own ledger (keyed on the derived run, so a replay never debits twice), the `AgentEpochRef` on the active occurrence, and the owed `Creation` exchange. `AgentTaskCreation` gained the carried escrow grant the specification 9.7 comment always promised and the admitting `AgentWakeId`; `AgentContinuousGoalSpec` gained `AgentEpochSpec` — the epoch's task definition, assignee, and observation scope — and an admission on a goal without one fails closed (`task-epoch-undefined`), rolling the whole transition back. Release-time promotion runs the identical path for the promoted occurrence.
  - Epoch completion returns through the new `AgentExchangeKind::EpochResult`, owed by the epoch task's own transitions exactly once — after its ledger closed (the run's settlement *and* return applied), so the consumption it reports is never an early under-count; a cancelled epoch owes it from the cancel transition, a dependency failure that terminates the epoch owes it from that transition the same way, and the journal's initiation record is the once-guard. The controller's apply verifies the sender is the very task the wake derives (`task-epoch-forged`), settles and returns the epoch's escrow idempotently, releases the wake — tolerating an explicit `CompleteWakeOccurrence` having raced, whose settlement still counts — and owes the promoted occurrence's epoch creation in the same transition; the sequence commits only whole, so a refusal persists no partially applied settlement or promotion. Epoch completion never terminates the continuous goal.
  - The goal-window ceiling is enforced at epoch admission: `AgentWakeWindowLedger` on the controller state charges the full epoch allocation, refills only inside whatever recorded transition first observes logical time across the boundary — rolling windows anchored at the first charge, calendar windows on UTC civil-date boundaries computed in-crate — and never on restart, activation, or shard movement. An admission the window cannot pay for parks with the new recorded `Deferred` disposition (a `deferred` counter joins the monotone set) and is retried — oldest parked first — by the next release or delivery whose transition observes a window able to pay: the admission transition promotes the oldest parked occurrence into a free slot *before* dispositioning the fresh delivery, so a deferred occurrence is never leapfrogged by a fresher one (nothing fires at the window turn itself; a controller-originated window-turn re-wake is a recorded slice 3.4 decision, bundled with failure-backoff re-wakes); a policy whose window bounds a dimension its epoch budget leaves unbounded is refused at construction (`wake-window-epoch-unbounded`), and one whose epoch budget exceeds a ceiling dimension — a window that could never pay for a single epoch — is refused the same way (`wake-window-epoch-exceeds-ceiling`).
  - Review carryovers from the slice 3.2 PR closed: the obsolete-revision fence runs before the scheduled-due-time watermark in `admit()` (a stale binding answers `Fenced`, never a swallowed duplicate); a fence no longer advances the watermark, so a future-dated obsolete straggler cannot swallow the new schedule's occurrences; `UpdateContinuousSchedule` resets the watermark, so a new revision may issue due times at or below what the old schedule reached; and an active downtime representative now *absorbs* later missed occurrences of its backlog (counted `missed`, never parked), so one downtime yields exactly one epoch rather than a representative plus an echo — the resolved reading of specification 21.1 item 2 — and a parked occurrence promoted past its maximum lateness comes back *as* that representative, recomputed at promotion, so the absorb holds across park and release.
  - Scenarios 36 and 51 are proven end to end: `tests/epoch_lifecycle.rs` (the full cycle — wake, epoch task and run, accepted result, upward settlement, release, the next durable occurrence parked, and a fresh scanner resuming it — on the direct-drive harness and again on real sharded actors that drain to zero resident entities between epochs), `tests/epoch_memory.rs` (distinct per-epoch session scopes with nothing visible across them; each epoch opens with its own occurrence reconstructed from controller state; what crosses epochs is the controller's counters and the goal ledger), `tests/epoch_admission.rs` (exactly one epoch task and run per wake under replays and an exhaustive owner-loss sweep; the epoch carries goal, wake, parent, and the debited escrow), and `tests/goal_window.rs` (deferral, replay-answered deferral, refill only by logical time across structural restarts, and release-time promotion charged against the turned window).
- `rakka-agent` continuous-goal lifecycle, controller re-wakes, and the M3 acceptance example (slice 3.4):
  - The continuous goal's lifecycle is now executed, not just validated: `AgentGoalLifecycleState` on the controller state carries status (`Active`/`Suspended`/`Expired`/`Retired`), a monotonic lifecycle revision, the change's provenance, the bounded suspension reason, the renewal override, the failure streak, the backoff, and the owed re-wake slots. Four fenced task commands — `SuspendContinuousGoal`, `ResumeContinuousGoal`, `RenewContinuousGoal`, `RetireContinuousGoal` — apply under the expected lifecycle revision (`wake-stale-lifecycle-revision` otherwise); expiry and `At`/`AfterOccurrences` retirement are observed by whatever recorded transition first sees them true, never by a timer. `Expired`/`Retired` are absorbing: fresh deliveries answer the recorded `Barred` disposition and the scanner marks their entries terminal, while an in-flight epoch's `EpochResult` settlement still lands so its escrow closes. Resume clears the streak and the backoff (an operator resume is an explicit override) and promotes what suspension parked, owing the promoted epoch's creation in its own transition; renewal fences to `[effective expiry − window, effective expiry)` under `RequiredBefore` and must strictly extend. A `Failed` epoch grows the streak and arms the geometric backoff *before* the release attempt (a raced explicit release still counts), `Cancelled` neither grows nor resets, the backoff gates fresh admissions and promotions alike, and a streak reaching `escalate_after_failures` auto-suspends with a bounded reason.
  - The controller now owes itself durable re-wakes — the slice 3.3 decision landed as one mechanism with two per-cause slots (`backoff` and `window-turn`; one slot would lose liveness when the causes coexist), recomputed idempotently by `ensure_rewakes` at the end of every mutating entry point and cleared while the lifecycle forbids admission. The re-wake is the new `AgentWakeOccurrence::Retry { due_at, cause }` under the new `AgentWakeTriggerKind::Controller`; the two require each other, `Controller` is exempt from the policy trigger allow-list for exactly this pairing, `Retry` exposes its due time so the parked entry fires at the computed moment, and the retry arm of `admit()` consumes the delivery without admitting — the pre-admit promotion already did the work, so a stale re-wake is a recorded no-op nudge that needs no cancellation. The slot and the `Retry` occurrence carry an attempt generation that is part of the wake identity: a retry delivered before the controller's own clock reaches its due time (a scanner host running ahead) is consumed while its cause still holds, so the consume re-arms the slot under the next attempt and the settle pass parks a fresh entry the fired one cannot absorb — clock skew costs one recorded cycle per scan instead of stranding the parked occurrences. The scheduled due-time watermark is now scoped to `Scheduled` occurrences. Entities park owed re-wakes from their settle passes through an optional object-safe `AgentWakeRewakeParker` (`SharedWakeTimerParker` adapts the shared wake-timer store; wired via `with_wake_timers` on the task store, entity, and sharding settings) — without the handle, owed re-wakes stay durably owed and query-visible; park-then-mark crash windows converge on the derived wake id. The wake-timer store's mutations now re-recover and retry a bounded number of times on a lost compare-and-set — the parker made the store multi-writer within one scan pass (the scanner's mark races the re-wake the delivered entity's own settle just parked), and every mutation is idempotent over the re-read record, so losing the race is normal operation rather than a failed pass. Terminal timer entries are never pruned implicitly; `prune_terminal(before)` is the explicit operational act.
  - Continuous-goal observability landed on the task entity: bounded counters `rakka.agent.wake.dispositions{outcome,trigger}`, `rakka.agent.epochs{outcome}`, and `rakka.agent.goal.lifecycle{transition}` emit post-commit on `Applied` replies only (never `Duplicate`; reply-driven and replay-suppressed on the exchange path), with admitted epochs counted as the difference of the controller's monotone `admitted` counter across the committed transition — the one source that sees a promotion made in the same breath as a fresh delivery, a release, or a resume — lifecycle transitions counted as the difference of the goal's lifecycle status across the committed transition (so observed flips — expiry, retirement by policy, escalation into suspension — count exactly like commanded ones, with `renewed` alone counted from its command since renewal leaves the status unchanged), and the wake scanner's existing counter rerouted through the agent-domain label validator. Audit is task history (specification 17.13's audit obligation, recorded where the durable truth already lives): new `AgentTaskHistoryKind` entries — `WakeDispositioned`, `EpochAdmitted`, `EpochSettled`, `GoalSuspended`/`GoalResumed`/`GoalRenewed`/`GoalExpired`/`GoalRetired`, `ScheduleUpdated` — are recorded only inside the recorded transition they describe, with the wake id and disposition in the bounded detail. The task-scoped authoritative query landed as `agent_task_operational_snapshot` (one durable read, content-redacted, no entity activation), `AgentWakeStatusView` grew additively (window ledger, lifecycle state, active epoch references), and `next_pending_wake_for_task` joins "next wake" purely from the wake-timer state.
  - The M3 milestone checklist is demonstrated end to end by `examples/continuous-goal-acceptance`: a 16-line transcript pinned three ways (README, `EXPECTED_TRANSCRIPT`, `tests/acceptance.rs`) walking passivation, derived-epoch admission with a real completed epoch run, duplicate-delivery convergence, coalescing, a crash mid-settlement with convergent replay, a downtime backlog, the schedule-revision fence, window exhaustion and the window-turn re-wake, failure backoff and escalation into suspension, fenced and real resume, renewal plus a stale former owner losing the compare-and-set after shard movement, per-epoch isolation, retirement by occurrence count with a barred late delivery, and the operational query answering from durable state alone.
- `rakka-agent` goal contract and lifecycle (slice 4.1, the first M4 slice):
  - The full durable goal contract of specification 8.1: `AgentGoalSpec` — owner, objective (artifact reference plus bounded summary), versioned success criteria, priority, deadline, cancellation reference, the conserved goal allocation and non-conserved limits, delegation ceilings, the goal-scope budget-exhaustion policy, bounded allowed-reference sets (skills, tools, workflows, knowledge spaces, environments), evaluator and required-evidence classes, and escalation/terminal-decision/stagnation references — versioned as `AgentGoalSpecRevision` under its own fail-closed `AgentRecordKind::GoalSpec` schema version, validated at construction and on load, and capped at 4 KiB serialized so a maximal goal-bearing continuous task stays inside the task's materialized bound with its growth reserve intact. The record is a component of the root task's own state (specification 6.3: the root `AgentTaskEntity` coordinates; a dedicated goal entity can take over later without changing the contract), and the goal identity defaults to the root task's value through the new infallible `AgentGoalId::for_root_task` — open decision 14's recorded disposition — while the types stay distinct.
  - The `AgentGoalStatus` lifecycle (`Proposed`/`Active`/`Waiting`/`Satisfied`/`Unsatisfied`/`Failed`/`Cancelled`/`Expired`) on `AgentGoalState`, with fenced transitions on a monotonic status revision. The terminal status is derived from the structured `AgentGoalTerminalReason` — an inconsistent outcome/reason pair is unrepresentable — and the `Unsatisfied`-vs-`Failed` distinction is a reason, never a guess: a criteria decision (`Satisfied` or `Unsatisfied`) is unconstructible without an `AgentGoalEvaluationRef` that assessed the criteria revision in force, so "the model declared success" cannot end a goal (specification 8.3; slice 4.2 wires the evaluator that may call the door). Entity commands `ActivateGoal`, `RecordGoalDecision`, and `ResumeGoal` deduplicate and fence like every other task command; a goal instituted at creation starts `Active` by default (`activate_on_creation: false` starts it `Proposed`, which gates assignment and parks continuous admission until activated); a goal's own passed deadline is expired durably by the settle pass — the goal entry point that always commits. The contract status is deliberately orthogonal to the M3 continuous admission gate: gate transitions project one-way onto the goal record in the same compare-and-set (suspension parks, resume reactivates exactly that park, retirement cancels with the structured `Retired` reason — specification 8.1 has no `Retired` status — and expiry expires), goal-terminal decisions retire the gate, and a terminal root task ends the goal it coordinates except on completion, where only the evaluator may satisfy. The `rakka.agent.goal.status` counter counts contract transitions by status difference across the committed transition, distinct from the gate's `rakka.agent.goal.lifecycle`; four new task-history kinds (`GoalActivated`/`GoalParked`/`GoalReactivated`/`GoalDecided`) record the contract's audit beside the gate's, and `AgentTaskOutcome`/`AgentTaskSnapshot` carry the goal outcome and `AgentGoalStatusView` so replays and `Describe` answer the goal from durable state alone.
  - The goal-scope budget-exhaustion policy of specification 9.7: `AgentGoalExhaustionPolicy` (default `Park`, per-dimension overrides) is consulted where the goal scope observes its own ceiling — a zero top-up grant in the run's exhausted conserved dimension, and an assignment refusal no settlement can relieve. `Park` moves the goal `Waiting` with the structured exhaustion and suspends continuous admission in the same compare-and-set; `Escalate` is the same park with the spec's escalation reference on record; `Terminate` fails the goal, the root task (`goal-budget-exhausted`), and the gate together. The goal's allocation seeds the root task's escrow narrowed to the definition ceilings — the definition-ceiling → goal-allocation → task rung of the budget hierarchy — and the one `ResumeGoal` door widens the ledger under those same ceilings (`AgentEscrowLedger::widen`: grow, cap, never shrink), reactivates the contract, and lifts the goal-driven admission park in one fenced compare-and-set, refusing a resume that leaves the exhausted dimension without headroom (`task-goal-resume-unrelieved`). Each resume door owns its wait (`task-goal-wait-owned-elsewhere`): the gate's resume refuses a budget park it never examined, and the goal's resume refuses an admission suspension the gate owns. The M3 window-ceiling deferral stays outside the policy — a window wait relieves itself at the persisted window turn — and the run-side contract is untouched: the run still receives the honest zero grant and stops with its original exhaustion.
  - Proven by the goal transition matrix inline in `goal.rs`, `tests/goal_contract.rs` (derived identity, proposed gating, fenced and deduplicated decisions, evaluation-bound satisfaction, cancellation and retirement projection, settle-pass deadline expiry), `tests/goal_budget.rs` (park/escalate/terminate at both trigger points, the resume door, and the wait-ownership fences), the new `tests/goal_passivation.rs` proof that the goal contract stays addressable while every entity is passivated to zero residents — one durable command then transitions it exactly once — and the bounded-state and metrics extensions; the whole M3 suite passes unchanged.
- `rakka-agent` progress, evidence, and evaluation (slice 4.2):
  - Goal evaluation is a durable effect (specification 8.3): the new `AgentRunEntityCommand::EvaluateGoal` commits a read-only `Evaluation` effect (`AgentRunEffectKind::GoalEvaluationCall`, default two attempts) in a bounded deduplicated transition — fenced against a winding-down run, a goal the run is not bound to, and a second in-flight evaluation — and the dispatcher executes it through the new application-owned `AgentGoalEvaluationExecutor` (absent executor: definitive `evaluation-executor-missing`). The verdict `AgentGoalEvaluationOutcome` is two-valued and *both values are terminal*: `Satisfied` decides the goal satisfied, `NotSatisfied` decides it unsatisfied, and an evaluator meaning "not met yet, keep working" returns `AgentGoalEvaluationFinding::Refused` instead — which fails the effect definitively and leaves the goal `Active` and decidable. All five evaluator methods of specification 8.3 are typed (`AgentGoalEvaluationMethod`): deterministic assertion, authoritative query, and the evaluator model execute now; human review executes as an `Approval` checkpoint bound to the evaluation effect itself, where the digest-bound grant *is* the verdict — the record carries the resolver and the durable authorized decision as evidence, a denial is a failed evaluation (the goal stays `Active` and decidable), and expiry never auto-approves; a verification workflow is refused closed (`run-goal-evaluation-workflow-deferred`) until workflows-as-tools land. The evaluator model runs under the distinct policy the request pins: `authorize_goal_evaluation` resolves the profile from the request alone against the definition and setup envelopes, so the agent's turn-bound settings profile can never stand in for the evaluator's. A failed evaluation is the second exception to the run's effect-failure wind-down (beside memory promotion): the coordinator run must outlive a refused or unwired evaluation so the goal stays decidable.
  - The completed evaluation is the durable `AgentGoalEvaluationRecord` — evaluator, method, criteria revision, verdict, bounded stable reason code, classed evidence references, resolver when human, and the producing effect generation; references and codes only, never model text, hidden reasoning, or credential material — under its own fail-closed `AgentRecordKind::GoalEvaluation` schema version. It parks in the run's one durable evaluation cell and crosses to the coordinating root task as the new `AgentExchangeKind::GoalEvaluation` exchange, owed from durable state under a derived operation id (a crash anywhere re-owes the identical exchange; the journal deduplicates), and the decision door's refusal settles the cell with the door's own code — the caller re-evaluates, never a crash loop.
  - The decision door is attested (scenario 30): under a configured `spec.evaluator`, `RecordGoalDecision` refuses every criteria decision (`task-goal-decision-unattested`) — the goal-evaluation exchange, sender-fenced to the task's currently assigned run (`task-goal-evaluation-forged`), is the only ingress — and without one the 4.1 contract stands. The shared decision core enforces, for both ingresses alike: the evaluation must exist, must have assessed the criteria revision in force (`goal-evaluation-stale`), must name the configured evaluator (`goal-evaluator-mismatch`), and must cover every `required_evidence` class with a classed evidence item (`goal-evidence-missing`, bounded by the new `AGENT_GOAL_EVALUATION_MAX_EVIDENCE`; a spec demanding more classes than one evaluation may present is refused as statically unsatisfiable). A request is bounded against the room its *method* leaves rather than the raw ceiling — `AgentGoalEvaluationMethod::evidence_reserve` holds back the slot the dispatcher fills with the authorized decision (`AGENT_GOAL_EVALUATION_HUMAN_DECISION_CLASS`) — so a human review that would overflow its record is refused at the commit door instead of after a human has already approved it and spent the grant. `AgentGoalEvaluationRef` grew additively — evaluation id, method, classed evidence items, and the SHA-256 attestation digest of the full record. `attestation_digest` and `to_evaluation_ref` are fallible and *propagate* an encoding failure (`evaluation-record-unencodable`) as the sibling `cryptographic_argument_digest` does, rather than substituting a placeholder: a decision is never bound to a digest computed over anything but its own record, and the door turns the failure into a refusal. The commanded ingress observes the goal's deadline *before* the attestation fence, so an expired goal refuses `goal-terminal` — the honest answer — instead of masking the expiry behind `task-goal-decision-unattested`. The new `ReviseGoalCriteria` command makes the staleness fence real: it advances the criteria revision (and the spec revision through `AgentGoalSpecRevision::updated`), fenced on the criteria revision itself (`goal-stale-criteria-revision`), and an in-flight evaluation is invalidated purely by the existing door fence — it completes, is refused stale, and is re-issued at the revision in force.
  - Stagnation detection at the epoch settlement (specification 8.3): `AgentGoalStagnationPolicy` on the goal spec — repeated-result and no-progress thresholds, disabled unless set, with a per-trigger continue/replan/wait/escalate/terminate action map (`Replan` is typed but refused at spec validation until a slice can execute it honestly). The wake controller's detector accounts every completed epoch's result fingerprint (the `AgentEpochResult.result_digest` that previously dropped at settle; failed and cancelled epochs neither grow nor reset the streaks, which keeps them disjoint from the failure streak), trips exactly at a set threshold with `RepeatedResult` before `NoProgress`, and the durable streak fields and monotone trip counters ride `AgentGoalLifecycleState`/`AgentWakeCounters` additively. A trip's gate-closing action lands before the release — a closed gate releases the active wake without promoting a coalesced occurrence — and `apply_goal_stagnation` executes the contract side in the same settlement: `Continue` records the detection only; `Wait`/`Escalate` park the goal `Waiting(Stagnant)` (the escalation and stagnation policy references on the audit record) and suspend admission; `Terminate` fails the goal (`AgentGoalTerminalReason::Stagnant` → `Failed`, never `Unsatisfied`, which stays unconstructible without an evaluation), the root task (`goal-stagnant`), and the gate together. `ResumeGoal` owns the stagnation wait and performs the one deliberate non-progress reset — the authorized resume clears the detector with provenance on record, because the next identical epoch would otherwise instantly re-park — and the gate's own resume now refuses *any* wait it does not own (previously only exhaustion-carrying waits; a stagnation park would have slipped through and split the two records). New audit kinds `GoalStagnationDetected` (the repeated fingerprint in the entry's digest slot) and `GoalCriteriaRevised`; `EpochSettled` rows now carry the epoch's result fingerprint; the new `rakka.agent.goal.stagnation{trigger}` counter counts trips by durable-counter difference, so a replayed settlement counts nothing and an observe-only `Continue` trip is still visible. Streak accounting and trip counting are separate steps (`record_epoch_progress` / `record_stagnation_trip`): a streak is a durable fact about the epochs and always accounts, while the counter moves only in lockstep with the trip's `GoalStagnationDetected` row, so a trip the goal's status suppressed — `Proposed`, or ended by a projection in the same settlement — reports nothing.
  - Proven by `tests/goal_evaluation.rs` (the scenario-30 end-to-end pin — unattested declaration refused, evaluation as a durable effect, the exchange decides once, the attestation persisted whole — plus evaluator mismatch, missing evidence class, mid-flight criteria revision, executor-missing leaving run and goal live, human-review approval and denial, a `NotSatisfied` verdict ending the goal `Unsatisfied` and staying absorbing, the human-review evidence reserve refusing ahead of the approver, an expired goal refusing `goal-terminal` rather than unattested, the workflow deferral, and the one-at-a-time fence), `tests/goal_stagnation.rs` (trips exactly at the threshold and never before, progress resets, failure-streak disjointness, wait/escalate/terminate arms, the both-ways door fence, the resume reset, no-progress fingerprint absence, the observe-only audit, and a suppressed trip accounting its streak while counting nothing), the `tests/goal_contract.rs` additions (criteria revision fencing and the slice 4.1 epoch-settle projection gap: an expiry observed while settling an epoch's result decides the goal in that same transition), the bounded-state growth proof over a fully populated stagnation policy, and the stagnation metric's once-per-trip pin; the whole M3 and 4.1 suites pass unchanged.

- `rakka-agent`/`rakka-a2a` durable delegation and A2A collaboration metadata (slice 4.3):
  - The durable delegation record of specifications 8.4 and 6.6: a model call to the one declared coordination tool (`AgentToolKind::AgentCoordination`, wired through the new `AgentRunEntityStore::with_delegation(AgentRunDelegationConfig)`, whose construction refuses a coordination set without the `Delegation` capability) is intercepted by the loop and committed as an `AgentDelegationRecord` — goal, parent task/run, lineage/depth, requested skill, the recorded catalog resolution, A2A message and deduplication identities, the producing `(turn, slot, effect, call)` coordinate, bounded input, advisory budget, deadline, revisions, and trace context — plus its outbound `AgentRunEffectKind::A2aSendCall` send effect, in one compare-and-set, strictly before anything reaches the sink. Every identity is a pure derivation: `delegation_id_for(scope, turn, slot)` (the wake-id digest construction) doubles as the A2A message id and deduplication key, the receiving surface derives the child `AgentTaskId` from that key, and a replay of any window converges on the same logical child. The send is idempotent by construction (default `AGENT_A2A_SEND_DEFAULT_MAX_ATTEMPTS = 3` attempts, no reconciliation protocol: an ambiguous loss retries safely under the same key); the receipt settles the run's bounded delegation cell (`AGENT_RUN_MAX_DELEGATIONS = 16`) `ChildCreated` in the same transition that succeeds the effect and completes the turn through a synthesized bounded tool result, a definitive failure settles it `Failed` (`Conflicted` for the explicit-conflict codes) before the standard wind-down, and a fenced send under a winding-down run settles it `Failed { run-winding-down }` on every fence path — the dispatch layer's fence, the in-place fence of a cancellation or failed-effect wind-down that catches the send committed but unsent, and a reconciliation that confirms an ambiguous send never executed — so the record, the effect, and the status never disagree and no cell stays `Pending` under a cancelled effect. A quiescing parent never spawns children, and waiting for the child's *result* is deliberately the fan-in slice's work, not this one's.
  - The application-owned catalog boundary of open decision 15 (disposition recorded): model output can only name an `AgentCapabilityId` skill and bounded input — `AgentDelegationToolCall` parses under `deny_unknown_fields`, so an agent id or endpoint in model output refuses — and the new `AgentDelegationCatalog` port in `rakka-agent` (with `StaticAgentDelegationCatalog` for tests; `delegation-skill-unknown`/`-not-authorized`/`-ambiguous` refusal vocabulary) resolves the concrete `AgentDelegationTarget` inside the committing transition; replays reuse the recorded resolution verbatim and never re-resolve, which is what makes catalog drift a visible conflict instead of a silent second child. Goal-scope narrowing rides the new assignment envelope (`AgentRunAssignment.delegation: AgentRunDelegationEnvelope`, copied by the task's assignment decision from its goal spec or its own creation provenance, so the run never reads the goal spec): a non-empty `allowed_skills` set closes delegation to those skills, the previously inert `allowed_tools` set now closes generic tool calls the same way (`goal-tool-not-allowed`), and every interception refusal is a failed tool result under its stable code, with application-supplied refusal text bounded at the recording door so an oversized catalog message cannot turn a refusal into a transition failure — the run survives, the model corrects course, and the existing iteration/budget ceilings bound the retries. Admission is also priced against the run's materialized bound: a delegation is held twice in durable state (cell and effect payload), so one whose commit would cross the remaining headroom is refused `delegation-headroom-exceeded` instead of committing a turn the bound check would wedge afterwards. Peer reach is structural plus one enforced check: the `A2aSend` request variant is constructible only by the interception, the peer-send executor is invoked for that variant alone, and the dispatch authority refuses a registered coordination-kind tool that arrives as a generic intent (`coordination-tool-not-intercepted`).
  - The versioned collaboration extension of specification 14.4 in `rakka-a2a`: `urn:rakka:a2a-extension:collaboration:v1` with the envelope schema number inside it, carried as one `AgentCollaborationMetadata` JSON object under the new public `io.rakka.collaboration` metadata key (metadata, not a data part — the message's parts *are* the child's input), declared in `message.extensions`, parsed whole at the single `normalize_agent_send` chokepoint. A client that never engages the extension is untouched — unknown-optional compatibility is preserved — and every half-formed engagement fails closed: an unserved version, the v1 declaration without the metadata object, the reserved key without the declaration, a foreign schema number, or a malformed envelope. Ingress populates the creation from the validated envelope — the child records `AgentTaskDelegationProvenance` (bounded: `AGENT_DELEGATION_MAX_LINEAGE = 16` ancestors and `AGENT_DELEGATION_PROVENANCE_MAX_BYTES = 8 KiB` serialized — the receiving side of the parent's own record bound, so a peer cannot inflate a child's durable state with unbounded scope or binding collections; validated, not trusted: the declared depth must agree with the presented lineage — depth is always the ancestor count plus one, `delegation-depth-incoherent` otherwise — and the parent run must live in the child's own tenant, so forged bindings refuse at the creation door before the enforcement slices could ceiling against them) with its parent and goal bindings; the escrow stays `None`, because a conserved grant cannot ride A2A and the envelope's budget is advisory provenance for the enforcement slices — and the public projection echoes `{delegation, parent-task, depth}` under the same key, which is both observability and the sender's identity check. The new `A2AAgentDelegationSendExecutor` implements the `AgentA2aSendExecutor` port in-process over the same `RakkaAgentA2AService` core an external caller uses (the client-transport precedent: no local entity shortcut), disambiguates the peer's `task-already-created` against the held child's collaboration echo — the child's deduplication window is bounded, so an aged-out replay of the delegation's own send earns the same refusal a genuine conflict does; an echoing child converges exactly as an in-window replay would, and only a child the identity does not own is the explicit `delegation-child-conflict` — and reports a child that answers under another delegation as `delegation-child-mismatch`, never an adoption. The envelope carries logical credential-binding references at most, never resolved credentials; `AgentDispatchTargetClass::A2aPeer` now accepts the executor-routed tool family, so a declared `a2a-peer` target classifies truthfully.
  - Proven by `tests/delegation_record.rs` (the record persisted pending before any executor invocation, derived-identity purity, every interception refusal surviving the run with its code durable in session memory — including an unbounded catalog refusal truncated at the recording door — goal tool narrowing, and a crash sweep at every run-store write converging on one cell, one identity, one send — the parent half of scenario 28), `tests/delegation_dispatch.rs` (absent executor fails closed; a peer conflict settles `Conflicted` and winds the parent down; a self-checking crash sweep proving a cancellation that catches the send committed-but-unsent fences it and settles its cell in the same transition, and that a terminal parent leaves no cell pending), the coordination-tool authority pin in `tools.rs`, the widest-provenance bound, lineage-refusal, oversized-provenance, and cross-tenant creation-refusal pins in `tests/task_bounded_state.rs`, the `A2aPeer` classification pin in the substrate's dispatcher tests, and `rakka-a2a`'s new `tests/collaboration_surface.rs` — scenario 39 end to end (one child task and run under the specialist, provenance and projection echo recorded, the parent task byte-equal in identity and ownership), scenario 28's A2A half (replayed sends converge on one child; a conflicting delegation under the same key is answered with the original child whose echo exposes the conflict; distinct delegations create distinct children; an aged-out deduplication window converges a replay on its own child instead of a false conflict), the fail-closed version matrix, the forged-parent-bindings refusals (incoherent depth, cross-tenant parent run), plain-client compatibility, and credential-reference hygiene; the whole M3, 4.1, and 4.2 suites pass unchanged.
- `rakka-agent` durable fan-out/fan-in, delegation lineage, and coordinator limits (slice 4.4; specifications 8.4, 8.7, and 9.7; scenarios 27 and 34):
  - The one durable fan-out group per run: the delegation cells *are* the membership, and the new `AgentFanInCell` on the loop state opens in the same compare-and-set as the first committed delegation, with an `AgentFanInPolicy` (`All`/`Any`/`Quorum{n}`, `#[non_exhaustive]` for the deferred policy-evaluator variant) taken from trusted state — the goal spec's new `fan_in` declaration or the wiring's default, never model output — so the fan-in rule is durably fixed before any child can exist, let alone report. The model chooses only *when* to wait, through the second declared coordination verb (`AgentRunDelegationConfig::with_fan_in_tool`, closed `AgentFanInToolCall` vocabulary: an optional deadline and nothing else); the await close freezes membership, and the run rests in the new `AwaitingChildren` loop phase — status `Running`, the honest non-residency status: no outstanding effect, no resident task, the entity passivates. `evaluate_fan_in` is a pure function of the durable cells, the policy, and the parent-side timeout marks, so arrival order can never change what a given durable state resolves to; the resolution is absorbing, and its bounded result table (delegation, disposition, terminal status, truncated reason code, result digest — never child content, and no repeated child task ids: the send receipts already carried them) becomes the awaiting call's tool result, evidence the resumed model synthesizes and proposes from — a resolved fan-in never completes the parent task and never satisfies the goal (specifications 8.3 and 14.4). `FireFanInDeadline` is the durable timer command: past the closed group's deadline it marks the still-unresolved members `timed_out` — a parent-side disposition, never a forged child result — and resolves; early, duplicate, and late deliveries all converge. `rakka.agent.fan_in.resolutions` counts every resolution under its bounded code, whichever path produced it — a child's result, the deadline timer, a member send that failed definitively, or an await that closed over members which had all already settled — measured across the whole entity operation, because the transitions that resolve hold no metrics handle. A definitively failed or conflicted send is now a fan-in disposition rather than a coordinator failure: the cell settles as before, the failure reaches the model as the call's failed tool result, and the coordinator survives to finish with the children it has — the 4.3 wind-down applies only to a send outside any fan-out group. Group membership alone is the test, never a still-unresolved group: an `Any` that its first child satisfied while a sibling's send was still in flight has already decided what it waited for, and that straggler's later failure must not wind it down.
  - The child's result returns through the ninth exchange, `AgentExchangeKind::DelegationResult`: a terminal delegated child task — its provenance is the return address — owes exactly one `AgentDelegationReport` (child task/run, terminal status and reason code, result digest, descendants created; references only) to its parent run, from the same transition that closed its ledger, under the pure `delegation_result_operation_id(tenant, delegation)`; the courier delivers it, sharding re-activates the owner wherever it lives, and the parent's accept — sender fenced to the very child the cell created, first-writer-wins on the cell's new `result` field, the durable fence a replay past the journal's bounded window converges on — records the outcome, runs the deterministic fan-in step, and completes the parked wait only on a run still going somewhere: a terminal or winding-down parent records the evidence and resumes nothing. The child settles only on the parent's definitive answers (`delegation-result-unknown-run`/`-unknown-delegation`/`-forged`/`-not-owned`); a receipt race (`delegation-result-early`), an undecodable payload, or a pre-4.4 owner leaves the exchange outstanding for re-drive. The full specification 9.8 failure-window table is on the kind's rustdoc. The in-fabric exchange is the approved carrier for this slice — the remote A2A result envelope ships with federation, and the wire shape is reserved — recorded as the 4.4 amendment's divergence note against specification 8.4's letter.
  - Coordinator limits, split by conservation exactly as the ledger's own category rule demands: `AgentBudgetDimension::Descendants` is the eighth conserved dimension — seeded at task creation from the tightest of the goal spec's `max_descendants`, the new definition-side `AgentTaskDefinition::delegation` ceilings (the one cap a forged or inflated provenance cannot escape), and the spec's own allocation; escrowed task → run through the existing `open_child` narrowing; spent one-plus-sub-quota per committed delegation against the durable cells (`Failed`/`Conflicted` sends release their debit; a `Pending` send always counts — deny-when-unknown); folded into the run's consumption exactly once at terminal so the existing settlement/return exchanges carry it up and a replacement generation is escrowed only the remainder. The new `WORK` dimension set keeps `first_empty_for` from refusing assignments over a zero descendants grant: an empty descendants dimension disables delegation, never work, and its exhaustion is a failed tool result (`delegation-descendants-exhausted`), never a park. Depth, fan-out, and concurrency stay at the delegation door as envelope- and cell-derived checks (`delegation-depth-exceeded`, `delegation-fan-out-exceeded`, `delegation-concurrency-exceeded` — `max_concurrent` re-documented as concurrently unsettled *direct children of one run*: a tree-global level is unenforceable by single-entity transitions, and descendants bounds tree-global mass), priced per planned call so a multi-call turn cannot overshoot. The deterministic even-split sub-quota (`granted_descendants`, recorded on the record; replays reuse it verbatim) crosses A2A as the child's narrowed `max_descendants` inside the now cap-bearing (never conserved) `AgentCollaborationBudget`, and the child's own creation min-narrows its ledger seed below it — a peer can only shrink a child's authority. Unused sub-quota is deliberately not credited back on the child's terminal result yet: `descendants_created` is recorded on the cell for a later slice to credit inside the same ingestion transition.
  - Lineage-based cycle rejection at agent-identity granularity: the new `ancestors` chain — the agent that committed each lineage entry, parallel entry for entry, closed by the delegating parent riding as `parent_run` — is carried on the record, the provenance, the assignment envelope, and the collaboration metadata (omitted when empty, so a root-level send still parses on a v1-strict receiver; a deeper chain fails closed cross-version, the extension's documented posture), validated for coherence at every door (`delegation-ancestry-incoherent`: a gap a peer could hide an ancestor in never becomes durable provenance), and compared against the resolved catalog target inside the committing transition (`delegation-cycle-detected` — the run's own agent always in the comparison set, so self-delegation refuses). A chain that carries lineage without its ancestor agents may finish its own work but not extend the chain (`delegation-ancestry-unknown`); the bounded-iterative escape hatch of specification 8.4 is deferred with this refusal-only default. Fan-in quorums that can never resolve refuse at the goal-spec and wiring doors (`fan-in-quorum-invalid`); the growth-reserve empirical test now measures the fan-in working set against the reserve and holds the raised `AGENT_DELEGATION_COMMIT_OVERHEAD_BYTES` (4 KiB) to its claim that the door's price covers the whole delegation lifecycle, including the later-arriving child result and resolution-table share.
  - The review's minor findings, closed in a follow-up pass: the model-supplied await deadline is the one model value that reaches durable timer state, so it now fails closed like every other model input — a deadline not in the future refuses `fan-in-invalid-arguments` before it can arm a timer that would mark every child timed out unheard (the trusted envelope bound stays exempt); a direct `settle_side_effects` sweep now counts its fan-in resolution exactly as `apply` and `accept` do (the inner pass carries no sampling, so a command's own settle cannot double-count); `FireFanInDeadline`'s rustdoc now states plainly that **the hosting application owes the scheduler** — nothing in the crate delivers it, the deadline is readable from the parked group, and an `All` wait with a lost child parks until it fires or 4.6's chase lands; the goal door's quorum bound now calls `AgentFanInPolicy::validate` instead of restating it, and `AgentFanInResolution::satisfied_by` documents that it is evidence whose contents may vary with arrival timing while the decision cannot. The coverage the review found thin is now pinned: `Quorum` through the run entity, the late result racing a fired deadline against the absorbing resolution, the uneven sub-quota split re-priced per slot (`⌊9/3⌋` then `⌊5/2⌋`), a conflicting duplicate result against the first-writer fence, the forged-report refusals under their exact definitive codes, a wound-down parent pinned by phase and effects rather than status alone, the duplicate deadline fire counting no second resolution, the pre-4.4 run state decoding without its fan-out fields, and the wire envelope without the `ancestors` key decoding into an empty chain.
  - Four review findings closed on the slice's edges. A child task that terminates by exhausting its assignments now owes its delegation result from the terminating transition — and every run-refused generation releases its escrow at its settle (the run accepted nothing, so nothing was consumed), which is both the headroom later generations deserve and the closed-ledger gate the owed report reads its final consumption through; before, an `All`-policy parent with no deadline parked forever over a child that had definitively failed. A delegation the model plans *after* the same turn's await verb is refused `delegation-after-await` instead of silently committing as a member no await can cover (whose definitive send failure would have revived the superseded unconditional wind-down). A task with neither a goal record nor delegation provenance — an epoch task, a plain agent-owned task — now carries a definition-only envelope, so `AgentTaskDefinition::delegation`'s depth/fan-out/concurrency ceilings actually enforce at its runs' delegation door instead of enforcing nothing for the tasks with no other authority source. And a creation carrying both a goal spec and delegation provenance refuses (`task-delegation-provenance-invalid`): a delegated child that instituted its own goal would re-root the delegation envelope — empty ancestors, depth zero — voiding the parent's ceilings and the cycle set.
  - Proven by the new `tests/fan_out_fan_in.rs` (the park with no outstanding effect, single resumption under `All`, `Any`'s quiet straggler, idempotent duplicates, forged/unknown refusals, the wound-down parent recording evidence without resuming, the deadline's timed-out resolution converging under duplicate fires, the failed-send disposition, the no-children refusal, the child-side settle-rule matrix, and real child task entities returning results through the exchange fabric with a re-driven settle converging), the new `tests/delegation_limits.rs` (each of scenario 34's six limit classes failing closed with its stable code — the counts re-derived from durable state on the per-command restarts the fixture already is — the wire sub-quota and terminal fold, the zero-grant assignment that still completes, deep-chain depth, ancestor and self cycles, unaccounted and incoherent ancestry, and the definition ceiling capping a forged root grant), the pre-4.4 decode pin in `tests/delegation_record.rs`, the ancestors round-trip/omission and forged-ancestry ingress refusals in `rakka-a2a`'s `tests/collaboration_surface.rs`, and the fan-in policy tables, arrival-order invariance, and quorum-bound units in `fan_in.rs`; the whole M3, 4.1, 4.2, and 4.3 suites pass with the one deliberate amendment above (`a_peer_conflict_settles_the_cell_and_the_coordinator_survives`).
- `rakka-agent` workflows as tools (slice 4.5; specifications 8.6 and 11.7; scenario 32; open decision 16 resolved):
  - The versioned `AgentWorkflowToolDescriptor` (workflow tool id, monotonic version, pinned workflow type and definition version, input/output schemas, bounded model-facing description and parameters, required capabilities, logical credential binding, declared *contained*-effect safety class — the tool's apparent safety is never stronger than the internals it admits — default deadline, declared cancellation/compensation support, and per-invocation checkpoint/authorization gates), wired as trusted deployment data through `AgentRunWorkflowConfig` and `AgentRunEntityStore::with_workflow_tools`. Each configured descriptor appears to the model as its own named tool; the loop intercepts the call by the config map — never the generic tool path, and the dispatch authority's new `workflow-tool-requires-interception` arm refuses a kind-`Workflow` registration that reaches generic dispatch, the `coordination-tool-not-intercepted` precedent. The previously dead placeholders now enforce: `AgentAuthorityEnvelope::workflow_tools` refuses `undeclared-workflow-tool` per dispatch attempt (definition and setup both, so dropping the tool revokes the very next attempt), and `AgentGoalSpec::allowed_workflows` narrows at the planner door (`goal-workflow-not-allowed`), carried on the assignment's delegation envelope like the tool set.
  - Create-or-adopt is an identity property, not a protocol: the interception commits — in one compare-and-set, strictly before any dispatch — the `AgentWorkflowInvocationRecord` (goal/parent-task/parent-run linkage, the descriptor's version, digest, and pinned workflow type/version copied at commit so a replay never re-resolves and a mid-flight descriptor upgrade is a visible conflict, bounded input, min-narrowed deadline, revisions, trace context), its cell, its fan-in membership, and the new `AgentRunEffectKind::WorkflowStartCall` effect. The derived `workflow_invocation_id_for(scope, turn, slot)` (the delegation-id digest construction, disjoint `workflow-invocation-` prefix) *is* the child workflow run id and the `StartRun` deduplication key, and the command id (`{invocation}#start-run`) is deliberately generation-free — an operator-reconciled new effect generation re-derives the identical `StartRun`, so recovery can never mint a second child run. The effect is the start, never the workflow: idempotent by construction (`AGENT_WORKFLOW_START_DEFAULT_MAX_ATTEMPTS = 3`), completed at the durable start receipt, while the child's internal effects keep their own durable boundaries in its own run. The application-owed `AgentWorkflowStartExecutor` (fail-closed `workflow-start-executor-missing`) delivers the `workflow_start_command`-built command to the child run's own durable inbox: acceptance is `Started`, duplicate acceptance is `Adopted` (byte-identical receipts, derived from the record and never the acceptance), and a deduplication-key match under a foreign command id — or an existing run whose pinned fields disagree — is a `Conflict`, never an adopt. A mismatched receipt settles the cell `Failed { workflow-start-run-mismatch }` rather than grafting a foreign child onto the invocation.
  - Workflow members reuse the 4.4 member-disposition interface, the fan-in obligation 4.4 recorded: `AgentFanInMemberId` widens the group's members, timeout marks, and `satisfied_by` across the delegation and workflow-invocation cell maps — on the wire a member stays the raw derived id string, distinguished by prefix, so a delegation-only 4.4 group round-trips byte-identically and a pre-4.5 node reading a mixed group parks deny-when-unknown instead of resolving early — the load-bearing cross-version fence, since a turn's effects clear when it records; while they are retained, the state also holds the new `workflow-start` request variant, which a pre-4.5 binary refuses loudly as unknown-variant (no schema-version bump, the 4.4 additive-field posture). The invocation joins the run's one group in the commit compare-and-set, `AwaitingChildren` is reused unchanged, `workflow-after-await` mirrors `delegation-after-await`, the combined membership is bounded at both doors (`AGENT_RUN_MAX_WORKFLOW_INVOCATIONS = 16`; the sixteen-row result table's growth reserve spans both kinds, priced by the shared headroom accumulator as `workflow-invocation-headroom-exceeded`), a definitively failed or conflicted start is a fan-in disposition the coordinator survives, wind-down fences settle a provably unsent start's cell `run-winding-down` on every fence path, and `FireFanInDeadline` covers workflow members for free. Deliberate decisions: a workflow invocation never debits the conserved `Descendants` dimension (a 4.5 workflow child cannot create agent tasks; `descendants_created` is recorded on the result for a later credit fold) and never counts against `max_concurrent` (a delegation-envelope ceiling; the membership bound is the workflow cap until a descriptor-level ceiling exists).
  - The child's terminal outcome returns as the new deduplicated `AgentRunEntityCommand::RecordWorkflowResult` — an entity command, deliberately not a tenth exchange: a workflow run is not a choreography participant, and wrapping it in an `AgentEntityAddress` would forge the identity the sender fences rest on. **The hosting application owes the relay**, the `FireFanInDeadline` obligation idiom: it observes the child's durable terminal state and constructs the command with `AgentRunEntityCommand::record_workflow_result`, whose `workflow_result_operation_id(tenant, invocation)` is pure so every re-drive owes the identical operation. Refusals are non-committing errors under stable codes (`workflow-result-unknown-run`/`-unknown-invocation`/`-forged`/`-not-owned`; a non-terminal status is unrepresentable — `AgentWorkflowTerminalStatus` is closed), duplicates answer from the journal and the cell's first-writer-wins result behind it, a terminal or winding-down parent records evidence and resumes nothing, and there is deliberately no early window — the child's identity is derived at commit, so a result arriving before the start receipt authenticates against the record and records first-writer-wins, the receipt settling the effect independently. `rakka.agent.workflow.results` counts accepted results.
  - The autonomy classifier gap is closed in `rakka-agent-workflow`: `AgentAutonomyTargetClass::ChildWorkflow` is first-class — `from_dispatch_class` no longer lumps `AgentDispatchTargetClass::ChildWorkflow` into the fail-closed `Other`, `from_label` parses `child-workflow` (closing the asymmetry with the dispatcher and the Postgres index), the Phase 5 catalog admits `ChildWorkflowCommand` under the `DeduplicationKey` idempotency expectation (the outbox dedup key *is* the derived invocation identity the child inbox dedupes on) with artifact-preferred payload policy, the Phase 5 default policy allows the class without a blanket approval requirement (agent-side descriptor gating owns approvals), and the dispatcher concurrency limits seed it. Registering a `ChildWorkflow` dispatcher and the compiled-plan node remain out of scope — an unregistered class still fails closed. The `VerificationWorkflow` evaluation method deliberately stays refused (`run-goal-evaluation-workflow-deferred`): executing it honestly means bridging the evaluation cell to this invocation path, recorded follow-up work.
  - Proven by the new `tests/workflow_tool.rs`: the one-compare-and-set commit with every derived identity, the scenario-32 crash sweep (every executor sighting across every owner-loss window carries one invocation, child run, and `StartRun` command id), the end-to-end adopt over a *real* child `AgentRunInbox` (a replayed invocation deduplicates in the child's own durable inbox — before and after the child completes its one internal step exactly once — and the relayed result resumes the parent to completion), the duplicate/conflicting result converging on the journal and the first writer, the exact forged-result codes with refusals proven non-committing, the failed/conflicted/unwired start as surviving fan-in dispositions, the early result recording first-writer-wins with no early window, the wound-down parent recording evidence without resuming, the after-await refusal, the goal-scope narrowing, the mixed delegation-and-workflow group resolving over both member kinds, and the pre-4.5 run-state decode pin with the `workflow-start` wire-tag pin; plus the member-id round-trip/prefix-decode and mixed-group units in `fan_in.rs`, the derived-identity and descriptor-bound units in `workflow_tool.rs`, and the first-class classifier pins in `rakka-agent-workflow`.
  - The review's findings, closed in a follow-up pass: the descriptor's `required_capabilities` — previously declared but enforced nowhere — is now copied onto every invocation record at commit and carried on the dispatch grant per attempt, the regular tool binding's discipline (the definition envelope declares workflow tools by id only, so the per-tool capability *subset* check awaits an envelope-side declaration, recorded as owed work); the `Conflicted`-vs-`Failed` cell settlement no longer hinges on an executor string convention — the dispatch layer normalizes every `AgentWorkflowStartFinding::Conflict` onto the exported canonical `AGENT_WORKFLOW_INVOCATION_CONFLICT_CODE`, folding the executor's own code into the failure message, and the conflicted test now proves a foreign conflict code still settles `Conflicted`; the code-surface drift closed — the planner's workflow refusals source their codes from `AgentWorkflowToolError` instead of restating literals, the never-constructed `ReasonTooLong` variant and `AGENT_WORKFLOW_RESULT_REASON_MAX_BYTES` are gone (the recorded terminal reason truncates at the run's uniform `AGENT_RUN_DETAIL_MAX_LENGTH`, documented where the command and result state it), and `AGENT_RUN_MAX_WORKFLOW_INVOCATIONS` is defined as `AGENT_RUN_MAX_DELEGATIONS` rather than restating sixteen, so the bounds move together. The two coverage gaps are pinned: `undeclared-workflow-tool` refuses at the *real* dispatch pipeline's gate — and the same shape with the envelope declaring the tool passes the door, failing only on the deliberately unwired executor — and a forged start receipt naming a foreign child run settles the cell `workflow-start-run-mismatch` while the coordinator survives it as a fan-in disposition.

- `rakka-agent` cancellation propagation and shared environment (slice 4.6; specifications 8.5 and 8.7; scenarios 29, 31, and 33):
  - Durable cancellation propagation across the delegation and workflow trees. A goal's terminal decision in the cancel or expiry family — an operator cancel, a retirement, a passed deadline observed by the settle pass — now records a nonterminal `AgentTaskCancellation` marker on the root task (never an instant terminal), decides the goal and closes admission at *request* time, fences new assignment and result proposals (`task-cancel-requested`), and propagates as durable exchanges: the new `RunCancel` exchange (task → accepted run, owed only after durable acceptance so a cancellation can never definitively refuse ahead of an in-flight assignment) and the new `DelegationCancel` exchange (parent run → each created, unsettled child task, in-fabric like `DelegationResult`, with the A2A carrier still reserved for federation). The child records its own marker and recurses in the same compare-and-set. The task finalizes terminal `Cancelled` only when its escrow ledger closes — budget settlement travels only after a known terminal run outcome, so ledger closure is durable proof of quiescence — and a winding-down run's own quiescence now spans its subtree (`AgentLoopState::awaits_children`): the parent stays `Cancelling` until every delegation and workflow cell holds a terminal outcome, with a child parked in its own reconciliation deliberately holding the whole ancestry nonterminal. Fixes the pre-4.6 gap where `AgentTaskEntityCommand::Cancel` terminalized instantly over a run holding an indeterminate effect.
  - The workflow leg: the new `WorkflowCancelCall` effect delivers the derived, generation-free `CancelRun` command (`workflow_cancel_command`, `"{invocation}#cancel-run"`) to the child workflow run's durable inbox, committed by the wind-down only under the descriptor's declared `supports_cancellation` — otherwise the invocation cell records a durable `Unsupported` disposition and the parent waits for the child's natural result — priced like a compensation (`Unaffordable` disposition, never a blocked wind-down), executed through the new application-owed `AgentWorkflowCancelExecutor`, and exempted (with `CompensationCall`, whose missing exemption at the dispatcher claim fence this fixes) from the wind-down dispatch fences.
  - The straggler chase: a resolved fan-in group's unresolved members — a fired deadline's timed-out stragglers, or the children an early `Any`/`Quorum` satisfaction left behind — owe their delegation-cancel exchanges and workflow-cancel effects from the same pure owed-derivation the wind-down uses; owed cancels never touch the parent's terminal reason, so a chase can never wind a satisfied coordinator down. `AgentCancellationProgress::Propagating` is now derivable (a cancelled scope with created, unsettled children), the task-level `derive_task` and the operational snapshot's `cancellation` field project the progress, and the A2A projection's "a request alone never projects CANCELED" now holds by construction. Per-agent settings revocation stays pull-at-dispatch — already immediate for every agent's own dispatches — and agent lifecycle fan-out, the dependents-registry sending half (slice 5.4), and descendant credit-back stay recorded deferrals.
  - The shared-environment contract (specification 8.5): `AgentToolDeclaration.environments` declares the `AgentEnvironmentRef` set a tool observes or mutates ("observe" *is* the `ReadOnly` safety class — no second mode axis), the new `AgentEnvironmentConcurrencyProtocol` on the binding declares the external coordination class a mutating adapter applies (no fail-open variant; required at `AgentToolRegistry::register` exactly when a mutating tool names an environment, and an `Environment`-kind descriptor must name one), and the tool authority enforces the references per attempt — binding ⊆ declaration ⊆ definition envelope, narrowed by the setup (`setup-excludes-environment`) and by the goal's scope (`goal-environment-not-allowed`), which now reaches the authority through the run's delegation envelope. The goal spec's `environments`/`knowledge_spaces` project into `AgentRunDelegationEnvelope`: environments as a narrowing (empty = none), knowledge spaces as a fail-closed grant (`Option` — a chain with no grant statement refuses communal writes, the ancestry-gap posture), carried through the delegation record, the child's provenance, and the A2A collaboration metadata (skip-if-empty, the `ancestors` wire-compat pattern), with the catalog's explicit `AgentDelegationTarget.knowledge_spaces` statement intersected against the parent's own grant so delegation can never widen it.
  - The run-driven communal claim append (scenario 33): the deduplicated `AgentRunEntityCommand::AppendClaim` commits one idempotent `ClaimAppendCall` effect whose `AgentClaimAppendProvenance` — agent, goal, task, run, delegation — is stamped from durable run identity by the committing transition, never command input, and whose space is validated at the door against the run's delegated grant (`run-claim-space-not-delegated`) and per attempt against the definition/setup envelopes. The new `AgentClaimAppendExecutor` fails closed when unwired; `rakka-agent-knowledge-graph` ships the graph-backed `KnowledgeGraphClaimAppendExecutor`, deriving the store's append operation from the intent's external idempotency key so every attempt of a generation converges on one claim. Proven by the new `concurrent_specialist_append_provenance` conformance clause (both backends), a genuinely racing two-connection append proof in the PostgreSQL backend suite, and `tests/communal_claim_append.rs` (the stamp, the fail-closed doors, command replay convergence, and the delegated specialist's grant with its delegation identity stamped).
  - Proven by the new `tests/cancellation_propagation.rs`: the scenario-31 spine end to end over real child task entities (root cancel → deferred root task → run-cancel → per-child delegation-cancel → recursive child finalization → terminal reports → parent quiescence → root finalization), with the send-executor invocation log pinning scenario 29's at-most-once half across every restart-shaped re-drive; the forged/unassigned/stale/not-delegated receiver fences; the settle-pass deadline-expiry propagation; and the `Any`-resolution straggler chase. Existing suites extended where the honest semantics changed: a cancelled root with an accepted run now defers (`goal_contract.rs`), and a wound-down parent's last child result completes its quiescence (`workflow_tool.rs`).
  - Review findings closed in a follow-up pass. Four were liveness holes in the new quiescence machinery: a fired fan-in deadline chased *nobody*, because it marks its stragglers timed out before resolving and the chase read the now-empty unresolved set — the chase set is now `fan_in::unreported_members` (unresolved **or** timed out), which is the honest question "which members did the group stop waiting for without a terminal report"; a definitively-refused delegation-cancel (`delegation-cancel-forged`/`-not-delegated`, both proving the addressed task will never report to this run) now releases its cell from `awaits_children` and re-checks the disposition on the spot, where before it held a cancelled parent in `Cancelling` forever with its escrow never closing; `commit_workflow_cancels` no longer commits on a terminal run (an effect that could never dispatch under a cell claiming it was sent) and stops at the per-run outstanding-effect bound instead of overflowing it, which used to abort the whole transition and re-abort identically on every later pass; and a re-entered wind-down no longer fences the work the first one authorized — `fence_unsent_effects` applies the same `exempt_from_wind_down_fence` rule the flush and claim paths do, and a run-cancel arriving at an already-winding-down run answers idempotently instead of re-running the wind-down.
  - Two ingresses still terminalized over a live run, the exact pre-4.6 gap this slice closes elsewhere: a failed dependency under the default `CancelDependents` policy now takes `request_task_cancellation` (deferring while the run winds down, owing the run-cancel, finalizing on escrow closure), and a proposal refused with the new `task-cancel-requested` code — exported as `AGENT_TASK_REFUSAL_CANCEL_REQUESTED`, the `AGENT_TASK_REFUSAL_STALE_GENERATION` precedent — now takes the run's wind-down path rather than terminalizing it `Failed` (the task's status is deliberately nonterminal at that point, so the old status-based mapping no longer read `Cancelled`) and skipping the subtree chase.
  - Also closed: `AgentCancellationProgress::derive_task` consulted only the assignment, so a cancelled continuous root between epoch assignments reported `Quiesced` while its admitted epochs were still executing — the snapshot now projects `outstanding_escrow`, the actual finalization gate, and the derivation reads it; `AgentClaimAppendRequest::validate` gained the `confidence_bps <= 10_000` range check its sibling bounds imply, so an out-of-range claim is refused at the door instead of after reserving the run's attempts; and the `RecordWorkflowResult` relay is documented as load-bearing for cancellation — a workflow-invoking deployment that does not wire it cannot complete a cancellation, which is specification 8.7's own posture (the parent does not know whether the child stopped), with delivering the command being equally the "explicit reconciliation decision" 8.7 names as the way out.
  - Coverage the review found missing is now pinned: the per-attempt environment and knowledge-space authority doors (binding ⊆ declaration ⊆ definition envelope, `setup-excludes-environment`, `goal-environment-not-allowed`, `setup-excludes-knowledge-space`, `claim-space-not-delegated` including the fail-closed no-grant-statement chain) in `tools.rs`; the graph-backed `KnowledgeGraphClaimAppendExecutor` end to end against a real store in the new `tests/claim_append_executor.rs` (provenance round-trip including the stamped effect, retry convergence on one claim, a fresh claim per generation and per slot, and a definitive store refusal arriving as a finding rather than a retryable error); the widest-provenance record proof with both new scope sets at their maximum; the delegation door-price proof with the cell's cancel outcome included; the fired-deadline chase; and the dependency deferral.

- `rakka-agent` M4 acceptance and goal views (slice 4.7; specification 17.18; the multi-agent goal milestone):
  - The authorized goal view: `assemble_agent_goal_view` assembles one goal's root and specialist tasks and runs, delegation and fan-in graph, workflow links, evaluations and their classed evidence, budgets, and cancellation state from durable state alone — one store read per record, no entity activation, correct while everything is passivated and telemetry is down. Resolution rests on the recorded open-decision-14 default (the goal identity is the root task's value); traversal is breadth-first over the durable delegation cells' `ChildCreated` edges plus a continuous root's admitted epochs, with linkage failing closed: a child whose provenance does not name the traversing delegation, or whose goal binding differs, becomes an explicit `AgentGoalViewOmission` (`unlinked-provenance`, `foreign-goal`, `record-missing`, `schema-unsupported`, `node-budget-exhausted`) rather than a joined forgery or a failed view — only the root record failing fails the call. The view is a causal cut over independently committed records, never a snapshot: every node carries its own durable revision, `root_revision` is the only fence-able anchor, and the docs state it must never authorize or advance execution. Bounded at `AGENT_GOAL_VIEW_MAX_TASKS` (64) with truncate-with-marker semantics and a tighter-budget variant (`assemble_agent_goal_view_bounded`). Each task resolves its highest-generation run — through the standing assignment, or re-derived from the assignee after result acceptance cleared it, a gap the acceptance walk itself exposed (a completed goal lost its whole tree); earlier generations stay an explicit gap the per-node generation counts surface, owned by the task projection.
  - Authorization is the owner check the goal record itself can answer: `authorized_agent_goal_view` compares the caller to `AgentGoalSpec.owner` and a non-owner receives `Ok(None)` — byte-identical to a missing goal, proven byte-for-byte against the absent-goal and child-task-id-probe answers, so the view never becomes an existence oracle. The principal-free core stays the composition point a boundary authorizer fronts, exactly as the A2A service authorizes its operations; an A2A wire surface for the view is recorded follow-up work.
  - The slices' recorded snapshot-view debt is paid by one shared derivation: `AgentRunCollaborationView` — delegation edge views (identities, ceilings, and reference digests; the delegated input and credential/capability references deliberately never ride it), the fan-in group with its `unreported_members` chase set, workflow-invocation views, the goal-evaluation view, and retained claim-append effects — carried by both `AgentOperationalSnapshot` (a serde-defaulted field; pre-collaboration snapshots serialize unchanged) and the goal view's run nodes, so the two surfaces can never disagree about what a cell holds.
  - Shared-knowledge references join through the new `AgentGoalClaimSource` port (settled claim receipts are pruned from durable run state when their turn completes, so the communal graph is the one complete enumeration): `rakka-agent-knowledge-graph` ships the store-backed `KnowledgeGraphGoalClaimSource` over `ClaimFilter::with_goal` across explicitly named spaces, and an absent or failing source degrades `claims_available` honestly — the durable half of the view is never degraded (the scenario-56 posture).
  - The sharded run factory closed a production gap the milestone example was the first deployment-shaped consumer of: `AgentRunEntityShardingSettings::with_delegation`/`::with_workflow_tools` now plumb the coordination and workflow-tool interception onto every hosted run — before this, no sharded deployment could serve either — and the testkit's `InProcessRunResultDelivery` gained the matching `with_delegation`, since the delivered model result is where a fan-out turn is intercepted.
  - The milestone's done-when is `examples/multi-agent-goal-acceptance`: an 18-line transcript pinned three ways (README, `EXPECTED_TRANSCRIPT`, `tests/acceptance.rs`) walking the multi-agent checklist end to end — three sharded agents; a goal-bearing root whose one fan-out turn commits two delegation records, one workflow invocation, and a closed three-member group; real children created through the in-process `rakka-a2a` service core with a replayed send converging on the deduplication key; the derived `StartRun` accepted into a compiled refund workflow's real durable inbox (registry-validated pins, invocation id = child run id = deduplication key) and a post-loss replay adopting the same child with the compiled step executed exactly once; the wait fully passivated (0 resident entities); root pod loss (a killed result write redelivered by the child's re-driven settle) and child pod loss (a non-idempotent payment invoked exactly once, parked Indeterminate, resolved by a deduplicated reconciliation decision); an attributable communal claim with goal/task/run/delegation provenance stamped; `Satisfied` only through the configured evaluator over durable evidence after a direct criteria decision refused `task-goal-decision-unattested`; and the authorized goal view reconstructing 3 tasks, 3 runs, 2 delegations, 1 workflow link, the evaluation, its evidence, and the claim — with planted content sentinels appearing in no queried surface. Wiring discoveries the walk enforced: the definition envelope must declare a workflow tool for its invocation to commit, and a specialist's envelope must grant a knowledge space for its claim append to dispatch (`widened-knowledge-access` otherwise).
  - Proven by the new `tests/goal_view.rs` (the delegated-tree assembly with workflow links; byte-identical answers across restarts without activation; the content-sentinel sweep; non-owner ≡ missing ≡ probed-child answers; node-budget truncation markers; partial children as edges-plus-omissions never failures; a failed send as an edge without a node; root schema failures closing the call while run schema failures omit; a continuous root's epochs joining as `is_epoch` nodes while their occurrences are active and leaving when released; the evaluation cell and terminal decision surfacing whole; and the claim join with its degraded half), the operational snapshot's collaboration assertion, and `rakka-agent-knowledge-graph`'s new `tests/goal_claim_source.rs` (goal-scoped provenance-intact joins, paging under the bound, unnamed spaces not joined). Deferred onward, recorded: the A2A/typed-client wire surface for the goal view, crash-point sweeps over the 4.6 task-cancellation compare-and-sets (Phase 6.1), earlier-generation run assembly, and the M5 team/conversation dimensions the non-exhaustive views keep additive.
  - Review findings closed in a follow-up pass. The deny-is-absent contract now truly never leaks: the owner fence in `authorized_agent_goal_view` runs before the root schema gate, deciding from the already-decoded owner field, so a root record unreadable under the caller's policy answers a non-owner `Ok(None)` instead of a distinguishable schema error. Run-join failures moved off the omissions list onto the assembled task node itself — `AgentGoalTaskNode::run_omission` (`run-record-missing`, `run-not-accepted`, and the new `run-schema-unsupported`, no longer aliasing the task-record code) — so an omission never names a task the view did assemble, and a failed run join visibly means the node's delegated subtree is unknown rather than absent; the omissions list is now a set (the budget drain checks `visited`, and a task that later joins through a second edge withdraws its earlier omission). Run re-derivation is gated on `last_refusal`: deciding a generation clears it, so a standing refusal proves the highest generation never produced an accepted run record, and a settled goal whose last generation was refused no longer reads `run-record-missing`/`run-not-accepted` anomalies over healthy state. The claim join is honest about its own bounds: `claims_truncated` marks a list cut at `AGENT_GOAL_VIEW_MAX_CLAIMS` (the source is asked for one reference beyond the cap), `claims_error_code` carries a failing source's stable code — never its free text — where `Err(_)` used to discard it, and the multi-space `KnowledgeGraphGoalClaimSource` merges every named space's prefix in the trait's stable claim-id order instead of filling the limit space-by-space in wiring order, so a bounded read can no longer starve a later-added space. Assembly latency: each breadth-first wave's task records, and then its run records, load in one concurrent round-trip each, and the claims join runs concurrently with the traversal — the same nodes, omissions, and truncation one-at-a-time processing produced, without the serial per-record store latency. The hand-rolled consumption fold was replaced by `AgentBudgetConsumption::saturating_add`, so a future conserved dimension cannot silently drop out of `live_consumption`; the acceptance example's 18 transcript lines now have exactly one source (`EXPECTED_TRANSCRIPT`), each assigned only after its asserts prove the line's facts.

- `rakka-agent` coordination capability model and handoff (slice 5.1; specification 8.8, 8.9, and 14.2; scenario 38):
  - The `AgentCoordinationCapability` descriptors (specification 8.8): `coordination.rs` gains the four policy payloads — `AgentHandoffPolicy` (the declared handoff tool and its revision), `AgentDelegationPolicy` (derived from the existing `AgentRunDelegationConfig` by the new `descriptor()`, so trusted config is never duplicated; the catalog stays runtime wiring), and the revision-only `AgentTeamPolicy`/`AgentModerationPolicy` shells their slices fill. Descriptors never join the serialized agent definition: admission keeps enforcing the *kind* set as the existing envelope dimension, and each runtime wiring validates its descriptor against that set at construction — `AgentRunDelegationConfig::with_handoff` refuses a coordination set without `Handoff`, exactly as `new()` refuses one without `Delegation`.
  - Handoff reuses the delegation idioms wholesale: initiation is a model-visible coordination tool the loop intercepts (`AgentHandoffToolCall` — a skill, a bounded reason, and explicit context *references*; unknown fields fail the parse, so model output can never name a target, budget, or scope); `handoff_id_for` derives the identity from the run's `(turn, slot)` coordinate with a domain segment separating it from `delegation_id_for`, and it doubles verbatim as the A2A message id and deduplication key; the `AgentHandoffCell` — a single `Option`, never a map: one unresolved transfer per run, ever — commits on `AgentLoopState` in the same compare-and-set as the `A2aHandoff` send effect (kind `A2aSendCall`, deliberately *not* exempt from the wind-down fence: a fenced unsent send settles its cell `Failed{run-winding-down}`, the fenced delegation send's posture); the target resolves once, inside the committing compare-and-set, through the same `AgentDelegationCatalog` (open decision 6's recorded disposition), and replays never re-resolve. The transfer is the turn's only work: refusals — `handoff-with-planned-calls`, `handoff-already-pending`, `handoff-children-outstanding`, `handoff-with-pending-effects`, `handoff-after-await`, `handoff-target-self`, the skill and headroom doors — are failed tool results the run survives, and the planner never enters the descendants ceilings: the same task is the same node in the tree.
  - The new machinery is same-task transfer (specification 8.9 and 14.2): the A2A send names the *same* `message.task_id` (`ContinueTask`, distinguished from slice 5.4's parked input delivery by the collaboration metadata's new handoff cluster), the deduplicated `AgentTaskEntityCommand::RecordHandoff` validates every wire claim against durable state — the claimed source must be the current *accepted* assignment (`handoff-source-not-current`), the resolved target must serve the task's typed contract (`handoff-contract-mismatch`), bounded by the definition's new `max_handoffs` (default 4, which is also what bounds an A→B→A oscillation; the delegation cycle check is deliberately not copied — returning a task to a previous agent is legitimate) — then stashes the source assignment whole on the new bounded `AgentTask::handoff` provenance (latest hop only; the chain is history, per specification 9.6) and lets the same compare-and-set mint the target's generation. That provenance is load-bearing three ways: it is the deduplication echo past the journal's bounded window (a replayed send converges on the recorded transfer or an explicit conflict), the source address every owed derivation reads after the task record starts naming the target, and the goal view's source-run join.
  - `HandedOff` is strictly after durable target acceptance, through the new twelfth exchange: `AgentExchangeKind::HandoffResult` (task → source run), owed by a pure derivation over the settled provenance — re-derived by every settle pass, with the provenance's `result_settled` marker as the once-guard past the journal ring — whose settle at the source records the call's result and the new `AgentRunTerminalReason::HandedOff` in one compare-and-set (the `HandedOff` run status, defined since M1, finally becomes reachable). Until then the source is fenced by the cell itself — new work refuses `run-handoff-pending`, the loop rests `AwaitingChildren`, and the unresolved transfer holds `settle_run_disposition` open so a racing wind-down cannot terminalize `Cancelled` over a responsibility that durably moved. A refused transfer resolves the other way (the user-approved single-attempt posture): the task restores the stashed source assignment exactly — readiness refusals, an exhausted assignment budget (`handoff-assignments-exhausted`), and the fail-closed escrow refusal (`handoff-budget-unaffordable`: the source's escrow child is still open, so an exact-fit budget cannot afford the target's generation) all resolve through one helper — and the source resumes with the failed tool result. Cancellation routes to exactly one owner: once the target's generation is accepted it takes the run-cancel while the source still terminalizes `HandedOff`; a refusal restores the source into its own wind-down; an unminted pending transfer resolves refused in the cancellation-marker transition itself.
  - The A2A surface: the collaboration extension gains the handoff cluster (`AgentHandoffCollaborationMetadata`, discriminated by shape under the one `io.rakka.collaboration` key and URI; a receiver that predates it — or a cluster field this build does not serve — fails closed, the mandated 14.4 posture, while ordinary clients stay untouched), the ingress derives the operation id under the reserved `AgentOperationKind::Handoff`, and the new `A2AAgentHandoffSendExecutor` carries the persisted record verbatim through the same service core an external caller uses — probing the task's durable echo on an ambiguous failure (the user-approved posture: echo ⇒ recorded, no echo ⇒ definitively unrecorded, an unanswerable probe leaves the attempt retryable and an exhausted budget parks the source indeterminate rather than resuming it beside a possibly-live transfer). The projection echoes the transfer (`handoff`, status, target, minted generation) beside the delegation echo, and no session or private memory can cross by construction: both stores are scope-keyed, and the record carries only bounded context references.
  - View lockstep: the handoff cell joins `AgentRunCollaborationView` (`AgentGoalHandoffView` — identities, the stable status label, codes, and counts only; `is_empty` extended so pre-handoff snapshots still serialize byte-identically) and the goal view's task node (`AgentGoalTaskHandoffView`, carrying the source pair), and the goal view's run re-derivation resolves a mid-transfer task to its recorded *source* run instead of fabricating a scope from the target assignee and the source's generation — generations before the latest handoff stay the explicitly surfaced gap. Task history gains `HandoffInitiated`/`HandoffAccepted`/`HandoffRefused` (specification 14.2's lineage in authorized history), and the run store counts `rakka.agent.handoff.results` by bounded outcome.
  - Proven by the new `tests/handoff_record.rs` (persisted-before-send with the fence held, identity purity, replay convergence on one transfer, the refusal matrix as survivable failed tool results, the full transfer with `HandedOff` strictly after acceptance and the target completing the same task, the restored source on a refused target, and the pre-slice decode/byte-identity sweep), `tests/handoff_cancellation.rs` (the crash-point sweep over the committed-but-unsent fence window, the one-owner cancellation race, and the open disposition under an unresolved transfer), the goal view's handoff join and lockstep assertion, and `rakka-a2a`'s new `tests/handoff_surface.rs` (scenario 38 end to end over the real service core, plus the wire half: replay convergence, forged source claims failing closed against durable state, unknown cluster fields refused, and a task-less handoff refused).
  - Review hardening on the slice, closing a cluster of split-ownership findings. The agents surface's projection sync gains a metadata half: a pre-existing projection whose bounded metadata — assignment agent, condition labels, the collaboration echo — no longer matches the authoritative snapshot is re-snapshotted (status never regressing), so a transfer on an A2A-created task actually surfaces in the echo the sending executor's identity check reads; the executor's post-ambiguity probe now reads the *authoritative* durable task view through an in-process seam instead of `tasks/get` (no principal to be denied over, no projection lag), reports the recorded transfer's minted generation, and classifies a settled previous hop as history (`handoff-not-recorded`) rather than a conflict. The wire arm is gated like the creation path: the transfer authorizes under its own `A2AOperation::RecordHandoff` with the cluster's claimed source/target bound into the authorization request (an authorizer can finally bind the caller to the source run it claims), the named target must resolve through the surface's catalog before anything durable, and the cluster must deduplicate under its own handoff id (`handoff-identity-mismatch`) so one transfer is one durable operation. At the task entity, the deduplication echo is checked before the terminal guard (a re-dispatched send replaying past the journal window converges instead of refusing `task-terminal` and resuming the source beside completed work), the wire claim re-passes the sender-side structural bounds (`handoff-context-invalid`, `handoff-request-too-large`), and the `result_settled` once-guard only settles when the exchange's operation id matches the materialized hop — a late settlement of a replaced hop can no longer quiesce its successor's owed result. At the run, a `ConfirmedNotExecuted` reconciliation on a winding-down run settles handoff and workflow-invocation cells exactly as the fenced-cancellation arm does, so the wind-down can quiesce. And the coordination wiring: `AgentRunDelegationConfig::new` accepts a handoff-only coordination set (the delegation and fan-in doors refuse `delegation-capability-missing` at plan time instead), while `with_handoff`/`with_fan_in_tool` refuse colliding tool ids (`coordination-tool-collision`) the loop's interception order would otherwise silently shadow — `with_fan_in_tool` now returns a `Result`. A follow-up closed the review's two remaining plausible findings. The executor no longer answers *definitively unrecorded* after an ambiguous send: absence at probe time cannot prove the failed write will never land, so the attempt stays retryable (`handoff-unrecorded`), the deduplicated re-send converges — echoing the late-landing write or recording fresh — and an exhausted budget parks the source indeterminate; a store failure the entity facade wraps through the choreography host (`store-error`) is likewise classified ambiguous rather than mapped onto a definitive refusal. And an `Accepted` handoff resolution contradicting a cell that settled under a *local* failure now corrects the record instead of being absorbed (`AgentHandoffCell::correct_accepted`, the one transition allowed to rewrite a settled resolution): a live source terminalizes `HandedOff` instead of spending budget on a task it no longer owns, while an already-terminal run keeps its history and corrects only the cell the collaboration and goal views join on.


- `rakka-agent` team coordination (slice 5.2; specification 8.10 and 17.13; scenario 42):
  - The sharded team entity (`team.rs`, entity type `RakkaAgentTeam`): a stable `AgentTeamId` scope whose durable state is the leader, root goal, embedded trusted `AgentTeamPolicy`, bounded membership with capability scopes, the durable shared task board, and a bounded mediated-message ring — all written in single compare-and-sets over the same `AgentExchangeHost` substrate the task and run entities use, with a deduplicated operation-id inbox (`Duplicate` answers the original outcome), append-only team history through the new `AgentTeamHistoryStore` (in-memory backend; PostgreSQL owed onward), opt-in idle passivation, and lazy expiry: a passed `expires_after_ms` horizon refuses commands purely (`team-expired`) while the settle pass records the durable flip — no timer ever fires for a team. Membership joins/leaves and disband fence on the lifecycle revision with provenance (`team-stale-lifecycle-revision`), the leader is immovable, a member holding an unresolved claim cannot leave, and disband refuses while any arbitration is in flight (`team-disband-claim-pending`) but never cancels active work — the board is data. The message ring is recipient-read (no push), bounded per policy with drop-oldest visibility (`messages_dropped`), and history records identities and codes only, never bodies. Creation and disband are entity commands with no wire carrier: a team is trusted application data.
  - Claim, release, and transfer are atomic board decisions under revision and lease fencing with stable derived identities: every decision bumps the entry's claim epoch (a command carrying an older expectation fails closed, `team-claim-stale-epoch`), the claim id derives from `(team, task, member, epoch)` by the `handoff_id_for` digest construction (`team_claim_id_for`, prefix `team-claim-`), and the lease bounds the claim-*pending* window only — an expired lease admits a steal that supersedes the pending claim, while an activated claim is never stealable regardless of lease (`team-claim-already-owned`). Release is holder-or-leader and pre-acceptance only; transfer is release-plus-claim in one compare-and-set whose task-side arbitration is the superseding claim, with post-acceptance transfer refused (`team-transfer-active-claim` territory belongs to the handoff machinery).
  - **The board never holds ownership** (scenario 42's core): a claim drives `decide_assignment` on the task entity through the new thirteenth exchange `AgentExchangeKind::TeamClaim` (team → task), owed in the same compare-and-set as the board mutation. The task's pure arbitration validates the decision against its own durable record — team provenance (`team-claim-wrong-team`), its new claim-epoch fence closing courier reordering, terminal/cancelling/handoff-pending/accepted-assignment/in-flight-offer guards, and the definition's new `max_team_claims` ceiling (default 4) — records the bounded `AgentTask::team_claim` provenance (the deduplication echo past the journal window, checked before every guard including the terminal one), and sets the assignee; the receiving entity's local-progress pass then mints the one generation, and the assignment-generation fence stays the one-normal-owner guarantee. The outcome returns by the fourteenth exchange `TeamClaimResult` (task → team) — the `HandoffResult` mirror: a pure derivation re-driven by every settle pass, `result_settled` as the once-guard, `Activated` settling the entry to its observational owner echo (generation and run mirror the task's truth, never a second copy) and `Refused` reopening it under the code the members rebase on. The claim gets exactly one assignment-generation attempt (the handoff single-attempt posture): readiness, exhaustion, budget (`team-claim-budget-unaffordable`), run refusal, and a cancellation arriving pre-mint all resolve through one helper that clears the assignee back to the board-pending posture. Board-governed tasks themselves are the existing creation with `AgentTaskCreation::team` and a deferred assignee: an agent-owned creation carrying a team may omit the assignee and wait unassigned — no generation ever mints without one.
  - The A2A surface: the collaboration extension gains the team cluster (`AgentTeamCollaborationMetadata`, the third shape under the one `io.rakka.collaboration` key, discriminated by its `team` field checked before `handoff` — `deny_unknown_fields` makes a two-discriminator payload fail whole; ordinary clients stay untouched) carrying the bounded verb vocabulary claim/release/transfer/post-task/message/join/leave — never create or disband. A team send must not name `message.task_id` (`team-send-names-task`); operation ids derive per verb under `TeamClaim` and the new `AgentOperationKind::TeamMessage`/`TeamOperation`; membership changes require an authenticated principal (the management-write precedent). The command authorizes under its own `A2AOperation::TeamCommand` with the typed `A2ATeamClaim` (verb, member, task, target) bound into the authorization request, applies through the team entity's deduplicated inbox on the service's new team stores, answers with an immediate response message (a board command never creates a task), and surfaces domain refusals — stale epochs above all — as structured `Rejected` payloads the caller rebases on. The public task projection echoes the governing claim (`team`, claim id, status, member, epoch, minted generation) beside the delegation and handoff echoes, through the metadata-synced snapshot path.
  - Observability (specification 17.13): team history kinds for creation/membership/post/claim/release-request/settle/transfer/message/disband/expiry, task history gains `TeamClaimRecorded`/`TeamClaimAccepted`/`TeamClaimRefused`, and the new bounded counter `rakka.agent.team.operations` ({`operation`, `outcome`}; `operation` joins the closed metric-key vocabulary) counts each durable decision once at the entity boundary — duplicates and replays count nothing. The `rakka.agent.team.claim`/`.message` otel span rows stay owed, the 5.1 precedent.
  - Proven by the new `tests/team_board.rs` (creation/dedup, lifecycle fencing, ceilings, the bounded ring with visible drops, disband contracts, lazy expiry, the ordered audit trail), `tests/team_claim_assignment.rs` (scenario 42's core: the claim driving exactly one accepted generation with the board mirroring it; concurrent claims admitting one owner with the loser failing closed; the stale-owner two-shard race on the team store; the stale release/transfer/steal matrix; the refused claim reopening the board through the result; the expired-lease steal superseding pre-acceptance; a late board decision refusing on the task's own fence; the release-races-offer restore; missing/foreign tasks closing entries; replay convergence at the operation log, journal, and provenance layers; and pre-team byte-identity), `tests/team_claim_recovery.rs` (self-covering crash-point sweeps over every team-store and task-store write of the claim round trip, plus the committed-but-unsent window), `tests/team_passivation.rs` (the board surviving to zero resident entities on real sharded actors with the claim activating across passivation; an idle team passivating on its own), the team-operations assertion in `tests/agent_metrics.rs`, and `rakka-a2a`'s new `tests/team_surface.rs` (scenario 42's wire half over the real service core, the fail-closed matrix, per-operation authorization denial with the claim bound in, and the plain-client-untouched posture).
  - Review hardening on the slice, closing the claim/release/steal race cluster the board round trip left open. The task-side refusal `team-claim-assignment-inflight` joins the team's definitive settle set with a reopening consequence, so an expired-lease steal refused over a still-undecided offer settles and reopens the entry instead of livelocking the exchange with the entry stuck pending. The superseded-activation owner echo now fills the entry whatever interim shape the board holds — pending under the doomed steal's claim, releasing, or reopened — not only the already-owned settle's `Active`-with-no-claim shape, and every claim-restoring settle consequence is guarded on the claim it names still being the entry's current one, so a late refusal can never clobber a filled echo; delivery order between the steal's refusal and the owner's activation no longer decides whether the board can ever show the owner. `release_team_claim` refuses an accepted claim as `team-claim-already-owned` *before* the settled-claim dedup echo, so a release racing the acceptance marks the entry owned instead of reopening a durably in-progress task to burn its remaining `max_team_claims` on guaranteed refusals. The definitive `team-release-unknown` settle restores the entry to pending — the release outran its own claim exchange, which the courier does not order — instead of wedging it in `Releasing` forever. A full board evicts its `Done` entries before refusing a post, so a long-lived team is never permanently exhausted by its own finished work. `record_team_claim` restores every touched field when the bounds check refuses (`task-state-too-large` now settles as a reopening refusal), keeping the invariant that a definitive refusal is proof no claim was recorded — the accept path persists state even under refusals. And the assignee fail-fast a board-governed creation forgoes gains its bounded replacement: `AgentTaskLimits::max_unclaimed_millis` (default `AGENT_TASK_DEFAULT_MAX_UNCLAIMED_MILLIS`, one day; `None` opts into an unbounded wait), observed lazily by the settle pass like every board horizon — a team-bearing task no claim ever names expires through the cancellation machinery whole (`cancellation-requested` / `unclaimed-expired`), surfacing the wrong-team-id or never-posted mistake and settling its delegated escrow home instead of parking silently forever. Proven by the four new race tests in `tests/team_claim_assignment.rs`, the eviction test in `tests/team_board.rs`, the no-partial-mutation unit pin, and the new `tests/task_unclaimed_expiry.rs`.

- `rakka-agent` moderation (slice 5.3; specification 8.11 and 17.13; scenario 43):
  - The sharded conversation entity (`conversation.rs`, entity type `RakkaAgentConversation`, the fifth sharded entity): a stable `AgentConversationId` scope whose durable state is the moderator, the ordered immutable participant roster, the mode, the completion rule, the embedded trusted `AgentModerationPolicy`, the governing `AgentTaskId` binding, the durable round/turn cursor, the dense turn ledger, the bounded transcript ring plus an identity-only transcript artifact reference, and the creation-fixed budgets — all written in single compare-and-sets over the same `AgentExchangeHost` substrate the other entities use, with a deduplicated operation-id inbox (`Duplicate` answers the original outcome), append-only conversation history through the new `AgentConversationHistoryStore` (in-memory backend; PostgreSQL owed onward, the team precedent), opt-in idle passivation, and lazy deadline expiry: a passed creation-fixed deadline refuses commands purely (`conversation-expired`) while the settle pass records the durable flip — no timer ever fires for a conversation, and the refusal never depends on whether the sweep ran (the team `require_active` rule, kept deliberately over a separate pre-flip code). The entity initiates and receives no exchange this slice — `AgentExchangeKind::ALL` stays at fourteen — but embeds the host with an empty journal so the conversation-terminal notification of the replayable-events slice is a code change, not a schema migration; creation is an entity command with no wire carrier: a conversation is trusted application data.
  - Only the current authorized participant may submit the next turn (scenario 43's fence): turn ownership is *derived* from the durable cursor — round-robin owner is `participants[turn]`, moderator-directed owner is the moderator on even turns and the stored `designated` on odd ones, the one owner fact that is a decision rather than a derivation — so recovery recomputes the owner by construction, never from residency. The two modes cover the two recovery shapes; a broadcast mode is deliberately absent. Moderator-directed moderator turns must carry a direction (`Designate` a roster member or `CloseRound`, mutually exclusive on the wire), the round ceiling parks the cursor under `ModeratorDecides` (`conversation-rounds-exhausted`, status stays active — never a silent park) while `AllRounds` ends the conversation in the same compare-and-set that completes the final round (completion beats exhaustion), and the turns-per-round rim admits only `CloseRound` (`conversation-turns-exhausted`). Creation refuses any policy whose maxed-out ledger and ring could not fit the state bound (`conversation-policy-too-large`) — a mid-round `conversation-state-too-large` would wedge the protocol with the early end as its only exit, so the arithmetic holds at the door.
  - **Turn deduplication is layered** (scenario 43's "without duplicating a turn"): the operation id derives from the turn's own logical coordinates *and its body digest* (`conversation_turn_operation_id` under the reserved `AgentOperationKind::ConversationTurn`; the new `ConversationOperation` kind covers create/end/expiry, the end round-qualified because a retried end at a later round is a new decision), so a durable redelivery re-derives the same operation whatever its message id. Inside the bounded window the operation log answers `Duplicate`; past it, the dense turn ledger is the durable echo — checked before every other guard including the terminal one, so a replay converges even after the conversation ended — with a regenerated same-coordinate submission refusing loudly (`conversation-turn-content-mismatch`) instead of being silently absorbed, and a foreign speaker's claim refusing as superseded (`conversation-turn-superseded`). The ledger stays affordable by the creation-time arithmetic; the transcript ring is never a deduplication surface (drop-oldest with visible `messages_dropped`). An accepted turn's reported usage is recorded even when it overshoots — the spend already happened in the speaker's run — and exhaustion refuses the *next* turn under the budget vocabulary's own code (`tokens`), never parking: budgets reuse `AgentBudgetConsumption`/`AgentBudgetExhaustion` and the deadline-fixed-at-creation idiom whole, with no parallel machinery and no new conserved dimension. The moderator's early end (`EndEarly`) fences on the round it was decided against (`conversation-end-stale-round`), gates on the policy's `moderator_may_end_early`, records its authenticated provenance, and terminalizes the conversation only — its proposed *result* rides the governing task's existing typed-result validation and goal-evaluation doors on the run side, unchanged.
  - The A2A surface: the collaboration extension gains the conversation cluster (`AgentConversationCollaborationMetadata`, the fourth shape under the one `io.rakka.collaboration` key, discriminated by its `conversation` field checked before `team` and `handoff` — `deny_unknown_fields` makes a multi-discriminator payload fail whole; ordinary clients stay untouched) carrying the bounded verb vocabulary submit-turn/end — never create. A conversation send must not name `message.task_id` (`conversation-send-names-task`); the early end requires an authenticated principal (the management-write precedent). The command authorizes under its own `A2AOperation::ConversationCommand` with the typed `A2AConversationClaim` (verb, claimed speaker, coordinate) bound into the authorization request, applies through the conversation entity's deduplicated inbox on the service's new conversation stores (`RakkaAgentA2AService` grows to eight store generics, spelled once through the new `SharedRakkaAgentA2AService` alias), answers with an immediate response message (a turn command never creates a task), and surfaces domain refusals — the owner fence above all — as structured `Rejected` payloads the caller rebases on.
  - Observability (specification 17.13): conversation history kinds for creation/turn/round-advance/end/expiry (identities, coordinates, and codes only — never bodies), and the new bounded counter `rakka.agent.moderation.turns` ({`operation`, `outcome`}, both already in the closed metric-key vocabulary) counts each durable decision once at the entity boundary — duplicates, ledger echoes, and replays count nothing. The guidance metric table's `mode` label and the `rakka.agent.moderation.turn` otel span row stay owed with the model-visible moderation tool, whose `AgentModerationPolicy.tool` hook ships shaped but dormant (the team precedent); the goal view's conversation join stays owed to the replayable-events slice with the task-side provenance echo and terminal notification.
  - Proven by the new `tests/conversation_protocol.rs` (creation/dedup, the roster and transcript-reference guards, the worst-case policy arithmetic, the fenced early end, token exhaustion refusing without parking, the once-only lazy expiry flip, the ring dropping oldest with deduplication never riding it, the pre-slice policy decode, and the golden-vector operation-id pin), `tests/conversation_turns.rs` (roster-order ownership, the owner and out-of-order fences, in-window duplicates, the seventy-turn past-window ledger echo converging even after the conversation ended with regenerated and superseded submissions refusing loudly, the moderator-directed designation flow with its rim, and the completion-rule split), `tests/conversation_recovery.rs` (self-covering crash-point sweeps over every conversation-store write of a full round — one ledger record per coordinate, one round advance, one usage charge, no duplicated history slot — plus the committed-but-unflushed history window), `tests/conversation_passivation.rs` (scenario 43 over real sharded entities: participant, round, turn owner, transcript reference, and budgets recovered at zero resident actors with the redelivered turn answering `Duplicate`; the stored designation surviving as the one non-derivable owner fact; an idle conversation passivating on its own), the moderation-turns assertion in `tests/agent_metrics.rs`, and `rakka-a2a`'s new `tests/conversation_surface.rs` (scenario 43's wire half over the real service core: the retried send converging on one recorded turn under a fresh message id, the fail-closed matrix, per-operation authorization denial with the claim bound in, and the plain-client-untouched posture).
  - Review hardening on the slice, closing the two blocking findings the entity shipped with. **The early end is the moderator's alone** (specification 8.11's "the moderator MAY end early under policy"): `EndEarly` now carries the ending agent and the transition fences that claim against the durable `moderator` (`conversation-end-not-moderator`), so a roster participant — or anything else the deployment authorizer lets issue a conversation command — can speak but never terminalize. The claim is a claim exactly like a turn's speaker: the wire names it in the same `io.rakka.collaboration.participant` field (a nameless end fails closed at the mapping boundary, before anything durable happens; the authorizer already sees it in `A2AConversationClaim`) and durable state decides. The policy refusal still comes first, because whether a conversation permits an early end at all does not depend on who asks — the discipline that already puts the round ceiling ahead of the turn-owner fence. **And a conversation store that loses a compare-and-set recovers instead of wedging**: `ensure_recovered` asks the host whether it still holds the authoritative record rather than trusting its own flag, the rule the task and run stores already carry. A conversation has two writers by construction — the resident sharded entity, which holds one store for its whole residency, and the A2A service, which builds its own on any node — so the loser of one race previously answered `exchange-not-recovered` to every command, exchange, and settle for the rest of its residency. Proven by the new non-moderator fence tests in `tests/conversation_protocol.rs` and `rakka-a2a`'s `tests/conversation_surface.rs` (the impostor end refusing as a decision the caller rebases on, the nameless end failing closed, the conversation still live after both), and the two-writer race test in `tests/conversation_recovery.rs` (the stale resident losing its compare-and-set, then reloading to answer the redelivery from the ledger and serve the next turn).
  - Review hardening, second pass: the three defects that undercut the durable turn protocol itself. **The direction is content, so it is identity**: `conversation_turn_body_digest` becomes `conversation_turn_content_digest(body, direction)` and covers the moderator's direction, so the turn's operation id *and* the dense ledger's stored digest both fence it — a turn regenerated to `Designate` where the recorded one closed the round now refuses `conversation-turn-content-mismatch` instead of being absorbed as a `Duplicate` for a designation nobody recorded. Segments stay length-prefixed, so the three direction shapes encode injectively; the golden vectors gain one pin per shape. On the wire the two spellings map through a single new `conversation_direction` helper shared by the operation-id derivation and the command build, because a spelling one side honored and the other dropped would put a turn's identity out of step with the decision it names. **The ledger echo is answered before the history headroom guard**, hoisted out of `apply_transition` into `apply`: the echo writes nothing at all, so a redelivered turn now converges through a history-sink outage instead of refusing `conversation-history-backlog` and leaving the retrying caller unable to learn its turn landed — history is observability, the ledger is the truth. (Refusals from the echo still count at the moderation counter; the echo itself still counts nothing.) **And the reported per-turn spend is bounded like every other wire claim**: the new `AgentModerationPolicy::max_turn_tokens` (default `AGENT_CONVERSATION_DEFAULT_MAX_TURN_TOKENS` = 1,000,000; `without_turn_token_ceiling()` opts out) refuses an implausible self-report with `conversation-turn-usage-too-large` *before* it touches the shared grant — one turn could otherwise report `u64::MAX` and exhaust a creation-fixed budget for every other participant, with no reachable progress left because exhaustion refuses rather than parks. Overshooting what *remains* stays legal below the ceiling, unchanged: that spend already happened in the speaker's run. The spend is now attributed too — the `TurnRecorded` history entry carries the reported count beside the speaker, so an exhausted conversation is never an anonymous total (counts and coordinates only, never bodies). The pre-slice policy shell still decodes, the new field taking its serde default. Proven by the new direction-fence and history-outage tests in `tests/conversation_turns.rs` (the outage test contrasting a *new* turn refused with nothing committed against the redelivery converging on the same store at the same moment, and a regenerated submission still refusing loudly through the outage), the implausible-report and attribution test in `tests/conversation_protocol.rs`, the per-direction identity pins, and the wire direction-mapping test in `rakka-a2a`'s `tests/conversation_surface.rs` over a moderator-directed world.
  - Review hardening, third pass: the state-bound accounting, which was three ways from being an upper bound on anything. **The bound now measures what is persisted.** `check_bounds` moved from `AgentConversation` to `AgentConversationState` — the value the compare-and-set actually writes, including the 64-entry operation log, the 32-entry pending history outbox, and the exchange journal, none of which any bound had covered — and it runs last in a transition, over the history entries that transition just owed. **The creation-time reserve now charges for all of it**, with a named per-entry reserve for each bounded collection (`AGENT_CONVERSATION_OPERATION_LOG_ENTRY_RESERVE_BYTES`, `AGENT_CONVERSATION_HISTORY_ENTRY_RESERVE_BYTES`, `AGENT_CONVERSATION_MESSAGE_RECORD_RESERVE_BYTES`) instead of folding them into one flat overhead constant. Honest accounting does not fit the old 32 KiB figure — the operation log and outbox alone are ~45 KiB of policy-independent overhead — so `AGENT_CONVERSATION_MATERIALIZED_MAX_BYTES` is now 128 KiB, sized so **every policy the hard caps admit provably fits**: the mid-round wedge the door guard exists to prevent is impossible by construction rather than merely refused, and `conversation-policy-too-large` becomes the defensive net that keeps the invariant honest if a cap or reserve ever changes. **The ring is charged what it costs to store**: `max_message_bytes` now measures a body's JSON-escaped length, so the number the door reserves is the number the bound sees — plain text is unaffected (its escaped length is its raw length), while a body of quotes costs double and refuses at half the characters, reporting the stored cost. **And the turns-per-round ceiling bounds records again**: it is checked for every turn rather than only the moderator's designating one (which let a round record `max_turns + 1` turns, billing a turn the operator never admitted), with the designation look-ahead widened to reserve all three slots a designation commits — this turn, the designated participant's, and the moderator's closing turn — so a round can always still close. Proven by the new `the_creation_arithmetic_upper_bounds_what_the_state_guard_measures` in `tests/conversation_protocol.rs`, which saturates a maxed policy for real — a full ledger, a full ring of maximally escape-expensive bodies, a full operation log, and a pending outbox filled through a downed sink — and asserts the measured serialization against both the reserve and the bound; plus the stored-cost message-ceiling test beside it, and `a_round_never_records_more_turns_than_its_ceiling_declares` in `tests/conversation_turns.rs` across four ceilings, which reports `a round declared 2 turns and recorded 3` against the old guard.
  - Review hardening, final pass: the remaining seven findings. **A configuration with no reachable terminal state refuses at the door** (`conversation-completion-unreachable`): the moderator-decides rule only *parks* the cursor at the round ceiling, so the early end is its sole exit, and forbidding it without a wall-clock deadline left a conversation whose governing task waited on a signal that could never come — each of the three roads out (the early end, a deadline, the all-rounds rule) makes it admissible again. **A parked cursor names no next speaker**: `turn_owner()` observes the round and turn ceilings, so `current_speaker` stops advertising a turn that would refuse `conversation-rounds-exhausted` forever and a driver polling it is routed to the moderator's end instead of retrying. **The early end's identity covers its reason** — `conversation_end_operation_id` takes the reason and digests it, the turn's body-digest discipline applied to the one other caller-reachable decision, so a regenerated end refuses `conversation-ended` instead of being answered `Duplicate` while the audited reason stayed the first attempt's; the new `conversation_create_content_operation_id` does the same for creation, so a second creation with a different roster refuses `conversation-already-created` rather than echoing a conversation it does not describe. **`AGENT_CONVERSATION_REASON_MAX_BYTES` is enforced** (`conversation-reason-too-large`) instead of being silently truncated at twice it by the generic detail bound. **The early end records who ended it**: conversation history entries gain `principal` and `reason` (both `#[serde(default)]`, so a pre-slice entry decodes unchanged), and the early end fills them beside the moderator as participant and the round it was decided against — leaving `detail` to hold the stable terminal code alone, ending the three-way overload a reader previously had to guess at. **And the wire records the moderation counter**: `RakkaAgentA2AService::with_metrics` wires a recorder into the entity stores the service builds itself, so `rakka.agent.moderation.turns` is no longer permanently zero for the only carrier the turn protocol has. Proven by six new tests in `tests/conversation_protocol.rs` (the unreachable-completion refusal with all three roads out, the parked projection, the bounded-and-attributed early end, the regenerated end refusing, the content-qualified creation), the wire counter test in `rakka-a2a`'s `tests/conversation_surface.rs` (which fails with `the applied turn counted: []` when the wiring is removed), and two in `tests/conversation_recovery.rs`: the committed-but-unflushed window is now *created* by a healable sink that is genuinely down — the test previously named a window a working sink never produced — and the history read cursor is paged end to end under a page size smaller than the history, asserting every entry exactly once in sequence order.
- `rakka-agent` human-owned tasks and the dependents registry (slice 5.4; specification 8.12, 9.2, and 14.3; scenario 41):
  - `AgentTaskEntityCommand::SubmitHumanResult`: an authenticated human or external service completes a deliberately unassigned human-owned task with a typed result, through the *same* deterministic validation core a run's proposal travels — the shared `validate_result` claim plus the origin-neutral accept/reject cores, so the two paths cannot drift. The operation id is derived by `human_result_operation_id`, pure over `(tenant, task, discriminator)`: a retried submission converges on the original decision (a recorded rejection included), a corrected resubmission carries a new discriminator. The fence ladder answers the durable echoes *before* every guard, the terminal one included — the accepted result replays idempotently past the operation log's bounded window, the latest rejection echoes from the materialized record, and older rejections are refused from a bounded fingerprint ring (`AGENT_TASK_REJECTED_SUBMISSION_ECHO_CAPACITY`) so no replay can spend the rejection budget twice — and every refusal is non-committing (`task-not-human-owned`, `task-not-awaiting-input`, `submission-principal-missing`/`-too-long`, `submission-causation-too-long`, `submission-already-rejected`, plus the shared task codes). Exhausting the rejection budget fails the task exactly as a run's exhaustion does. `AgentAcceptedResult.run` became `Option<AgentRunId>` with an additive `principal`; task history rows gained an additive `principal` for authenticated attribution. A human task is never a substitute for an effect-bound checkpoint: exact-effect approval stays `AgentCheckpoint`-bound, pinned in the ownership and command docs.
  - The dependents registry — the sending half of dependency propagation deferred from slice 4.6. Two new Task→Task exchange kinds: `DependencyRegistration` (owed by the dependent in the same compare-and-set that records the forward edge — creation, late declaration, or the settle pass for a pre-registry edge; the upstream records a bounded reverse edge, capped at `AGENT_TASK_MAX_DEPENDENTS` = 32) and `DependencyOutcome` (owed by the upstream's terminal transitions through the one `owed_child_reports` consult point, *immediately* at the terminal commit — status, terminal reason, and result digest are absorbing, so unlike a delegation report nothing waits for escrow closure). An already-terminal upstream answers a late registration with the outcome on the receipt, so it never records a moot entry or owes a notification for it. The receiving side applies the outcome through the existing `record_dependency_outcome` core, which is what guarantees a failing edge takes `request_task_cancellation` over a live dependent run, never a direct terminalization; the `RecordDependencyOutcome` command remains the application-relay path, double delivery converging by the same-outcome-idempotent/different-outcome-conflict rules. Sender fencing at both ends (`dependency-registration-forged`, `dependency-outcome-forged`), settled markers on the edge and the registry entry as the once-guards past the journal window, and — the deliberate posture — a registration to a not-yet-created upstream stays *outstanding*: a racing create converges on the next re-drive, and a never-created upstream leaves the dependent durably `Blocked`, the documented stuck-dependency struggle signal. To make that posture livable, `drive_pending_exchanges` now records an `UnsettleableRefusal` against the outstanding exchange instead of erroring the whole settle pass — one unanswerable envelope can no longer wedge every other exchange an entity owes — and reports it, see below.
  - An unanswerable exchange is now reported and bounded rather than absorbed. The courier's `AgentExchangeDriveReport` gained `unsettleable: Vec<AgentExchangeUnsettleable>` (the exchange kind and the receiver's refusal code, both from closed vocabularies), every entity progress type — task, run, team, conversation — gained an `unsettleable` count beside `failed`, and each of the four settle passes emits the new `rakka.agent.exchange.unsettleable` counter, labeled by bounded `operation` and `error_code`. Not failing the pass is deliberate and stays: one stuck envelope must never wedge the other exchanges an entity owes. But a pass that returned `Ok` and said nothing made a durably wedged entity indistinguishable from a healthy one, which is the half that was wrong. The cost is bounded too: the new `AgentExchangeJournal::record_unsettleable_refusal` writes only when the recorded code *changes*, so a standing wedge no longer burns a durable revision on every settle pass forever — the courier keeps re-delivering, because delivering is the only thing that discovers the receiver can finally answer, but standing wedged is no longer a standing write. The exchange that is stuck, and since when, stay legible on the journal's own pending record.
  - The human-submission door, closing the review findings against its first cut. Five ways it could refuse a submission that had already been decided, spend a budget on a submission nobody made twice, or accept one it should not have. (1) A human-owned task with no dependencies is born `WaitingForInput`, which is precisely what the door reads as "the graph permits this now" — so a *late* `DeclareDependency` now demotes it to `Blocked` exactly as it does an agent-owned task, and the door additionally re-tests `dependencies_satisfied()` status-independently (`task-dependencies-unresolved`), the same belt-and-braces `awaits_assignment` has always had. A projection is the wrong thing to gate determinism on. (2) The past-window submission echo is answered *before* the history-headroom guard rather than inside the transition behind it — the ladder is now the pure `probe_human_submission_echo`, and a replay of a decided submission writes nothing at all, so a caller whose submission durably completed the task can still learn so while the history sink (pure observability) is backed up. Same rule, same reason, as the conversation ledger's echo in slice 5.3. (3) `AgentTaskLimits::validate` refuses `max_result_rejections` above `AGENT_TASK_REJECTED_SUBMISSION_ECHO_CAPACITY`: the ring is what stops a replayed rejection spending the budget twice, and a budget larger than the ring is a guard that silently covers only part of what it claims. (4) `AgentTaskDefinition::validate` refuses `EvidenceRequired` on a *human-owned* definition. No submission surface carries evidence artifacts yet — the A2A binding's `evidence_digest` is advisory and the artifact strategy is deferred — so the rule could never be satisfied, and every attempt was a committed rejection walking the task to `ResultRejectionsExhausted` and cancelling its dependents with it. Refused where it is declared instead of discovered when someone's approval is spent; an agent-owned task may still require evidence, because its run can carry artifacts. (5) `AgentAcceptedResult.run` is serialized `skip_serializing_if = "Option::is_none"` like every other optional cell rather than as an explicit `null`; nothing is deployed on the pre-`Option` shape, so `CURRENT_AGENT_TASK_STATE_SCHEMA_VERSION` is deliberately not bumped and the field says so.
  - The typed client's result submission is idempotent by default and keeps its conversation. `AgentClientTaskResultRequest::deduplication_key` left unset used to fall through to `Message::new`'s fresh random id as the ingress discriminator, so every retry was a *different* durable submission: one lost reply plus one human mistake could spend two rejections, and a few retries could reach `ResultRejectionsExhausted` on a submission its caller made once. The transport now derives the key from the submission's own claim — task, contract, content — so a retry converges on the decision already recorded and a corrected resubmission carries a different identity without the caller having to remember to change one. The request also gained `context`, forwarded to the A2A `context_id` exactly as `AgentClientTaskRequest::context` is; without it the surface correlated the task with itself and silently dropped it from the conversation that created it.
  - Dependency-graph liveness, closing the review findings against the registry's first cut. Three edges could leave a dependent durably `Blocked` with no terminal status, no failure policy applied, and no signal. (1) `AgentExchangeHost::accept` memoized *every* result in the receiver's applied log, refusals included, so the racing-create convergence above never actually happened: each re-drive replayed the recorded `task-not-created` instead of re-running the arm. The host now declines to memoize exactly the refusals its participant's own `check_settle` classifies unsettleable — the same gate that keeps them outstanding on the initiator, so the two sides agree by construction, and the class is inherently non-mutating (an uncreated receiver, an undecodable payload, a kind this binary predates). (2) A *definitively* refused registration — the ceiling, a bounds failure, a forged claim — means the upstream holds no registry entry and never will, so no notification can arrive; the dependent now resolves that forward edge `Failed` through the same `record_dependency_outcome` core an upstream failure takes, and the edge's declared `AgentDependencyFailurePolicy` decides what that means, instead of the edge hanging unresolved. (3) The dependents registry (32) and the forward edges (32) share one `AGENT_EXCHANGE_PENDING_CAPACITY` (64) pending list, and recording an owed exchange fails the whole transition on overflow — so a fully-connected task could fail to terminalize at all. A terminal task now withdraws the registrations it can no longer act on (new `AgentExchangeJournal::withdraw`, for exchanges whose result the initiator provably cannot consume), and the notification derivation fits itself to the journal's free slots, deferring the remainder to the next settle pass rather than failing closed. Deferral is safe because the derivation is pure over durable state and the registry entry's `outcome_settled` marker survives the transition.
  - The A2A wire half, unlocking the slice 1.12 deferral: a plain `message/send` naming an existing `task_id` is now the authenticated typed-result submission — the parts carry the result, the new `io.rakka.agent.result` metadata object carries the declared contract (definition, versions, schema; parsed whole or refused), the authenticated principal is required, and the send authorizes under its own `A2AOperation::SubmitTaskResult` with the claimed contract bound in as `A2ATaskResultClaim` — never an undifferentiated send. A committed validation rejection answers the ordinary task view (never an error claiming nothing happened) with the rule code riding the projection's new bounded rejection echo (`io.rakka.agent.rejections`, `io.rakka.agent.last-rejection`), assembled in `agent_metadata_from_snapshot` so it heals on every read and write path; non-committing entity refusals answer as `Refused` decisions the caller rebases on. Two normalize-time guards close the fall-through holes: a delegation envelope naming `message.task_id` refuses `delegation-send-names-task`, and a result binding on a task-creating send refuses `result-submission-requires-task`. The typed client gained `submit_task_result` (`AgentClientTaskResultRequest`; a required `AgentClientTransport` method, breaking for external transport implementors) over the identical durable path.
  - Observability: `rakka.agent.human.results` ({`outcome`: accepted/rejected/exhausted}) and `rakka.agent.dependency.outcomes` ({`outcome`: completed/failed/cancelled}), both counted as durable-state differences across the committed transition, so duplicates, echoes, and refusals count nothing. `RakkaAgentA2AService` now wires its recorder into *every* entity store it builds — the four task stores and the team store, not only the conversation store — because the service constructs its own stores rather than routing through the sharded entities, and a store built without the recorder silences its counters with no other symptom. For a human submission the wire is the only carrier there is, so an unwired store meant `rakka.agent.human.results` could never leave zero in a real deployment; the wire test now asserts the counter rather than only the durable record. The wiring is no longer per-site: three private accessors (`task_store`, `team_store`, `conversation_store`) are the only places the service constructs an entity store, and the new `crates/rakka-a2a/tests/service_shape.rs` fails if a second construction site appears. This defect had shipped three slices running — the conversation store in 5.3, the task and team stores in 5.4 — each time by adding a `new` call beside the wiring rather than through it, so the rule is now enforced rather than documented.
  - Proven by the new `tests/human_owned_tasks.rs` (scenario 41 end to end: the authenticated completion unblocking a real dependent with the registration and notification legs settling durably; the exhausted human task cancelling its live dependent through the request path with the run winding down before terminal `Cancelled`; the golden-vector operation-id pins; the full refusal ladder committing nothing; past-window replays of the accepted and rejected submissions converging without re-spending; and the owner-loss sweep over every task-store write of the whole flow), `tests/dependency_registry.rs` (the already-terminal-upstream receipt, the `ContinueWithEvidence` edge proceeding past a failure, the forged/unknown/conflicting fencing matrix, the thirty-third-dependent ceiling, relay-and-exchange convergence, the pre-registry edge registering on the settle pass, and the three liveness edges: a dependent created *before* its upstream converging on the re-drive, a ceiling-refused registration resolving the dependent's own edge under its policy, a task at both ceilings terminalizing and notifying all thirty-two dependents with nothing left owed, and a permanently unanswerable exchange counting on every sweep while its attempt counter moves exactly once), the substrate-level withdrawal test in `tests/choreography.rs`, the two new metric assertions, the two new exchange kinds riding `tests/choreography.rs`'s six failure windows via `AgentExchangeKind::ALL`, and `rakka-a2a`'s new `tests/human_task_surface.rs` (scenario 41's wire half: the completing submission and its duplicate converging on one terminal view, the fail-closed matrix ending with nothing durable changed, per-operation authorization denial with the claim's fields asserted in the check, the rejection echo on the `Ok` view, exhaustion projecting `FAILED`, the owner-loss sweep over the submission, and the typed client converging through the same durable path).
- `rakka-agent`/`rakka-a2a` terminal coordination notifications (slice 5.5b; specification 8.10, 8.11, and 14.2 — the two write-path debts slices 5.2 and 5.3 recorded, re-parked when 5.5 shipped as a read contract):
  - `AgentExchangeKind::TeamTerminalNotice` (task → governing team): a terminal board-governed task now closes its board entry *eagerly*, in place of the lazy close that waited for some member's claim attempt to be refused `Done` + code — a board can no longer hold a live-looking entry for a task that ended minutes ago. The notice is owed from the same compare-and-set as the terminal commit through the `owed_child_reports` consult point, with a settle-pass twin (`settle_team_terminal_notice`) backstopping the terminal transitions that never consult it (goal budget exhaustion, stagnation, the human path's exhausted rejections) — and deliberately back-filling: a task that terminalized *before* this exchange existed loads its marker unset and closes its stale entry once on its next settle pass. The operation id is pure over `(tenant, team, task)` — terminality is absorbing under an immutable governing team, so one logical notice exists per task, ever. The receiving board applies it behind **two payload fences, not one**: the notice must be initiated by the task it names (`team-terminal-notice-forged`), *and* it must report that task as ended (`team-terminal-notice-not-terminal`). The sender fence alone proves who sent the notice, never that the sender is terminal — a task's own entity is the legitimate sender either way — so a notice populated non-terminally by a rolling-upgrade peer, a new terminal path that forgets the status, or an envelope minted before a state rewind would clear it trivially and evict a working member from live work for good. Both are definitive at both ends of the exchange: the courier re-delivers the *stored* envelope rather than re-deriving it, so a payload that fails on its face answers the same way for as long as it exists. Past the fences the board marks the entry `Done`, clears any standing claim, echoes the terminal-reason code on `last_code`, checks its bound, and **bumps the entry's claim epoch**. The bound check is not ceremonial: the close usually shrinks an entry, because a dropped claim costs more than the code echo adds, but not when it closes an entry no claim ever named — and `terminal_reason` arrives as a free-form wire string capped only at `AGENT_TEAM_DETAIL_MAX_LENGTH`, so thirty-two such closes carry up to sixteen kilobytes against an effective cap of twenty-eight. This is a growth point like `post_task`, and unchecked its overflow committed here and surfaced at the *next* `post_task` or `add_member`, wedging the board somewhere else with the bytes that caused it already durable. Validate-then-mutate is literal, the `record_team_claim` discipline: a refusal restores the entry whole. It stays outstanding rather than settling, because the board's own eviction of finished entries is what lets a re-drive converge. `Done` is absorbing under `settle_claim_action`, and by its own guard rather than by that bump alone: the eager close moves the entry past any in-flight reply, but the *lazy* close — the arm a claim attempt against an already-terminal task takes — closes at the entry's current epoch, so the epoch guard could not absorb a second reply for the same decision and a closed entry could be rewritten `Active` around a claim no task ever accepted. That entry would be wedged for the board's lifetime: eviction only removes closed entries, claim/release/transfer all refuse one, and the terminal notice is owed exactly once. An explicit terminal guard now sits after the epoch guard, and the `(Release, "team-claim-already-owned")` arm gained the `claim_is_current` check its five siblings already carried — it keeps the claim rather than clearing it, so against a board that had moved on it would mark a newer claim's entry owned on the strength of the previous claim's acceptance. Both are pinned by unit tests verified to fail with their own guard removed; the epoch bump is now defense in depth. A missing or already-`Done` entry — never posted, evicted under ceiling pressure, or closed by an earlier delivery — accepts idempotently with no board write; an entry re-posted after eviction closes lazily exactly as before. No active-team gate, the `apply_claim_result` posture: the board is data, and an expired team's entry still closes.
  - `AgentExchangeKind::ConversationTerminalNotice` (conversation → governing task), plus the task-side conversation provenance cell and the projection echo — what makes a terminated conversation *observable from its governing task*. Each of the three terminal flips — rounds complete, the moderator's early end, the lazy expiry sweep — owes the notice in its own compare-and-set (the conversation's exchange host, pre-wired empty in slice 5.3, now initiates its first exchange), with a settle-pass consult as the crash backstop and pre-slice back-fill. The task records `AgentTask::conversation` (`AgentTaskConversation`: conversation id, terminal status and reason, round/turn coordinates, `ended_at` — identity and coordinates only, never transcript content) with a `conversations` lifetime counter: latest-only, the handoff and team-claim precedent — the chain is history, as `conversation-terminal-recorded` rows. **The cell's `ended_at` is strictly monotonic**: a notice from a different conversation takes the cell only by being newer than the one already in it, and the counter rides the same guard because it counts notices *recorded*. Without that, an older conversation's notice re-driven after its operation aged out of the bounded applied ring — its reply lost, sixty-four exchanges ago on a task with dependents and board traffic — took the cell back from a newer conversation and re-incremented the counter, after which `get_task` echoed the older conversation's status/reason/rounds/turns and `agent_metadata_from_snapshot` healed the projection to that wrong value on every read. What the latest-only shape still costs is the other direction, and it is narrow: an older conversation whose *first* delivery arrives after a newer one is accepted without being materialized, so it appears in neither the cell nor the count, and stays readable through its own entity and the coordination replay surface. An already-terminal task still records the cell (deliberate, user-approved: the cell is observational provenance, and the common race is the conversation's completion landing beside the task's own result acceptance). Recording it **does not advance `AgentTaskState::updated_at`**, which means the time of the last accepted transition *of this task* — status, assignment, result, goal, ledger — and noting what another entity did is not one. The distinction is load-bearing rather than pedantic: that clock is the sole input to the board-governed unclaimed horizon, which re-arms from the task's last transition so a refused claim's reopened window starts fresh. Advancing it here let a conversation postpone an unrelated task's expiry, and because a task keeps no registry of the conversations naming it, it cannot tell one that legitimately governs it from a series minted to keep it alive — so a never-claimed task and its delegated escrow could be parked indefinitely past exactly the wait the horizon exists to bound. Any later echo about another entity must follow the same rule. The recording keeps the literal validate-then-mutate discipline, restoring both fields on a bounds refusal, and a pre-terminal record keeps the task's growth reserve so the cell can never eat the room the assignment, result, and terminal reason still need. The public A2A projection echoes the cell beside the delegation/handoff/team echoes under `io.rakka.collaboration` (`conversation-id`, `conversation-status`, `conversation-reason`, `conversation-rounds`, `conversation-turns`), assembled in `agent_metadata_from_snapshot` so it heals on every read and write path. The identity key is `conversation-id`, **not** the bare cluster name its three siblings use: `io.rakka.collaboration` carries two directions under one key, and `parse_collaboration_envelope` discriminates the inbound clusters on exactly those bare names, so an echo keyed `conversation` would have sorted ahead of `team` and `handoff` and let a task's own echo decide which cluster a round-tripped send was read as. The two older echoes are shipped surface and cannot be renamed, so the parser also gained the structural half: the version declaration is checked **before** the discriminators, and since every command shape requires `schema` while no echo carries one, a projection echo replayed onto a send is now refused for what it is (`the collaboration metadata object declares no schema version`) instead of under whichever cluster its keys happened to trip. It was refused either way — the defect was that the reason depended on the task's collaboration history rather than on what the client sent, so adding a terminated conversation to a handed-off task changed the answer. Any echo added later must keep clear of the bare cluster names.
  - Both kinds ride the established choreography idioms whole: sender fencing at both ends (`team-terminal-notice-forged`, `conversation-terminal-notice-forged` — the payload names its initiator and the receiver checks the envelope's address against it), journal initiation as the once-guard inside the bounded window with a durable settled marker past it (`team_terminal_notice_settled` on the task, `terminal_notice_settled` on the conversation, each flipped only when the settled envelope's operation id matches the one currently derived), and one shared refusal classifier per kind (`coordination.rs`) used by *both* the initiator's settle rule and the receiver's memoization gate, so the two sides agree by construction. `team-not-found`, a closed team, and a forged verdict are definitive on the team side; on the conversation side the forged verdict is the only one. Two of the task's other answers are the task saying *not yet*, so both stay unsettleable and unmemoized: `task-not-created`, the dependency-registration posture, so a notice racing its task's creation converges on a later re-drive; and a record too full to grow the cell, because the receiver charges a pre-terminal task the growth reserve its own lifecycle still needs and a terminal task nothing at all — the very cell refused today fits once that task ends, and settling on it would quiesce both ends over a refusal the receiver is about to stop making. `task-dependency-limit-exceeded` — the *other* exit of that same `check_bounds` call — classifies the opposite way and is definitive: a task's dependency map only grows and recording a cell never touches it, so that refusal is identical forever, and leaving it outstanding would re-run the receiving arm's durable write on every settle pass for the life of both entities. One bound the exchange can never satisfy, one it merely has to wait out. The conversation has no autonomous timer, so the store's own `apply` couriers best-effort after the command that flipped it: the sharded entity's `Command` arm sends itself no `Settle` and a terminal conversation accepts no further command, which makes the flipping command the last drive that notice would otherwise ever get. Past it, delivery rides whatever else drives the settle pass — the A2A surface after every conversation operation, the application's settle sweep, or recovery — so a crash between the terminal commit and delivery defers the notice, never loses it. That settle pass asks the owed-notice *derivation* whether it would advance anything rather than re-stating its bail-outs, so the no-write property cannot drift: the hand-mirrored precheck reproduced four of five and missed the reason-less terminal record — `terminal_reason` is `pub` and `serde(default)`, so a `status: Ended` row without one deserializes cleanly — and since `AgentExchangeHost::initiate` persists even for an empty owed vector, every pass then wrote byte-identical state forever. One predicate cannot disagree with itself, and its error now surfaces ahead of the revision the committed derivation would have burnt discovering it. A pre-slice binary fails to decode the new kinds (a delivery failure, outstanding until upgrade), and a same-binary receiver without the arm answers `unsupported-exchange`, which the settle rules keep outstanding — the rolling-upgrade rule.
  - **A standing delivery failure is no longer a standing write** (`AgentExchangeJournal::record_delivery_failure`, so every exchange kind, not only these two). An unchanged failure code now persists nothing and reports `false`, the rule `record_unsettleable_refusal` already applied to the sibling case for the same reason. The new terminal notice is what made it load-bearing: `owed_team_terminal_notice` fires for any terminal task carrying a governing team, with no check that the deployment routes `AgentEntityClass::Team`, and a missing route is a *delivery error* rather than a refusal — so no classifier sees it, no settle rule ends it, and the identical `exchange-no-route` answer came back on every sweep forever. A deployment that names a team on its tasks without hosting team entities now pays one durable revision for that, not one per settle pass. The exchange still stays outstanding, because delivering is the only thing that discovers a route has appeared, and the courier's report still counts the failure on every sweep, so a standing wedge stays alertable as a rate. The trade is that `attempts` stops advancing while the code is unchanged — the same trade the refusal side already made.
  - Audit and observability: new history kinds `team-task-closed` (team side, with the closing member when a claim stood) and `conversation-terminal-recorded` (task side), both flowing into the slice 5.5 replay surface under their scope-qualified labels; the team's eager close counts `rakka.agent.team.operations` {`close`, `applied`} once per fresh application at the accept boundary, replays never.
  - Proven by the new `tests/team_terminal_notice.rs` (the Active entry closing with no claim attempt — the done-when — plus the unclaimed close, the never-posted idempotent no-op, the epoch regression pin, the settled marker burning no revision, the committed-but-unsent window under an injected lost delivery, and self-covering crash sweeps over every team- and task-store write of the terminal flow), the four board-guard pins beside it in `team.rs` (the two `settle_claim_action` guards, the terminal-status fence, and the overflow-refusal pin, each verified to fail with its own guard removed), the dependency-ceiling classification pin and the owed-notice pins in `conversation.rs` (the reason-less record owing nothing, and three settle passes over one burning no revision — measured against the store, not argued), `tests/choreography.rs`'s undeliverable-envelope pin (five sweeps against a router with no routes: one attempt, one revision, five counted failures) and `tests/conversation_terminal_notice.rs` (all three terminal flips recording the cell, the two command-path courier pins — an early end and a completion flip each delivering with no settle pass at all, both verified to fail with the courier removed — the already-terminal task still recording it, the racing-creation notice staying outstanding then converging, the second conversation overwriting the cell while the chain stays in history, quiescence, and crash sweeps over both stores); the missing-team definitive settle in `tests/task_unclaimed_expiry.rs` and, beside it, the pin that two governed conversations ending deep inside the horizon leave it landing exactly where the definition put it (verified to fail with the `updated_at` bump restored, where the task is still `Created` past its own horizon); the bounds-restore pin beside `record_team_claim`'s, the reserve-window pin next to it that refuses the cell on a live task and admits the byte-identical cell once that task terminalizes, and the monotonicity pin that replays an older conversation's notice after a newer one recorded and asserts the whole record is untouched (verified to fail with the guard removed, where the cell regresses, the counter reaches three, and a fourth provenance row is written); the `close` counter assertion in `tests/agent_metrics.rs`; the scope-qualified label pins in `tests/coordination_replay.rs`; both kinds riding `tests/choreography.rs`'s failure windows via `AgentExchangeKind::ALL` (now 18); and `rakka-a2a`'s `tests/conversation_surface.rs` echo test — a task created over the public surface, its conversation ended over the wire, and `get_task` healing the conversation echo into the projection it had written before the end, now also asserting the echo keeps clear of the inbound discriminator — plus `tests/collaboration_surface.rs`'s echo-replay pin, which sends a task's whole collaboration echo back as a command and asserts the refusal names the missing schema rather than a cluster it tripped (verified to fail with the gate removed, where it classifies as a *team* command). `docs/rakka-compatibility.md` gained the register these keys belong in: the two directions the one metadata key carries and how they are told apart, the full echoed-key list across all four clusters, and the merged coordination-event kind labels including `team/team-task-closed` and `task/conversation-terminal-recorded`.
- `rakka-agent`/`rakka-a2a` replayable coordination events (slice 5.5; specification 17.13 and 14.5; scenario 45; open decision 21 resolved):
  - **A scoped replay cursor over the durable logs the coordination surfaces already keep.** Every coordination transition already wrote an ordered, deduplicated record — the task, team, and conversation history logs, and the run's decision-event sink — each written *after* the compare-and-set that decided it, on a sequence that transition consumed. So this adds no second write path, no new durable record, and no new outbox: the new `rakka_agent::events` module is a *read* contract over the four logs, and durable task/run state stays the correctness source. `AgentCoordinationCursor` encodes an `AgentEntityAddress` key plus its sequence (`task/acme/order-1:7`, `team/acme/support:3`, `conversation/acme/panel:2`, `run/acme/agent/run-1:4`), which is what gives the team and moderated-conversation scopes a cursor at all: the substrate's public `<task-id>:<sequence>` shape cannot name them, and it stays byte-identical as the compatibility commitment it is. Identity segments may contain a colon, so the sequence is taken from the last one. A cursor is never trusted on its face — it carries its own tenant and entity, so one naming any scope other than the one being read is refused (`coordination-cursor-scope-mismatch`), and a substrate-shaped cursor fed to this surface fails closed rather than being followed.
  - **Two answers, never a short page** (scenario 45's two halves). A replay resolves to a page contiguous from the cursor, or to an explicit `WindowExpired { oldest_retained, resume_from }` naming a cursor the reader can actually resume from once it has resynchronized from authoritative state — because a page that quietly skipped an evicted entry is indistinguishable from a complete one, which is the whole hazard. The page carries `complete_through` for the matching ambiguity at the head: entries reach their log on the settle pass *after* the transition, so an empty tail can mean "you are current" or "the entity still owes its sink", and the team and conversation snapshots gained `owed_history` beside `history_entries` so a reader can tell which. The merged `AgentCoordinationEventKind` label is `<scope-class>/<source-label>` because the source vocabularies are not disjoint — a task records `team-claim-recorded` when it takes a board claim and a team records `team-claim-recorded` when it makes one, and a label merging them would merge two different events at two sequences in two logs. The event itself is bounded identities, coordinates, digests, and stable codes; it never carries turn bodies, prompts, tool payloads, memory records, or credentials.
  - **Bounded retention is opt-in, and its contract lives on the trait.** The three in-memory history stores gained `with_retention(n)`, *off* by default: these logs are also the audit record specification 17.13 requires, and the entity refuses a transition rather than lose an entry, so enabling eviction forfeits that audit obligation for whatever the window drops and the builder says so. A read below the retained floor answers the new `HistoryWindowExpired { oldest_retained }` on each entity's error enum, and a hole *inside* the page truncates it before the hole — the retained prefix is delivered whole, whatever the reader's page size — while the read that *starts* at the hole is refused with the floor past it, as is a cursor past the newest retained entry, which the log never issued and an empty page would silently vouch for. `AgentTaskHistoryCursor::resuming_at` (and its team and conversation twins) is the companion that turns a reported floor into a cursor. `rakka_agent::testkit::assert_{task,team,conversation}_history_store_contract` is the conformance harness every backend can run, so the owed PostgreSQL history backends inherit the contract instead of reimplementing it.
  - **Six defects the replay contract would otherwise have been nominal against.** (1) The run's decision outbox drops its oldest *unflushed* event after that event already consumed a sequence, and the sink checked only the head — so a reader paged silently across the hole; the sink now stops its page before the discontinuity and refuses the read that starts at it, so the hole is declared while every retained event stays deliverable. (2) A decision whose derived identity could not be formed counted a drop *without* consuming its sequence, making that loss undetectable by any reader; it now consumes it, so the loss is a visible hole rather than a silence. (3) `AgentTeamError::is_domain_refusal` and its conversation twin are exclusionary, so a new variant becomes a "refusal" by default — the window-expired *read* answer would have been returned to a caller as a rejected command and counted against the entity's refusal metric; both now exclude it and document why the list is a trap. (4) `RakkaAgentA2AError::code()` flattened every projection failure to `"projection"`, so the existing `replay-window-expired` code never reached the wire despite `TaskProjectionError::code` documenting those codes as a compatibility commitment; it forwards the underlying code now, which is a deliberate change to an existing wire code. (5) `A2AAuthorizationRequest` had no constructor, so every new typed claim broke each struct-literal site; it gained `new(operation)` plus `with_*` setters, became `#[non_exhaustive]`, and the five existing sites moved over. (6) The service serves the run scope through an `Option<Arc<dyn AgentDecisionEventSink>>` builder rather than a ninth store generic, reading the three history stores from fields it already holds.
  - **The wire: two read operations, each its own authorization class.** `A2AOperation::CoordinationEventRead` carries the addressed scope as `A2ACoordinationClaim`; `A2AOperation::GoalViewRead` carries the goal as `A2AGoalViewClaim` — the goal-view wire surface slice 4.7 recorded as a follow-up, over its untouched principal-free assembly core. Both are direct service methods (the `replay_task_events` precedent; the agents surface's route binding has been deferred since slice 1.12, so the service core is the surface), and neither borrows the task-shaped cancellation normalization, so `resolve_agent_tenant` is the new tenant-only helper. `authorized_agent_goal_view_bounded` is the clamped entry point a wire needs — the unbounded wrapper fans out to the maximum whatever the caller asked. **A `GoalViewRead` denial answers `Ok(None)`, not an error**: byte-identical to an absent goal, to an unauthenticated caller, and to a goal that never existed, because a distinguishable wire error would reopen exactly the existence oracle the goal record's owner fence closed.
  - **The typed client** gained `coordination_events` and `goal_view` as required `AgentClientTransport` methods (breaking for external transport implementors, the slice 5.4 precedent), returning `rakka-agent` domain types directly. An exhausted window travels as the reply's own arm rather than an error, because a caller that must resynchronize still needs to know where to resume; the reconnect cursor is the subscription contract, and live push stays out of scope.
  - **Six derived struggle signals, read-time only** (specification 17.13): `AgentStruggleSignal` over approaching budgets, repeated iteration failure, repeated result rejection, stuck dependencies, stalled team claims, and moderation exhaustion, derived purely from the authoritative snapshots under an `AgentStrugglePolicy` whose thresholds are deployment policy and never durable. Deriving twice from one revision gives the same answer, and nothing they observe can mutate correctness state. The stuck-dependency signal is gated on how long the blocked task has been untouched, because an unsettled registration is normally one settle pass wide and an ungated derivation would report every freshly blocked task as stuck.
  - **Review hardening (the fifteen findings of the slice-5.5 review).** `AgentDecisionEventSink::read` returns the new `AgentDecisionEventPage`, whose `has_more` is the sink's own explicit answer — it was inferred from `events.len() == limit`, which a compliant short-paging sink would break into a silent stop (breaking for external sink implementors). The session view no longer blanks on a retention hole: it resumes at the reported floor and shows every decision the sink retains, with the loss still visible through `decision_drops` and `decision_lag`; only a sink *fault* degrades the view to unavailable. `AgentCoordinationSources::replay` takes the authenticated tenant and fences the scope against it inside the one shared entry point (`coordination-scope-foreign-tenant`), so a future surface cannot forget the check; a scope that was never created answers the new `coordination-scope-unknown` instead of an empty page — closing the pasted-cursor-as-scope trap, where a caller's own cursor `task/acme/order-1:7` supplied as the scope parses as a phantom entity that "recorded nothing" — and an unwired run scope is refused *before* the durable run read, so the stable `coordination-run-events-unavailable` answer no longer depends on the run store's health and costs no discarded snapshot derivation. `CoordinationEventRead` authorization now carries the authenticated principal (the typed client threads its own), so a deployment can grant coordination history to an operator without opening it to every caller in the tenant. The merged `AgentCoordinationEvent` carries `goal` for run-scope decisions, and the documented drop list names the loop phase and decision source it cannot carry. `AgentOperationalSnapshot` gained `has_accepted_result`, captured before redaction — the repeated-iteration-failure signal had gated on the snapshot's always-redacted `accepted_result` and never suppressed anything. Budget struggle signals iterate `AgentBudgetDimension::CONSERVED` with the stable `as_label` vocabulary (`loop-iterations`, `cost`, …) instead of a hand-rolled snake_case list, so a ninth conserved dimension is watched the day it lands and operators filter one vocabulary. The in-memory history stores keep retention inside the shared state, so clones share the bound with the log they share. A coordination replay `limit` of zero asks for `AGENT_COORDINATION_DEFAULT_PAGE_SIZE`. The conformance harness now probes a hole *inside* the log (prefix delivered whole, refusal naming the floor past the hole, resume from that floor), mirrors the stores' zero-means-one retention clamp, and asserts the page-size bound in all three copies; the flagship replay test anchors completeness to the store's own length rather than its own observations, and the surface tests pin the principal claim, the never-created-scope refusals, and the traversal-budget pass-through.
  - Proven by the new `tests/coordination_replay.rs` (the cursor resuming across task, team, and conversation logs one event at a time with no gap and no repeat; the same cursor answering the same page, including across a re-driven idempotent flush; the foreign-scope, cross-tenant, and substrate-shaped cursor refusals; the agent scope refused by name rather than answered with an empty page that would claim it had done nothing; the store conformance harness bounded and unbounded across all three logs; a reported floor resuming for real; and the kind-label injectivity pin against the `team-claim-recorded` collision), `tests/decision_events.rs`'s new dropped-decision gap test — which fails against the pre-slice sink — `rakka-a2a`'s new `tests/coordination_surface.rs` (the scoped cursor paging over the real service core, the tenant and scope fences, per-operation authorization with every claim field asserted inside the authorizer before anything is read, the owner's positive goal-view read and its clamped traversal budget, the four-way deny-is-absent equality measured against that real view, and the two unserved scopes refused by name), and two struggle-signal proofs in `tests/operational_query.rs`. The two terminal-notification exchanges slices 5.2 and 5.3 had recorded against this slice are re-parked explicitly on the new plan entry 5.5b: they are write-path choreography, not event replay.

### Changed

- Workspace MSRV is raised from Rust 1.80 to Rust 1.85, required by the published A2A SDK crates used by the Phase 0 clustered sharded A2A agents example.
- The workspace `axum` dependency is upgraded from 0.7 to 0.8. `axum` types appear in the `rakka-http` public API, and axum 0.8 replaces the `/:param` route syntax with `/{param}` and changes websocket text/close payloads to `Utf8Bytes`.
- Workspace crates now share release metadata and internal path dependencies include explicit versions for packaging.
- CI separates required validation from optional PostgreSQL and local Kubernetes integration jobs.
- Historical implementation plans now live under `docs/plans/` instead of the product-doc root.
- `rakka-workflow` outbox entries support a terminal `Cancelled` status: `DurableInbox::record_outbox_cancelled` settles an undelivered entry before dispatch, cancelled entries are never due again, and compaction reclaims them like other terminal entries.
- Agent dispatcher cancellation is durable end to end: `AgentDispatcherWorker::cancel_run_dispatches` settles cancelled effects at the outbox layer, in-flight entries carry a typed `cancellation_requested` flag that blocks re-claiming after lease expiry, and worker refresh finalizes expired cancellation-requested entries as cancelled instead of redelivering them.
- Agent dispatch target classification only honors `target_class` attribute and target-type refinements that are compatible with the effect kind, so a mislabeled effect can no longer be routed to a dispatcher that deterministically rejects it.
- Agent autonomy policy classification (`AgentAutonomyTargetClass::for_effect`) is derived from the dispatcher's `AgentDispatchTargetClass::classify`, removing the name-substring webhook/push heuristics so policy admission and dispatch routing always agree on an effect's class.
- `AgentEscrowLedger::widen` leaves a dimension the top-up does not name unchanged: `None` in a top-up allocation means *no additional allocation*, never *lift this cap*, so a partial goal-resume top-up (e.g. tokens only) no longer silently erases the bounded dimensions it did not name.
- The Phase 5 default autonomy catalog admits the agent domain's tool-family rides under their real classes: the `A2aPeer` descriptor accepts `ToolCall` with inline payloads (slice 4.3's outbound A2A sends carry no input artifact), and the `ChildWorkflow` descriptor accepts `ToolCall` while the dispatcher now classifies `workflow-tool` targets as `ChildWorkflow` — so denying or rate-limiting the class actually governs workflows-as-tools, and the default catalog no longer denies the sends the dispatcher routes. Workflow cancels deliberately stay in the tool family: a policy that blocks starting children must never block stopping them.
- `AgentFanInResolution::satisfied_by` now honors its documented invariant under the `All` policy: an unsatisfied resolution carries no members (per-member outcomes stay readable from the delegation and workflow cells), so non-empty `satisfied_by` always means the wait was satisfied.
- `AgentGoalSpec::validate` fails closed when the spec cannot be serialized (new stable code `goal-spec-unencodable`) instead of treating an encoding failure as zero bytes and letting the spec bypass the size bound.
- The delegation cell's durable `peer_status` carries stable kebab-case labels (`submitted`, `completed`, `input-required`, …) derived by a direct match instead of the peer SDK's protobuf wire labels (`TASK_STATE_SUBMITTED`).
- The knowledge-graph goal-claim source queries its configured spaces concurrently, so the goal view's claim join costs the slowest space rather than the sum of all spaces.
- `rakka-persistence-postgres` applies its migration under a session advisory lock (`MIGRATION_LOCK_ID`), so concurrent migrators no longer race PostgreSQL's system catalogs. `CREATE TABLE IF NOT EXISTS` is not atomic against a concurrent creation of the same table: two nodes starting together against a fresh database both saw the tables missing, both proceeded, and the loser failed to start with `duplicate key value violates unique constraint "pg_type_typname_nsp_index"` instead of the no-op the `IF NOT EXISTS` reads like. The guard matches the one `rakka-a2a`, `rakka-sharding-postgres`, and `rakka-agent-postgres` already applied to theirs — this crate, the oldest of them, was the one still unguarded — under a lock id distinct from every sibling's so the subsystems' migrations do not serialize against each other. The race is invisible once the tables exist, which is why a reused developer database never showed it; a DSN-gated test now races four migrators in a private schema.

### Security

- Security and operational defaults are documented in `docs/rakka-v1-security-operational-defaults.md`.
- Internal remoting is documented as trusted-cluster traffic and production exposure remains out of scope for v1 without an operator-provided security layer.

### Validation

- Required validation: `scripts/validate.sh`.
- Packaging validation: `scripts/package-check.sh` in Cargo offline mode.
- Release-candidate review: `docs/rakka-v1-release-candidate-review.md`.
- Optional PostgreSQL validation: `RAKKA_POSTGRES_TEST_DSN=postgres://postgres:postgres@localhost:5432/postgres cargo test -p rakka-persistence-postgres`.
- Optional knowledge-graph backend conformance: `RAKKA_POSTGRES_TEST_DSN=postgres://postgres:postgres@localhost:5432/postgres cargo test -p rakka-agent-knowledge-graph-postgres`.
- The PostgreSQL suites also run in CI on every pull request and push, against `pgvector/pgvector:pg16` on a fresh database each time, rather than only on a manual `workflow_dispatch`.
- Optional Kubernetes validation: `RAKKA_K8S_RUN_LOCAL_CLUSTER=1 RAKKA_K8S_IMAGE=<image> examples/kubernetes/local-cluster-scenario.sh`.

## 0.1.0-v1-rc.0 Draft

This section is reserved for the first reviewable v1 release-candidate notes.

### Release Checklist

- Run `scripts/validate.sh` from a clean checkout.
- Run `scripts/package-check.sh` from a clean checkout.
- Review `docs/rakka-v1-release-packaging.md`.
- Confirm optional PostgreSQL and Kubernetes checks were either run or explicitly deferred.
- Update this changelog with user-facing changes, known limitations, and migration notes.
